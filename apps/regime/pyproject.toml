[project]
name = "regime"
version = "0.1.0"
requires-python = ">=3.10"
description = "CWT-regime portfolio strategy: Optuna walk-forward search, backtest, trade live."
dependencies = [
    "ss-cli",
    "ss-loaders",
    "ss-features",
    "ss-indicators",
    "ss-wavelets",
    "ss-portfolio[alpaca]",
    "ss-plotting",
    "numpy",
    "pandas",
    "tqdm",
    "matplotlib",
]

[project.optional-dependencies]
research = [
    "bt",
    "optuna",
    # vectorbt is the fast vectorized backtester. Pin to 1.0.x; uv's
    # dependency-metadata override in the root pyproject strips numba
    # from its requirements (numba comes from the nix devShell — see
    # README.md "Nix dev shell" section).
    "vectorbt>=1.0,<2.0",
]

[project.scripts]
regime = "regime.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/regime"]
