# Leaderboard

Master log of every walk-forward / out-of-sample eval run in this repo,
in chronological order. Append-only — when a finding is upgraded or
downgraded, add a new row referencing the prior; never rewrite history.

> **Sharpe backfill (2026-05-18):** rows before 2026-05-18 that computed a Sharpe but recorded only IC / R² have had it added *in place* with a `[backfill 2026-05-18 ← artifact]` provenance tag; non-portfolio diagnostics are tagged `Sharpe N/A`. No train/val numbers, verdicts, or conclusions were altered.
>
> **Deflated-Sharpe backfill (2026-05-22):** the raw Sharpe column is *not* a fair cross-arc ranking key — it mixes construction / universe / era / friction, and many rows report a **mean of per-window Sharpes** that can flip sign vs the deployable return stream. The new [Cross-arc Deflated-Sharpe ranking](#cross-arc-deflated-sharpe-ranking) section ranks the deployable strategy arcs on the **deflated-Sharpe t-stat** (`ss_portfolio.standardize_oos`), computed on each arc's re-run OOS net return stream and deflated by the number of configs the arc tried. Only stream-bearing strategy arcs get a DSR; meta-evaluations (compose scalar Sharpes) and non-portfolio diagnostics are `DSR N/A` by construction. This is **additive** — no existing row's numbers or verdicts were altered. See [`findings/deflated-sharpe-leaderboard.md`](findings/deflated-sharpe-leaderboard.md).

The point of this file is to make the *OOS verdict* of every claim
visible at a glance, so an in-sample finding can't sit unchallenged in
[CLAUDE.md](https://github.com/sughodke/StockSurvey/blob/master/CLAUDE.md)
(or [Findings](findings/index.md)) for weeks before someone notices the
OOS check disagrees. A row here is the source of truth on whether
something is shippable; the prose pages around it are narrative.

## Operating conditions

Recurring universes, windowings, and friction params — referenced by
short tag in the table below so each row stays one line.

### Universes

| tag | size | source | span | filter |
|---|---:|---|---|---|
| **phase-2**       |   21 | Stooq archive `./StooqData/` | 2013-01-29 → 2025-12-11 | mega-cap fixed list (PHASE2_TICKERS) |
| **stooq_us_long** |  312 | `apps/notebook/data/stooq_us_long/manifest.json` | 2000-01-03 → 2026-04-24 | Stooq archive subset, baked-in |
| **factor-narrow** |  297 | stooq_us_long → `min_history_bars=6500` | ~2000 → 2026 | canonical factor walk-forward universe |
| **factor-wide**   | 2073 | full Stooq archive → `min_history=3500`, `start_grace_days=3650` | 2010-01-01 → 2026 | accepts late-listing tickers, NaN early bars |
| **regime-2010-24** | n/a | Stooq archive | 2010-01-01 → 2024-12-31 | regime trainer training span |
| **meta-alloc-arcs-6** | 6 arcs | `count_regimes_since_2005.build_master` (dca, gate, pairs, relational, dca_winner_4etf, vol_v3) | 2005-02-25 → 2025-12-11 | 6 strategy-arc daily return streams; vol_v3 enters 2024-04-12 only (5-arc panel pre-2024-04, 6-arc post) |

PHASE2_TICKERS = `AAPL, AMZN, BA, BAC, CRM, CSCO, DIS, GE, GOOGL, JNJ,
JPM, KO, META, MSFT, NFLX, NVDA, T, TSLA, UNH, WMT, XOM`. 5 of these
(CRM / GOOGL / META / NFLX / TSLA) post-date stooq_us_long's 2000
cutoff and are missing from that subset — Phase-2 only loads cleanly
from the full archive.

### Windowings

| tag | shape | notes |
|---|---|---|
| **6-window factor** | rolling, train=63 blocks (~3y) / val=39 (~2y) / step=39 (~2y) | rebal_days=20 unless noted; AdamW lr=1e-2 wd=1e-3 n_steps=200 default |
| **6-window factor (q)** | rolling, train=20 / val=12 / step=12 | scaled by 20/63 to keep train/val years comparable at rebal_days=63 |
| **6-window factor (10d)** | rolling, train=126 / val=78 / step=78 | scaled ×(20/10) from the 20d anchor to keep train/val years comparable at rebal_days=10 |
| **6-window factor (5d)** | rolling, train=252 / val=156 / step=156 | scaled ×(20/5) from the 20d anchor to keep train/val years comparable at rebal_days=5 |
| **regime-3w-optuna** | rolling, 3 windows, 20 Optuna trials/window | regime trainer multi-window search |
| **phase-2 split** | single split, train 2013-01-29 → 2020-12-31, val 2021-01-01 → 2025-12-11 | 8y / 5y; mirrors `build_canonical_checkpoints.py` |
| **70/30 single** | single split, first 70% train / last 30% val | one-shot train/val, not rolling |
| **single train+val ticker** | one trained set + one held-out ticker | not technically walk-forward; SSL reconstruction context |
| **meta-alloc-3fold-2015-25** | three contiguous OOS folds (2015–2018, 2019–2022, 2023–2025-11); pooled OOS n=2740 days | rebal cadence 20 TD, 10 bps switching cost per `|Δw|/2`; expanding-window training; lookback L=252 unless noted |

### Friction / portfolio defaults

`rebal_days=20`, `commission_bps=10.0`, `top_n=10` for portfolio metrics
unless explicitly overridden in a row's notes column.

### Verdict labels

- **confirmed-OOS** — val performance matches or extends the train
  finding. Safe to cite as evidence; safe to ship if other gates pass.
- **reversed-OOS** — train win does not survive in val (or worse, val
  underperforms baseline). Do NOT ship; treat the train finding as
  in-sample artifact.
- **partial-OOS** — some windows / metrics survive, others don't.
  Caveat-laden; not shippable without further qualification.
- **confirmed-null** — both train and val show no edge. Useful as a
  falsification (saves later researchers from re-running it) but does
  not advance the strategy.
- **diagnostic** — stability / structure observation rather than a
  shippable claim. E.g. "Optuna best params bounce window-to-window"
  — informative but no train/val comparison to verdict on.
- **pending** — experiment in flight, no final number yet.

## Cross-arc ranking — primary: Ledoit-Wolf ΔSR vs DCA

**Step 4-5 of [`TODO/ladder-methodology-rewrite.md`](TODO/ladder-methodology-rewrite.md)
restructures this ladder.** The methodology agent's brief
([`.research-walkforward-apples-to-apples.md`](../../../.research-walkforward-apples-to-apples.md))
established that **DSR is a within-arc selection-bias correction,
not a cross-arc rank key** (Bailey-LdP never claimed otherwise). The
literature-canonical cross-arc test is **Ledoit-Wolf (2008)
studentized stationary-bootstrap CI on Sharpe-difference of
date-aligned streams**. That's the new primary column below.

DSR-deflated-t stays in the table as a **within-arc gate** only —
demoted from rank key. It still tells you whether an arc's stream is
distinguishable from `n_trials` coin flips *within its own search*,
but it does NOT directly compare arc A's deflated-t to arc B's.

### Primary: Sharpe-difference vs DCA-canonical (95% Ledoit-Wolf CI)

Computed by `apps/docs/scripts/compute_sharpe_diff_vs_dca.py` →
`Output/sharpe-diff-vs-dca.json`. Each arc's daily/block return
stream is date-aligned to DCA's daily stream (block-aggregated to
match where needed), then `sharpe_difference_ci` runs a 2000-iteration
stationary block bootstrap (Politis-Romano), studentized inversion
per Ledoit-Wolf. `ΔSR` is in annualized Sharpe units.

| arc | n_overlap | **ΔSR ann** | 95% CI lo | 95% CI hi | excludes 0? | alignment |
|---|---:|---:|---:|---:|:---:|---|
| dca-canonical (the reference) | 5232 | +0.000 | +0.000 | +0.000 | — | exact |
| vol-v3-dolthub (c=0bps, optimistic) | 29 | **+1.50** | −0.16 | +3.45 | no | block20 dates |
| vol-v3-dolthub (c=200bps, realistic) | 29 | **+1.17** | −0.38 | +3.06 | no | block20 dates |
| relational analog cross_ticker | 1241 | +0.24 | −0.32 | +0.80 | no | tail |
| gate v0 (excess) | 4680 | +0.21 | −0.38 | +0.82 | no | tail |
| dca-basket-search-winner-4etf | 5232 | +0.17 | −0.39 | +0.73 | no | tail |
| factor 5d LO skip-1 | 936 | −0.32 | −0.96 | +0.29 | no | tail |
| lie shape-kNN Phase-2 (n=57) | 57 | −1.04 | −2.34 | +0.35 | no | tail |
| pairs v0 | 4680 | −0.44 | −1.04 | +0.21 | no | tail |
| low-vol BAB L/S | 249 | −0.66 | −1.29 | +0.06 | no | tail |
| factor LO baseline 20d | 234 | −0.49 | −1.17 | +0.30 | no | tail |
| **momentum 12-1 L/S** | 249 | **−0.74** | **−1.28** | **−0.12** | **YES** | tail |
| **factor LS baseline 20d** | 234 | **−0.84** | **−1.45** | **−0.11** | **YES** | tail |
| **factor 5d LS skip-1** | 936 | **−1.09** | **−1.77** | **−0.44** | **YES** | tail |
| **lie shape-kNN wide (n=94)** | 94 | **−1.62** | **−2.59** | **−0.57** | **YES** | tail |

**Read this row by row.** "Excludes 0" means the 95% CI *does not
contain* ΔSR=0 — i.e., the arc is statistically distinguishable from
DCA at the 5% level. **Zero arcs exclude 0 on the positive side**.
Four arcs exclude 0 on the *negative* side (significantly worse than
DCA). Every "promising" arc — vol-v3, relational, gate, factor LO,
basket-winner — has a CI that **includes 0**, meaning the lift over
DCA is statistically indistinguishable from noise under proper
testing.

This is the cleanest possible answer to "what beats DCA?": **nothing
on the current board does, at the 95% level under literature-canonical
cross-arc methodology**. Vol-v3-DoltHub's apparent +1.17 lift is real
in point estimate but the bootstrap CI is wide because n_overlap=29
is short; the lower bound −0.38 is not credibly above 0.

### Secondary: DSR deflated-t (within-arc gate only — NOT a cross-arc ranking)

Each arc's DSR is computed under the Step-1 corrected `standardize_oos`
(null-noise floor in quadrature; structural-only `sharpe_std_ann=0.072`).
This table is reordered to follow the Ledoit-Wolf primary column above;
deflated-t magnitudes within a row are still meaningful but **DSR
rank between rows is no longer the primary cross-arc test** —
absolute magnitudes depend on each arc's `(n_obs, n_trials)` tuple in
ways that are not directly comparable.

**Methodology Step 1 fix (2026-05-23, null-noise floor).** Per the
ladder-methodology-rewrite migration ([TODO](TODO/ladder-methodology-rewrite.md)),
the 2026-05-22 calibration used `sharpe_std_ann=0.25` (workspace
empirical from 39 factor arms) as the TOTAL cross-trial std. A
synthetic test showed this **double-counts the null estimation
noise**: the 0.245 empirical contained ~0.234 of pure null noise from
those arms' own sample length; the structural-only residual is
√(0.245² − 0.234²) ≈ **0.072 ann**. `standardize_oos` now treats the
supplied `sharpe_std` as the structural-only component and combines
in quadrature with the per-arc null floor `1/√(n_obs−1)`:

    sharpe_std_effective = √( struct² + null_floor² )

`compute_dsr.py` default is now `sharpe_std_ann = 0.072`. Effect:
short-stream arcs (vol-v3 n=33, lie n=60, relational-val n=1241)
deflate more punitively because their null floor dominates;
long-stream arcs (DCA n=5232) are barely affected. **Under this
corrected methodology, DCA-canonical clears the t=+2 bar for the
first time (+2.02).** Vol-v3-DoltHub stays #1 (+4.08, was +5.55) but
its 5+ deflated-t claim was inflated by the prior calibration.

**Within-stratum DSR readings** (cross-stratum DSR-rank is NOT
meaningful per the methodology agent). Each stratum groups arcs with
comparable `(arc class × sample structure × universe)`. Within each
stratum, deflated-t magnitudes are directly comparable; across
strata, look at the Ledoit-Wolf ΔSR column above.

**Stratum A — multi-asset passive baselines** (daily ETF streams,
long history, low search cost):

| arc | n_trials | ann Sharpe | DSR | deflated t |
|---|---:|---:|---:|---:|
| **dca-canonical** (the live strategy) | 4 | +0.692 | **0.978** | **+2.02** |
| dca-basket-search-winner-4etf (Optuna) | 200 | +0.863 | 0.846 | +1.02 |

**Stratum B — short-vol options surface** (block, regime-tailwind
sample, options friction caveat):

| arc | n_trials | commission | ann Sharpe | DSR | deflated t |
|---|---:|---|---:|---:|---:|
| **vol-v3-dolthub-oos (c=200bps, realistic)** | 12 | 200 bps | +2.410 | 1.000 | **+3.43** |
| vol-v3-dolthub-oos (c=0bps, reference) | 12 | 0 bps | +2.822 | 1.000 | +4.08 |
| vol-v3-regime-gated (gauss314 2019-23) | 12 | 0 bps | +1.153 | 0.535 | +0.09 |

**Stratum C — daily-frequency single-name books**:

| arc | n_trials | ann Sharpe | DSR | deflated t |
|---|---:|---:|---:|---:|
| relational analog cross_ticker | 16 | +1.146 | 0.762 | +0.71 |
| pairs v0 | 4 | +0.203 | 0.409 | −0.23 |

**Stratum D — pre-registered single-hypothesis cross-sectional
factors** (n_trials=1 immune to DSR deflation):

| arc | n_trials | ann Sharpe | DSR | deflated t |
|---|---:|---:|---:|---:|
| low-volatility L/S | 1 | +0.128 | 0.738 | +0.64 |
| 12-1 cross-sectional momentum L/S | 1 | +0.081 | 0.656 | +0.40 |

**Stratum E — factor walk-forward** (block frequency, heavy search
cost):

| arc | n_trials | ann Sharpe | DSR | deflated t |
|---|---:|---:|---:|---:|
| factor 5d LO skip-1 (focused n=8) | 8 | +0.357 | 0.505 | +0.01 |
| factor 5d LO skip-1 (conservative n=50) | 50 | +0.357 | 0.148 | −1.05 |
| factor indicator-baseline LO 20d | 50 | +0.192 | 0.070 | −1.48 |
| factor long-short book 20d | 50 | −0.163 | 0.001 | −3.19 |
| factor 5d LS skip-1 (focused n=8) | 8 | −0.452 | 0.000 | −3.72 |
| factor 5d LS skip-1 (conservative n=50) | 50 | −0.452 | 0.000 | −4.63 |

**Stratum F — shape-similarity L/S** (block; universe-specific):

| arc | n_trials | universe | ann Sharpe | DSR | deflated t |
|---|---:|---|---:|---:|---:|
| lie shape-kNN Phase-2 (21 names) | 9 | mega-cap | −0.018 | 0.057 | −1.58 |
| lie shape-kNN wide (factor-narrow 297) | 9 | broad | −0.703 | 0.000 | −3.49 |

**Stratum G — gate / regime overlays**:

| arc | n_trials | mode | ann Sh (excess) | DSR | deflated t |
|---|---:|---|---:|---:|---:|
| gate v0 drawdown overlay | 6 | excess vs EW | −0.100 | 0.037 | −1.79 |

**What the rewritten ladder says.** Under the literature-canonical
**Ledoit-Wolf studentized Sharpe-difference CI** (the primary table
above), **zero arcs beat DCA**. Every arc with a positive ΔSR — vol-v3
(+1.17 to +1.50), relational (+0.24), gate (+0.21), basket-winner
(+0.17) — has a 95% CI that *includes 0*. Four arcs are significantly
*worse* than DCA: momentum-12-1 (CI [−1.28, −0.12]), factor LS
baseline (CI [−1.45, −0.11]), factor 5d LS skip-1 (CI [−1.77, −0.44]),
lie shape-kNN wide (CI [−2.59, −0.57]). **The DCA leg of the
DCA + vol ensemble is the only deployable strategy with statistical
backing on the current ladder.**

The within-stratum DSR readings tell a complementary story. DCA-
canonical clears t=+2 (the only arc to do so on a long-history
stream). Vol-v3 in its realistic-cost form (Stratum B) posts t=+3.43
at n=33 obs — high deflated-t but the underlying overlap-with-DCA
sample is short, which is why the Ledoit-Wolf CI on the same window
spans 0. The factor walk-forward arcs (Stratum E) deflate
catastrophically below 0, reinforcing the cross-sectional null
conclusion documented across the factor app's findings.

**The rewrite changed the headline interpretation, not the
underlying strategies' verdicts.** Each arc's finding page still
documents the same regime-conditional, partial-OOS, or confirmed-null
result. What's gone is the implication that "rank by deflated-t"
identifies the *deployable winner*. The Ledoit-Wolf column says the
deployable winner is *just DCA*, and within strata DSR tells you
which sub-arcs are at-or-near-noise within their own selection
budget.

**For the operator deploying the DCA + vol-v3 ensemble**: the
ensemble's +5.35 deflated-t lift over DCA-overlap (from
[`dca-vol-ensemble-optuna`](findings/dca-vol-ensemble-optuna.md))
sits inside the Ledoit-Wolf CI for vol-v3 alone vs DCA ([−0.38, +3.06]
at c=200bps). The honest read: **the ensemble's incremental value
is plausibly positive but is NOT statistically significant at the
95% level**. Deploying the ensemble vs DCA-alone is a research-
hypothesis-test deployment, not a confirmed alpha lift. The vol-v3
arc's regime-tailwind caveat (calm-bull sample only) compounds this.

**Migration status**: Steps 1-5 of
[`TODO/ladder-methodology-rewrite.md`](TODO/ladder-methodology-rewrite.md)
complete. Step 1 (null-noise floor) shifted the within-arc DSR. Step
2 (realistic options friction) demoted vol-v3 from +4.08 to +3.43.
Step 3 (per-arc-class `sharpe_std`) was data-blocked — Optuna studies
only persist top-10 trials, so we kept the workspace 0.072 default.
Steps 4-5 (Ledoit-Wolf CI primary + stratified ladder) restructured
this entire section.

Beyond row #1, **no other arc clears the conventional t = +2 bar
without the regime-tailwind caveat**. **DCA — the canonical live
strategy — is the cleanest** (deflated t +1.93, DSR 0.97): a modest
+0.69 Sharpe over 5232 daily bars and only 4 trials, just shy of
significance and on the full 20-year span (crises included). Relational
analog (t +1.53) and the original vol v3 (t +1.32) are the next two.
**The headline Sharpes still mislead the other way:** the factor
indicator baseline — recorded `confirmed-OOS` on mean val IC — has a
pooled-stream Sharpe of only +0.19 and deflated t = −1.54 once its
~50-config search is priced in. This is the multiple-testing reality
the [research-strategy note](findings/index.md) flags (few
confirmed-OOS out of many rows), made quantitative.

**Caveats.** (1) The factor row's `confirmed-OOS` verdict was on mean
val *IC*, not stream Sharpe — the DSR does not overturn the IC result,
it adds the deployability-with-deflation read. (2) The vol v3 re-run's
headline (60d-gate fired-α Sharpe −0.66) differs from the original
+2.01 row — a reproduction gap (likely data-snapshot / composition
drift) that itself argues for the DSR's skepticism; the DSR above uses
the 126d deployment-recipe full-panel stream. (3) `n_trials` is a
conservative reconstruction (see the finding); DCA and relational rank
order is robust to ±50% on their trial counts, vol/factor are already
below the bar at any plausible count.

**Rows not on this ladder are `DSR N/A` by construction**, in two
classes: *meta-evaluations* that compose scalar per-window Sharpes (the
cfr macro-gate, all oracle arms, sizing/overlay diagnostics, regime
Optuna best-params) — no return stream exists to deflate; and
*non-portfolio diagnostics* (replay reconstruction R², macro-regime
Pearson r, compression error) — Sharpe undefined. A deflated Sharpe is
*defined* only on a return stream.

## Master table

Date is the row's recording date (when the result was finalised), not
the experiment's first kickoff. App is the directory under `apps/`.
Metric is what train / val numbers refer to.

| date | app | experiment | universe | windowing | metric | train | val | delta (val − train) | verdict | artifact |
|---|---|---|---|---|---|---:|---:|---:|---|---|
| pre-2026-04 | regime    | Optuna walk-forward over (lookback, n_tail, top_n, divergence) | regime-2010-24 | regime-3w-optuna | val Sharpe (best params/window) | not reported | later windows ~+1.36..+1.63 | n/a | diagnostic | best-params drift wide; signal unstable across windows |
| pre-2026-04 | regime    | JAX-Adam differentiable trainer, single 70/30 split (impl since deleted) | regime-2010-24 | 70/30 single | Sharpe | +1.22 | +0.80 | −0.42 | confirmed-OOS | scale weights collapsed to long horizons (126d 48%); impl [removed](https://github.com/sughodke/StockSurvey/commit/68ee595) with [JAX dep drop](https://github.com/sughodke/StockSurvey/commit/191d787) |
| 2026-04-30 | regime    | Log-returns CWT input vs raw close (controlled, kernel_half_extent=3 both arms) | regime-2010-24 | regime-3w-optuna | val Sharpe (median / mean / worst) | n/a | raw close: +0.15 / +0.07 / −0.41 ; log-returns: +0.03 / −0.29 / −1.06 | raw close beats in 3/3 windows | confirmed-OOS | `Output/regime-eval-{rawclose-kernel3,logreturns}.{log,json}` |
| 2026-04-30 | factor    | Deterministic-indicator baseline (linear head) | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | n/a | +0.0120 ; +0.440 | 5/6 windows positive | confirmed-OOS | `Output/walkforward-{linear,mlp}-s200-wd0.001-windows.npz` |
| 2026-04-30 | factor    | Deterministic-indicator baseline (MLP head) | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | (3× linear train IC) | +0.0081 ; mean val Sh +0.457 [backfill 2026-05-18 ← walkforward-mlp-s200-wd0.001-windows.npz] | 4/6 windows positive | partial-OOS | mlp triples train IC over linear but lower val IC + 2 negative windows — overfitting |
| 2026-05-04 | factor    | sign_demeaned target reduction probe | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | n/a | +0.0088 vs +0.0120 control (Δ = −0.0032) ; Sh probe +0.379 vs control +0.440 [backfill 2026-05-18 ← forecast-probe-{probe-sign,control}-windows.npz] | 5/6 windows degrade | reversed-OOS | falsified; `Output/forecast-probe-{control,probe}-windows.npz` |
| 2026-05-04 | factor    | vol_innovation forecast target | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | range 0.435..0.536 | +0.4743 ; +0.515 | 6/6 windows positive | confirmed-OOS (for vol IC) | real signal but only +0.075 Sharpe lift; `Output/forecast-probe-probe-vol-windows.npz` |
| 2026-05-04 | factor    | Vol-target overlay (fcst-VT vs trail-VT vs EW)  | factor-narrow | 6-window factor | mean val Sharpe | n/a | EW +0.215, trail-VT +0.231, fcst-VT +0.232 | fcst − trail = +0.001 | confirmed-null | forecast adds nothing over trailing vol for sizing; `Output/vol-overlay-{summary,windows}.{json,npz}` |
| 2026-05-04 | factor    | Pure CWT bundle vs IndicatorGridConfig at matched setup | factor-narrow | 6-window factor (K=1) | mean val IC (return / vol) ; mean val Sharpe | n/a | cwt-return +0.0091, indicator-return +0.0120 ; cwt-vol +0.2165, indicator-vol +0.4743 ; Sh cwt-return +0.461 / indicator-return +0.44 ; cwt-vol +0.453 / indicator-vol +0.515 [backfill 2026-05-18 ← cwt-bundle-*-windows.npz + summary.json] | tied at noise on returns; indicators 2× CWT on vol | confirmed-null (returns) / partial-OOS (vol) | `Output/cwt-bundle-{summary.json,*-windows.npz}` |
| 2026-05-04 | factor    | Feature augmentation: vol forecast as 75th feature for return head | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | n/a | aug +0.0117 vs base +0.0120 (Δ −0.0004); +0.442 vs +0.446 | head L1-share on forecast feature = 0.014 (uniform) | confirmed-null | `Output/feature-aug-summary.json` |
| 2026-05-04 | factor    | Horizon pivot rebal=63d (quarterly) | factor-narrow | 6-window factor (q) | mean val IC (return / vol) | n/a | q-return −0.0019 vs 20d-return +0.0120 ; q-vol +0.3439 vs 20d-vol +0.4743 | quarterly worse on both | reversed-OOS | quarterly Sharpe rises mechanically from less commission, not skill; `Output/horizon-pivot-*.{json,npz}` |
| 2026-05-06 | factor    | Universe pivot (factor-narrow → factor-wide) | factor-wide | 6-window factor | mean val IC (return / vol) ; mean val Sharpe | n/a | wide-return +0.0106 ; wide-vol +0.4091 ; Sh wide-return +0.205 ; wide-vol +0.366 [backfill 2026-05-18 ← universe-pivot-wide-{return,vol}-windows.npz] | tied / slightly worse vs narrow | confirmed-null | 7× universe doesn't lift the +0.012 return ceiling; `Output/universe-pivot-{summary.json, *-windows.npz, close.pkl}` |
| 2026-05-06 | factor    | Regime gate via aggregate forecast vol (gate-70/80/90) | factor-narrow | 6-window factor | mean val Sharpe | n/a | always-on +0.215, gate-70 +0.015, gate-80 +0.195, gate-90 +0.240 | gate-90 +0.025 over always-on (within ±0.17 noise) | confirmed-null | vol forecast doesn't gate return signal; `Output/regime-gate-{summary.json,windows.npz}` |
| 2026-05-07 | replay    | 2D Haar DWT-L1 keep-LL CWT-tile compression vs uncompressed (cwt-only bundle) | stooq_us_long (295-ticker pool, AAPL primary, NVDA val) | single train+val ticker | NVDA val R² (rsi / cci / vol / macd) ; Sharpe N/A | not reported | rsi 0.576 vs 0.582 ; cci 0.610 vs 0.603 ; vol −0.30 vs −0.38 ; macd both broken ; Sharpe N/A [non-portfolio: SSL reconstruction R²] | rsi −0.006 ; cci +0.007 ; vol +0.084 ; macd N/A | partial-OOS | SSL reconstruction R², not a portfolio metric; CSCO zero-shot peaks (RSI 0.92 vs 0.90 ; CCI 0.89 vs 0.85) marginally favor compressed; MACD head broken in both arms (orthogonal bug); `Output/cwtonly-{,dwtL1-}*` |
| 2026-05-07 | relational | Analog-kNN DWT-L1 vs uncompressed fingerprint | phase-2 | phase-2 split | Daily Sharpe | dwt-L1 1.116 vs baseline 1.032 (Δ +0.084) | dwt-L1 1.099 vs baseline 1.146 (Δ −0.046) | val edge reverses sign | **reversed-OOS** | full-period in-sample +0.04 Sharpe was driven entirely by 2013-2020 sub-period; do NOT pin compress_levels=1 on canonical analog checkpoint; `Output/relational-idea-b-analog-knn-dwt-walkforward-{equity.png,stats.txt}` |
| 2026-05-08 | relational | 8-arm distance-scorer × ±DWT-L1 — **analog cross_ticker baseline** | phase-2 | phase-2 split | Daily Sharpe | 1.032 | 1.146 | **+0.114** | **confirmed-OOS** | only arm whose val *exceeds* train; this is the canonical `Output/relational-analog.json`; `Output/relational-dwt-phase2-walkforward.{csv,txt}` |
| 2026-05-08 | relational | 8-arm — analog cross_ticker DWT-L1 (supersedes 2026-05-07 single-arm row above) | phase-2 | phase-2 split | Daily Sharpe | 1.116 | 1.099 | −0.016 | reversed-OOS | confirms prior single-arm walkforward; do NOT pin compress_levels=1 on canonical analog |
| 2026-05-08 | relational | 8-arm — analog **per_ticker** baseline | phase-2 | phase-2 split | Daily Sharpe | 1.305 | 0.824 | **−0.482** | reversed-OOS, severe | per_ticker pool gives best in-sample Sharpe (1.305) of any arm but loses 0.48 OOS; not shippable |
| 2026-05-08 | relational | 8-arm — analog per_ticker DWT-L1 | phase-2 | phase-2 split | Daily Sharpe | 1.320 | 0.876 | **−0.444** | reversed-OOS, severe | DWT-L1 marginally rescues per_ticker val (+0.052 over uncompressed pt), but absolute level is still 0.27 below uncompressed cross_ticker val |
| 2026-05-08 | relational | 8-arm — farthest baseline | phase-2 | phase-2 split | Daily Sharpe | 1.321 | 0.828 | **−0.493** | reversed-OOS, catastrophic | most extreme train→val gap of any arm; train-only artifact |
| 2026-05-08 | relational | 8-arm — farthest DWT-L1 | phase-2 | phase-2 split | Daily Sharpe | 1.102 | 0.833 | −0.269 | reversed-OOS but smaller | DWT compresses train Sharpe back toward val level; val Sharpe nearly identical to uncompressed (0.833 vs 0.828) — DWT doesn't add OOS skill, just reduces in-sample inflation |
| 2026-05-08 | relational | 8-arm — diversified baseline | phase-2 | phase-2 split | Daily Sharpe | 1.222 | 1.002 | −0.220 | partial-OOS | only non-cross_ticker-analog arm with val Sharpe > 1.0; better than DWT version |
| 2026-05-08 | relational | 8-arm — diversified DWT-L1 | phase-2 | phase-2 split | Daily Sharpe | 1.245 | 0.832 | **−0.413** | reversed-OOS | strictly worse than uncompressed diversified on val (0.832 vs 1.002); clearest "compression hurts OOS" signal in the table |
| 2026-05-09 | relational | analog cross_ticker — universe-shift validation (ex-Phase-2; same algo, wider universe) | factor-narrow-ish (296 stooq_us_long names ex-PHASE2_TICKERS) | phase-2 split | Daily Sharpe | 0.615 | 0.484 | **−0.131** | reversed-OOS — **also collapses vs Phase-2 baseline** | val Sharpe 0.484 vs Phase-2 cross_ticker val 1.146 (Δ −0.66) directly confirms the macro-tailwind concern: ~0.5-0.7 of Phase-2's val edge was mega-cap-specific behavior, not generalizable cross-sectional skill. MaxDD also worse (-39% vs -31%). Algorithm: `analog_knn_scores_fast(n_workers=24)` mp.Pool over t-axis, OPENBLAS_NUM_THREADS=1 per worker (12 min wall on Modal cpu=8 vs 2-4h serial estimate). Universe is mid/large-cap-survivor names, not true small caps. Artifacts: `Output/relational-exmegacap-{equity.png,stats.txt,walkforward.csv}`. Repro: `uvx modal run apps/relational/scripts/modal/relational_exmegacap_modal.py` (after `prep_exmegacap_prices.py`). |
| 2026-05-10 | factor    | f32→f64 precision fix in `forward_log_returns` (linear walkforward verification post-`3002e8d` regression) | stooq_us_long (297 tickers) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val IC ; mean val Sharpe | n/a | f32 +0.0005 (regressed) → f64 +0.0031 (restored) ; Sh f32 +0.409 ≡ f64 +0.409 (Sharpe path unaffected by the f32 IC-numerator cancellation) [backfill 2026-05-18 ← ssl-walkforward-linear-s200-wd0.001-windows-{f32,f64}.npz] | restoration: +0.0026 vs regressed | [`diagnostic`](#verdict-labels) | `pearson_rank_ic` numerator catastrophic cancellation in f32 arithmetic at the +0.003 IC magnitude where SSL signal sits; fix at [`9209fa9`](https://github.com/sughodke/StockSurvey/commit/9209fa9) keeps f64 internal, casts to f32 only at Tensor boundary; reproduces prior 2026-05-09 doc bit-for-bit (per-window: −0.000, +0.001, +0.003, −0.002, +0.010, +0.006); also retroactively fills the missing leaderboard entry for the 2026-05-09 SSL walkforward baseline. Mechanism + verification in [`factor-f32-precision-cancellation`](findings/factor-f32-precision-cancellation.md). Artifacts: `Output/ssl-walkforward-linear-s200-wd0.001-windows{,-f32,-f64}.npz`. |
| 2026-05-10 | factor    | Multi-task aux head (`mlp_multitask`, `aux_weight=0.1`) — winsorized z-score MSE alongside rank-IC | stooq_us_long (297 tickers) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val IC ; mean val Sharpe | +0.762 | linear +0.0031 ; mlp −0.0120 ; **mlp_multitask +0.0001** ; Sh linear +0.409 ; mlp −0.282 ; mlp_multitask +0.444 [backfill 2026-05-18 ← ssl-walkforward-{linear,mlp,mlp_multitask-aux0.1}-windows.npz] | mt vs mlp: **+0.0121** ; mt vs linear: **−0.0030** ; mt vs indicator baseline: **−0.0119** | partial-OOS for the `mt > mlp` claim, [`confirmed-null`](#verdict-labels) for the indicator-ceiling claim | shared trunk + 2 parallel scalar heads (Wp primary rank-IC, Wa aux winsorized z-score MSE). 5/6 windows lift mt over mlp; 1 window (w5) regresses. Train IC drops 0.825→0.762 — clean regularization signature. **Aux head itself does not learn**: val_aux MSE pinned at 1.0–1.2 across every window (z-scored target var = 1.0, so ≈ random). Lift mechanism is gradient regularization through the trunk, not aux magnitude extraction. Linear-on-encoder still beats encoder + mlp + multitask. Indicator baseline (+0.0120) still beats every encoder arm. Run on f64-corrected baseline (commit [`9209fa9`](https://github.com/sughodke/StockSurvey/commit/9209fa9)); linear and mlp baselines reproduce the doc bit-for-bit. Mechanism + per-window data in [`factor-multitask-aux-head`](findings/factor-multitask-aux-head.md). Follow-up `aux_weight ∈ {1.0, 10.0}` sweep resolved in [`factor-multitask-aux-weight-sweep`](findings/factor-multitask-aux-weight-sweep.md). Artifacts: `Output/ssl-walkforward-{linear,mlp,mlp_multitask-aux0.1}-s200-wd0.001-windows.npz` + `summary.json` + `comparison.png`. Wall: linear 63s, mlp 75s, mlp_multitask 1208s. |
| 2026-05-10 | relational | **Passive equal-weight benchmark — Phase-2** (regrades 2026-05-08 `confirmed-OOS` analog cross_ticker row) | phase-2 | phase-2 split | val Sharpe (BH / EW-rebal20-10bps) | passive train BH +1.431 ; EW +1.307 | BH **+1.079** ; EW **+1.066** | n/a (passive baseline) | [`diagnostic`](#verdict-labels) — **regrades model row to alpha-zero** | leaderboard's pre-test threshold ("if Phase-2 EW ≥ 1.0, model adds ~0.15 alpha") confirmed at 1.07 → analog cross_ticker val 1.146 has **alpha +0.067 vs passive** (≈ 6 bps Sharpe), within single-split noise. **Train-side passive BH +1.431 vs model train 1.032 — model also underperformed passive in-sample**; the "+0.114 train→val Δ" was movement up the underperformance curve. The canonical `Output/relational-analog.json` is essentially a high-fee mega-cap index fund. Artifacts: `Output/equal-weight-benchmark.json`. Script: `apps/relational/scripts/equal_weight_benchmark.py`. Finding: [`passive-ew-benchmark`](findings/passive-ew-benchmark.md). |
| 2026-05-10 | relational | **Passive equal-weight benchmark — stooq_us_long** (regrades 2026-05-09 `confirmed-OOS` Morlet wide-universe row) | stooq_us_long | phase-2 split | val Sharpe (BH / EW-rebal20-10bps) | passive train BH +0.878 ; EW +0.838 | BH **+0.850** ; EW **+0.851** | n/a (passive baseline) | [`diagnostic`](#verdict-labels) — **regrades both Morlet and Ricker arms to alpha-negative** | the +0.17 Morlet-over-Ricker lift in [`relational-morlet-failure`](findings/relational-morlet-failure.md) was movement within an alpha-negative regime: Morlet val 0.717 has **alpha −0.133** vs passive 0.850 ; Ricker val 0.547 has **alpha −0.304**. Both underperform passive on this universe — the bundle migration moved a losing strategy slightly less far in the wrong direction. Cleanest "wide-universe win" in the leaderboard becomes a less-bad-loss. Artifacts: `Output/equal-weight-benchmark.json`. Script: `apps/relational/scripts/equal_weight_benchmark.py`. Finding: [`passive-ew-benchmark`](findings/passive-ew-benchmark.md). |
| 2026-05-10 | relational | **Passive equal-weight benchmark — ex-Phase-2** (regrades 2026-05-09 `reversed-OOS` universe-shift row) | factor-narrow-ish (296 stooq_us_long ex-Phase-2) | phase-2 split | val Sharpe (BH / EW-rebal20-10bps) | passive train BH +0.833 ; EW +0.821 | BH **+0.818** ; EW **+0.832** | n/a (passive baseline) | [`diagnostic`](#verdict-labels) — **catastrophic alpha confirmed** | leaderboard's pre-test threshold ("if ex-Phase-2 EW ≥ 0.6, model is negative alpha") confirmed catastrophically: passive 0.82, model 0.484, **alpha −0.334**. Three rows of relational `reversed-OOS` 8-arm evidence (farthest / diversified / per_ticker) compound — the entire kNN-on-CWT-fingerprint strategy class destroys ~1/3 of a Sharpe unit vs passive holding off mega-caps. Artifacts: `Output/equal-weight-benchmark.json`. Script: `apps/relational/scripts/equal_weight_benchmark.py`. Finding: [`passive-ew-benchmark`](findings/passive-ew-benchmark.md). |
| 2026-05-10 | factor    | Multi-task aux head (`mlp_multitask`, **`aux_weight=1.0`**) — `aux_weight` sweep arm | stooq_us_long (297 tickers) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val IC ; aux MSE (train→val) ; mean val Sharpe | +0.618 ; aux MSE 0.778 | **−0.0084** ; aux MSE **1.159** ; mean val Sh +0.434 [backfill 2026-05-18 ← ssl-walkforward-mlp_multitask-s200-wd0.001-aux1-windows.npz] | val IC vs aux=0.1: **−0.0085** ; aux MSE crosses 1.0 in the wrong direction | [`confirmed-null`](#verdict-labels) for magnitude-extraction; supersedes the "regularizer-only" reading from the aux=0.1 row | aux head fits train magnitude (train MSE 0.78) but predictions reverse sign on val (val MSE 1.16, *worse than predicting zero on a unit-variance target*). Joint loss drags primary val IC down to −0.0084 (1/6 windows positive). Cross-sectional winsorized z-score of forward 20-day log returns is **regime-non-stationary** — the magnitude leaders flip between train and val windows, so any magnitude-aware aux objective is anti-predictive OOS. Doesn't fit any of the 3 pre-registered branches; 4th branch added. Full sweep retrospective in [`factor-multitask-aux-weight-sweep`](findings/factor-multitask-aux-weight-sweep.md); per-window data in [`factor-multitask-aux-head`](findings/factor-multitask-aux-head.md#aux_weight-sweep-2026-05-10). Artifacts: `Output/ssl-walkforward-mlp_multitask-s200-wd0.001-aux1-windows.npz`, `Output/ssl-walkforward-summary-aux1.json`. Wall: 1210s. |
| 2026-05-10 | factor    | Multi-task aux head (`mlp_multitask`, **`aux_weight=10.0`**) — `aux_weight` sweep arm | stooq_us_long (297 tickers) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val IC ; pos-val frac ; mean val Sharpe | +0.140 (mean) ; **3/6 windows trunk fully collapsed** (tr_ic = 0.000) | **+0.0048** ; pos frac 0.50 (artefactual) ; mean val Sh +0.412 [backfill 2026-05-18 ← ssl-walkforward-mlp_multitask-s200-wd0.001-aux10-windows.npz] | val IC vs aux=0.1: +0.0047 ; mean entirely carried by w3 (+0.0254) | [`confirmed-null`](#verdict-labels) — trunk collapse contaminates the surface-level "lift" | At 100× the aux=0.1 gain the aux gradient destabilizes joint optimization: 3/6 windows (0, 2, 5) hit exact tr_ic = 0.000 (trunk projections zero out, output collapses to constant). Surviving 3/6 windows show train aux MSE 0.89–0.98 and val aux MSE 1.01–1.06 — same overfit-and-reverse pattern as aux=1.0 in miniature. The +0.0048 mean is window 3's +0.0254 alone. Headline pos-val-IC frac of 0.50 is misleading (3 zero-collapsed windows ≠ "neutral"). Full sweep retrospective in [`factor-multitask-aux-weight-sweep`](findings/factor-multitask-aux-weight-sweep.md); per-window data in [`factor-multitask-aux-head`](findings/factor-multitask-aux-head.md#aux_weight-sweep-2026-05-10). Artifacts: `Output/ssl-walkforward-mlp_multitask-s200-wd0.001-aux10-windows.npz`, `Output/ssl-walkforward-summary-aux10.json`. Wall: 1204s. |
| 2026-05-10 | relational | **Passive equal-weight benchmark — factor-wide-ish** (extends 2026-05-10 EW arc to broader universe) | factor-wide-ish (2162 tickers, full archive `min_history=3500`, `start_grace_days=3650`) | phase-2 split | val Sharpe (BH / EW-rebal20-10bps) | passive train BH +0.771 ; EW +0.804 | BH **+0.681** ; EW **+0.674** | n/a (passive baseline) | [`diagnostic`](#verdict-labels) — **establishes monotonic passive-Sharpe decay with universe breadth** | Completes the 4-universe sweep: passive val Sharpe drops monotonically as universe broadens (Phase-2 1.08 → stooq_us_long 0.85 → ex-Phase-2 0.82 → factor-wide 0.68). Mega-cap concentration carried the 2021-2025 passive Sharpe; broader universes dilute it. **No model arm has been run on factor-wide for relational** (factor-side `universe-pivot` 2026-05-06 row showed factor-wide IC tied with factor-narrow → broadening doesn't lift IC, but separate model class). The model row would need val Sharpe ≥ 0.68 to be alpha-positive here, **down from 0.85 on stooq_us_long** — broader universes lower the EW bar at the cost of higher per-name dispersion. Caveat: ffill convention treats post-delisting bars as flat-at-last-price, optimistic for passive Sharpe by ~0.05-0.15 depending on delisting rate; bias affects all universes equally so per-universe deltas are fine. Artifacts: `Output/equal-weight-benchmark.json`. Source pickle: `Output/universe_pivot_close.pkl` (built via `apps/factor/scripts/modal/prep_universe_pivot_data.py`). Finding: [`passive-ew-benchmark`](findings/passive-ew-benchmark.md#factor-wide-extension). |
| 2026-05-10 | factor    | **Long-short market-neutral constructor** on rank-IC linear head — closes the [`factor-rankic-long-only-mismatch`](findings/factor-rankic-long-only-mismatch.md) diagnostic | factor-narrow (297 stooq_us_long names, `min_history_bars=6500`) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val Sharpe (long-only / long-short) ; mean val IC | mean train IC +0.119 | LO **+0.278** ; LS **−0.067** ; mean val IC +0.0055 | LS alpha vs LO: **−0.345** ; pos-LS-windows **2/6** | [`confirmed-null`](#verdict-labels) — falsifies the discarded-short-signal hypothesis | Constructor: per-bar z-score → clip ±3σ → re-demean → L1-normalize so `sum(w)=0` and `sum(|w|)=1`; cost `commission_frac × L1(Δw)` (no 0.5 factor — LS L1 is one-sided). Both arms eval'd on the same head trained on rank-IC, n_steps=200 lr=1e-2 wd=1e-3 commission_bps=10. Pre-registered cuts in [`TODO/long-short-constructor`](TODO/long-short-constructor.md): pass at LS ≥ +0.20 with ≥4/6 positive — fail at < +0.10 *or* ≤2/6. Result hits both fail triggers. Per-window: w0 LO −0.99 LS −0.34 (LS rescues a bad LO); w1 LO **+0.86** LS −0.38 (LS destroys a good LO — cleanest evidence the LO win was market beta, not bottom-tail skill); w2 LO +0.73 LS +0.27 (LS dilutes); w3-5 LS uniformly worse or tied (+0.06 best). No window where LS materially exceeds LO. Mean train IC +0.119 / val IC +0.0055 puts the head's residual signal below the friction floor (10 bps × 2× LS turnover ≈ 400 bps annualized) — long-short can't help if neither tail clears noise. Implication per `confirmed-null` next-move rule: stop testing variations of cross-sectional return prediction; pivot to [`different-prediction-problem`](TODO/different-prediction-problem.md). Artifacts: `Output/long-short-eval-{windows.npz,summary.json}`. Driver: `apps/factor/scripts/long_short_eval.py`. Implementation: `factor.objectives.{long_short_weights, block_sharpe_long_short}`; per-window column `WalkForwardWindow.{train,val}_sharpe_long_short`. |
| 2026-05-10 | factor    | **Loss-pivot — `block_sharpe` as training loss** (Sharpe-aligned head, same softmax-LO constructor as eval) | factor-narrow (297 stooq_us_long names, `min_history_bars=6500`) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val Sharpe (LO) ; mean val IC ; mean val IR vs EW ; mean logT | n/a (per-window train metrics in finding) | val Sh LO **−0.097** ; val IC +0.0066 ; val IR **−0.415** ; logT **−1.60** | val Sh delta vs rank-IC: **−0.375** | [`confirmed-null`](#verdict-labels) — Sharpe-as-loss *destroys* val Sharpe in the low-IC regime | Same head/universe/windows/n_steps/lr/wd as the rank-IC baseline (mean val Sh +0.278); the only change is the training loss. `block_sharpe` from `factor.objectives` (already differentiable, used at eval) wired as `loss = -block_sharpe(scores, log_temp, blr, mask, rebal_days, commission_frac)` with log_temperature added to AdamW. Mechanism: temperature trains down to **~0.2** (near-argmax concentration); train IC collapses 5× from 0.115 → 0.022 (head is no longer fitting cross-sectional ordering, just betting on top picks). At IC=+0.005 the concentrated bets amplify variance without amplifying mean → val Sharpe drops by 0.375. Validates that **rank-IC's scale-invariance was acting as inadvertent shrinkage toward EW**, which is the right risk control in low-IC regimes — the user's earlier "rank-IC is wasteful" intuition was wrong in this regime. Implementation: `loss_kind='block_sharpe'` arg added to `train_scorer_walkforward`; trainable `log_temperature` shape `(1,)` for AdamW shape-match. Driver: `apps/factor/scripts/loss_pivot_eval.py`. Artifacts: `Output/loss-pivot-block_sharpe-windows.npz`, `Output/loss-pivot-eval-summary.json`. Finding: [`factor-loss-pivot`](findings/factor-loss-pivot.md). |
| 2026-05-10 | factor    | **Loss-pivot — `block_ir_vs_ew` as training loss** (IR-vs-EW-aligned head, same softmax-LO constructor) | factor-narrow (297 stooq_us_long names, `min_history_bars=6500`) | 6-window factor (63/39/39 blocks @ 20-day rebal) | mean val Sharpe (LO) ; mean val IR vs EW ; mean val IC ; mean logT | n/a | val Sh LO **−0.109** ; val IR **−0.394** ; val IC +0.0006 ; logT **−1.50** | val Sh delta vs rank-IC: **−0.387** ; val IR delta vs rank-IC: **+0.092** (still negative) | [`confirmed-null`](#verdict-labels) — IR-aligned loss can't push val IR positive either | New `block_ir_vs_ew` in `factor.objectives` — per-bar active return = (after-cost portfolio block return) − (pre-cost EW block return), then Sharpe of that active series annualized. Same softmax-LO constructor, same temperature treatment as `block_sharpe` arm. Result: val IR moves from rank-IC's −0.486 to ir_vs_ew's −0.394 (+0.092 lift, materially less than rank-IC → block_sharpe's +0.071) — the IR-aligned loss did improve IR by the smallest margin, but couldn't push it above zero. Val Sharpe drops 0.387 vs rank-IC for the same concentration mechanism documented in the block_sharpe row. Three independent tests now converge on the same diagnosis: at +0.005 to +0.012 cross-sectional IC, no portfolio constructor or training loss produces a head that clears EW after costs. The binding constraint is signal magnitude. Per `confirmed-null` rule, pivot to [`different-prediction-problem`](TODO/different-prediction-problem.md). Driver: `apps/factor/scripts/loss_pivot_eval.py`. Artifacts: `Output/loss-pivot-ir_vs_ew-windows.npz`, `Output/loss-pivot-eval-summary.json`. Finding: [`factor-loss-pivot`](findings/factor-loss-pivot.md). |
| 2026-05-10 | gate      | **Drawdown-gate v0** — predict EW universe forward 20d max DD, gate exposure (binary, train-pred 95th-quantile threshold) | stooq_us_long (312 tickers) | 6-window gate (1260-train / 780-val / 780-step daily bars over 2000-2025) | mean val Sharpe (gated vs unconditional EW) ; mean val Pearson r ; pos-alpha-windows | trained on each window; train R² 0.05–0.50 | mean unc EW Sh **+0.773** ; mean gated Sh **+0.840** ; **mean val Pearson r +0.264** | **mean alpha +0.067 Sharpe ; 4/6 positive windows** | [`partial-OOS`](#verdict-labels) — real Pearson signal, marginal Sharpe lift in noise band | First test of the prediction-problem pivot — orthogonal alpha source from cross-sectional return prediction. Mean val Pearson r = +0.264 is **~25-50× larger** than apps/factor's +0.005 to +0.012 IC ceiling, validating that drawdown forecasting is a genuinely different prediction class (not bottlenecked by the same signal-magnitude problem). But gate monetization is fragile at v0: window 1 (2008 GFC) carries +0.32 alpha by flatting to 51% exposure; calm windows accumulate small false-positive losses (−0.02 to −0.07 each) that nearly cancel the GFC win. Mean alpha within ±0.10 noise band. Threshold sensitivity (binary gate): q=0.85 mean alpha **−0.114**, q=0.90 **−0.009**, q=0.95 **+0.067** — strictly monotone, signal lives only in extreme tail of predicted DD. Sigmoid mode at q=0.95: mean alpha +0.059 with 2/6 pos (catches +0.46 in window 1, but graduated exposure changes in calm periods net negative). Mechanism: OLS linear model has skill on tail drawdowns but poor magnitude calibration; can't extrapolate across regime shifts (windows 2-5 train on calmer periods than they validate against). Costs not modeled in v0 (10-40 flips per val window @ 10 bps would shave 0.13-0.5%/y). Per `partial-OOS` rule, stratify by regime — visible in per-window data (works in high-vol windows 1, 4; flat in low-vol 2, 3, 5). v1 follow-ups: regime-conditional gate, two-stage classifier + sizing, cross-sectional dispersion features, MLP head. Parked at v0 — running [`apps/pairs`](TODO/apps-pairs.md) next per "tackle all three" plan. App scaffold: `apps/gate/{src/gate, scripts/run_baseline.py, scripts/run_walkforward.py}` (~600 LoC). Driver: `uv run python apps/gate/scripts/run_walkforward.py --threshold-quantile 0.95`. Artifacts: `Output/gate-walkforward-summary.json`. Finding: [`gate-drawdown-v0`](findings/gate-drawdown-v0.md). |
| 2026-05-10 | pairs     | **Pairs classical v0** — Engle-Granger cointegration screening + classical z-score state machine (entry ±2σ, exit ±0.5σ, 10bps × 2 commission) | factor-narrow (297 stooq_us_long names, `min_history_bars=6500`) | 6-window pairs (1260-train / 780-val / 780-step daily bars over 2000-2025) | mean agg val Sharpe ; mean pair Sharpe ; pos-window-frac | per-window EG-passing rates 2249–4755 (regime indicator) | **mean agg val Sharpe +0.099** ; mean pair Sh +0.115 ; pos-window-frac **4/6** | n/a (LS by construction; benchmark is zero, not EW) | [`confirmed-null`](#verdict-labels) per pre-registered cuts (mean < +0.20 fail floor) | Second test of the prediction-problem pivot. Per-window pipeline: 297 tickers → 44k pair candidates → \|corr\|≥0.7 prefilter → 10-25k → EG p<0.05 → 2-5k → top-50 by p-value. Trade rule: classical state machine, lag-1 position, 2-leg commission. Per-window agg Sharpe: w0 **−1.233** (dot-com pairs deployed into 2005-2007 bull mkt, catastrophic), w1 **+0.870** (GFC sweet spot, 0.82 pos pair frac), w2 +0.593, w3 +0.392, w4 +0.080, w5 −0.109. Mean dragged to +0.099 by window 0 outlier; mean ex-w0 = **+0.365** (would be marginal-OOS if dropped, but no rule for ex-post outlier removal). 4/6 windows positive — directional consistency real but Sharpe magnitude below threshold. Regime-conditional reading: works in 2008-2017 mean-reverting markets, fails in 2005-2007 + 2020-2023 trending bull markets. EG-passing-rate per window itself is a regime indicator (high in mean-reverting, low in trending) — natural v2 gate. Implementation hot path: `coint(maxlag=1)` skips BIC lag-selection inner loop (~17× speedup, 500ms→30ms per pair). Three confirmed-nulls now stack: passive-EW gate, long-short constructor, loss-pivot, plus this pairs result + the partial-OOS gate-drawdown. v1 parked, pivoting to [`apps/vol`](TODO/apps-vol.md) per "tackle all three." App scaffold: `apps/pairs/` ~700 LoC. Driver: `uv run python apps/pairs/scripts/run_walkforward.py`. Artifacts: `Output/pairs-walkforward-summary.json`. Finding: [`pairs-classical-v0`](findings/pairs-classical-v0.md). |
| 2026-05-11 | gate      | **Gate v1a — macro features in predictor stack** (`run_walkforward.py --with-macro`) | stooq_us_long EW (312 tickers, daily 2003-2025; TIPS span constraint shifts start 3y vs v0) | 5-window gate (1260-train / 780-val / 780-step) | mean val Pearson r ; mean alpha (gated vs unc EW) ; pos-alpha-windows | n/a (5 windows fitted per-window) | mean val r **+0.012** ; mean alpha **−0.086** ; pos windows **1/5** | vs v0: val r −0.252, alpha **−0.153**, pos-frac 4/6 → 1/5 | [`confirmed-null`](#verdict-labels) — macro as direct features WORSE than baseline | Tests v1 plan from macro-regime-diagnostic. 5 macro features (`fed_funds, credit_baa, m2_yoy, real_yield_10y, vix`; dropped `slope_10y_3m` per diagnostic noise verdict) concat'd to gate's existing 10-feature aggregate stack. **COVID window kills it**: val 2020-05 → 2023-06, gated Sh +0.263 vs unc +1.045, alpha **−0.782**. Predictor trained on 2015-2020 Goldilocks saw macro features in tight range; in val (Fed 0→5%, M2 25%→−1%, war), values hit out-of-distribution territory and the model overfit to "VIX high → expect DD" fired during COVID *recovery* rather than crash. Mechanism: macro features have non-stationary distributions across train/val regimes (unlike scale-invariant aggregate vol/return features). Conclusion: don't add macro as direct predictor inputs. Driver: `apps/gate/scripts/run_walkforward.py --with-macro --threshold-quantile 0.95`. Artifacts: `Output/gate-walkforward-summary-with-macro.json`. Finding: [`macro-regime-diagnostic`](findings/macro-regime-diagnostic.md#v1-results-2026-05-11). |
| 2026-05-11 | macro     | **Macro v1b — binary VIX meta-gate** (`macro_meta_gate_eval.py`) — retroactively suspend deployment when VIX < 1y rolling median at val_start | All 17 prediction-problem-pivot windows (6 gate + 6 pairs + 5 vol) | meta-gate retroactive across pre-existing v0 walk-forward results | pooled mean alpha (raw + per-app-z-scored) ; per-app lift ; Sharpe N/A | raw mean +0.085 across 17 windows | meta-gated raw mean **+0.075** (lift −0.010); **per-app-z-scored lift +0.215** ; per-app: pairs +0.028, gate −0.008, vol −0.058 ; Sharpe N/A [meta-gate: reports per-app α-lift, not a standalone portfolio Sharpe] | 5 windows kept (high-VIX); 12 suspended (low-VIX) | [`partial-OOS`](#verdict-labels) at risk-adjusted level, [`inconclusive`](#verdict-labels) at raw alpha | Tests the alternative integration path from macro-regime-diagnostic v1 plan: macro state as deploy/suspend gate at the meta-level (above the apps' per-bar predictors), not as direct features. Pairs benefits most because catastrophic w0 (−1.23 alpha) gets suspended (VIX 13.5 vs median 15.3); vol loses most because w4 (+0.134 alpha) just below median (VIX 22.8 vs 25.6) gets suspended. Binary threshold is the load-bearing flaw — diagnostic's Pearson r was graduated, binary cut throws away magnitude. v2 plan: continuous macro-percentile scaler (sigmoid on VIX percentile over 5y rolling) + composite macro (Chicago Fed NFCI etc.) before committing. Driver: `apps/gate/scripts/macro_meta_gate_eval.py`. Artifacts: `Output/macro-meta-gate-eval.json`. Finding: [`macro-regime-diagnostic`](findings/macro-regime-diagnostic.md#v1-results-2026-05-11). |
| 2026-05-10 | macro     | **Macro regime diagnostic** — pulled FRED features (fed_funds, slope_10y_3m, credit_baa, m2_yoy, real_yield_10y, vix) at val_start of each pivot-arc window, computed Pearson r vs per-app-z-scored alpha across all 17 windows | 6 gate + 6 pairs + 5 vol = 17 windows pooled across all three pivot apps' v0 walk-forwards | val_start macro state vs window outcome (one obs per window) | per-feature Pearson r ; contingency table win-rate ; Sharpe N/A | n/a (this is the diagnostic) | best feature: **credit_baa r = +0.488** ; **VIX r = +0.408** ; **fed_funds +0.435** ; m2_yoy −0.378 ; real_yield_10y +0.342 ; slope_10y_3m only 5 of 6 features land in suggestive band ; Sharpe N/A [non-portfolio: per-feature Pearson-r diagnostic] | 5 of 6 directionally consistent (rate-tightening / liquidity-restriction = alpha-friendly) ; VIX-above-median win rate **62%** vs VIX-below-median **11%** (6× lift) | [`diagnostic`](#verdict-labels) — promotes macro features to v1 of `apps/gate` | New `packages/macro/` (`ss_macro`) shipped: no-auth FRED CSV loader for 6 canonical regime features. Diagnostic tests whether macro state at window-start predicts which v0 windows produce alpha. n=17 is small (Pearson r SE ≈ 0.26) so individual r values are noisy, but the **directional consistency across 5 independent features** is the strong signal. The single VIX-median split shows the cleanest contingency: 5/8 wins above median vs 1/9 below = 6× win-rate lift. Win windows cluster in 2008 GFC + 2022 hiking cycle; lose windows in 2005-2007 mid-bull + 2021 ZIRP melt-up + 2014 calm. Validates the user's "macro-econ regime" hypothesis: macro state is the regime structure, not equity-internal. Path forward: add macro features to `apps/gate`'s predictor stack (~50 LoC), test whether gate val Pearson r lifts above v0 +0.26. NOT folding into `apps/regime` — that app is per-ticker CWT-portfolio ranking; macro is universe-wide time-series, conflating would muddle contracts. Operational rule added to CLAUDE.md: default any v1 of a partial-OOS predictor to a macro-gated deployment (deploy when VIX above 1y rolling median). Driver: `apps/gate/scripts/macro_regime_diagnostic.py`. Artifacts: `Output/macro-regime-diagnostic.json`. Finding: [`macro-regime-diagnostic`](findings/macro-regime-diagnostic.md). |
| 2026-05-10 | vol       | **Vol surface v0** — multivariate OLS on 10 untested surface features (skew, smile, IV/HV ratio, OI imbalance, VIX-spread) → forward 20d IV/RV gap, top-20% gated short-vol arm | gauss314 SPX (3,877 symbols × 938 dates over 2019-10 → 2023-07) | 5-window vol (300-train / 120-val / 120-step daily bars; calendar-limited by gauss314 span) | mean val Pearson r ; mean alpha (gated vs unconditional per-cell Sharpe) ; pos-alpha-windows | 5 windows trained per-window | mean val r **+0.120** ; mean unc Sh +0.109 ; **mean gated Sh +0.198** ; **mean alpha +0.089 ; 5/5 positive** | per-cell-Sharpe alpha +0.089 (just below +0.10 marginal floor) | [`inconclusive`](#verdict-labels) — strongest directional consistency in the prediction-problem-pivot arc but mean below threshold | Third app in the prediction-problem pivot. Tests the explicitly-untested feature class flagged by NO_OPTIONS.md (which used only `ATM_IV`). New `packages/iv` (`ss_iv`) promoted from `apps/relational` so both apps share IV loaders + short-vol PnL conventions. **Audit-vs-walkforward divergence is the key methodological lesson:** univariate Pearson r per feature ≤ +0.003 (looked dead), multivariate val Pearson r = **+0.12** (40× larger) — the surface signal lives entirely in the joint structure, not in any single feature. Late windows (2022-12 → 2023-06) carry it: w3 val r +0.27 / alpha +0.155, w4 val r +0.24 / alpha +0.134; early windows (2021) train R² = 0.000 / val r ≤ 0.055. Regime-conditional like everything else — works in the post-COVID-vol regime era, flat in the 2021 mega-cap melt-up. **Partially refutes NO_OPTIONS.md's "IV market efficiently incorporates dislocation" claim**: market is efficient at the ATM-IV level the prior arc tested, leaves residuals at the full-surface level. 5/5 positive windows is the cleanest directional consistency across gate-drawdown / pairs / vol-surface. Caveats: per-cell Sharpe is weak (no temporal aggregation), costs not modeled (options friction 100-1000 bps round-trip would consume most of the +0.089 alpha), gauss314 span limits to 5 windows. v1 follow-ups: per-rebal portfolio aggregation, costs-in-loop, DoltHub extension to 2026, MLP head, OI-liquidity universe restriction. New app scaffold: `apps/vol/` ~600 LoC + `packages/iv` shared. Driver: `uv run python apps/vol/scripts/run_walkforward.py`. Artifacts: `Output/vol-walkforward-summary.json`. Finding: [`vol-surface-v0`](findings/vol-surface-v0.md). |
| 2026-05-12 | factor    | **Sizing-input v0 — `mse_alpha` vs `rank_ic` head-to-head** on signal-quality emission (top-decile − bottom-decile predicted alpha per val bar) | factor-narrow (297 stooq_us_long names, `min_history_bars=6500`) | 6-window factor (63/39/39 blocks @ 20-day rebal) | pooled lag-1 autocorrelation of signal-quality ; Spearman(per-window sq_mean, val Sharpe) ; mean val MSE-alpha | rank_ic train IC +0.115 (mean) ; mse_alpha train IC +0.024 (mean — scale-invariance lost as expected) | rank_ic: lag-1 ac **+0.905** ; ρ **+0.486** ; val MSE-alpha 5.72e-01 ; val Sh +0.278 . mse_alpha: lag-1 ac **+0.821** ; ρ **+0.486** ; val MSE-alpha **1.10e-02** (52× smaller) ; val Sh **+0.482** | mse_alpha vs rank_ic on the pre-registered sizing-input criteria: lag-1 ac Δ **−0.084** ; Spearman Δ **0.000** | [`confirmed-null`](#verdict-labels) — `mse_alpha` produces *calibrated* scores (52× smaller MSE-alpha) but **adds zero information for the sizing-input emission** vs rank_ic | Pre-registered cuts in [`TODO/factor-sizing-input-reframe`](TODO/factor-sizing-input-reframe.md): PASS if mse_alpha clears both (a) lag-1 autocorr ≥ +0.20 AND delta ≥ +0.10 vs rank_ic ; (b) Spearman ρ ≥ +0.40 AND delta ≥ +0.10. Result: both arms' signal-quality clears the absolute thresholds (lag-1 0.82–0.91, ρ +0.486 identical), but neither criterion lifts vs rank_ic — the mse_alpha training change moves score *magnitudes* but not the rank-based signal-quality stat. Mechanism: top-decile − bottom-decile dispersion depends only on score ordering, and rank-IC training already extracts that ordering optimally; rescaling to alpha units adds calibration without adding signal. Side observation: mse_alpha val Sharpe long-only is +0.482 vs rank_ic +0.278 (Δ +0.204) — interesting but in noise band at n=6, and unrelated to the sizing-input hypothesis under test. **Positive side-result for the broader v1 plan:** signal-quality from EITHER training objective has the temporal-stability + val-Sharpe-correlation the macro-meta-gate v1 needs (+0.486 Spearman is above the +0.40 marginal threshold, +0.82–0.90 lag-1 autocorr is comfortably stable). Per `confirmed-null` next-move rule, stop testing the loss axis on apps/factor — proceed straight to v1 with rank_ic as the upstream head. Wall: rank_ic 47s, mse_alpha 27s on T4. Driver: `uvx modal run apps/factor/scripts/modal/sizing_input_eval.py` (local smoke: `apps/factor/scripts/sizing_input_eval.py`). Artifacts: `Output/sizing-input-{rank_ic,mse_alpha}-windows.npz`, `Output/sizing-input-eval-summary.json`. Implementation: `WalkForwardWindow.{train,val}_mse_alpha`, `signal_quality_per_val_bar`, `val_start_date`; `loss_kind='mse_alpha'` arg added to `train_scorer_walkforward`. Finding: [`factor-sizing-input-v0`](findings/factor-sizing-input-v0.md). |
| 2026-05-12 | macro     | **Sizing-input v1 — factor signal-quality as meta-gate input** (extends 2026-05-11 macro v1b harness with factor-only + VIX-AND-factor + VIX-OR-factor arms) | All 17 prediction-problem-pivot windows (6 gate + 6 pairs + 5 vol) | meta-gate retroactive — pivot val_start joined to most-recent factor `val_start_date` ≤ pivot val_start from `sizing-input-rank_ic-windows.npz` (6 factor windows → 5 distinct sq values mapped onto 15 in-range pivot rows) | pooled per-app-z-scored lift across 4 arms (VIX-only, factor-only, VIX-AND, VIX-OR) vs raw-no-gate baseline | n/a (retroactive across pre-existing v0 walk-forwards) | **VIX-only +0.215 z** (baseline) ; **factor-only −0.143 z** ; **VIX AND factor −0.034 z** ; **VIX OR factor +0.106 z** ; best factor arm +0.106 vs VIX +0.215 (Δ −0.109) | best factor arm lift vs VIX-only: **−0.109** (factor signal-quality at this temporal grain DEGRADES the gate) | [`confirmed-null`](#verdict-labels) on the incremental-lift hypothesis (best factor arm 0.109 below VIX-only, well outside ±0.05 FAIL band) | Pre-registered cuts in [`TODO/factor-sizing-input-reframe`](TODO/factor-sizing-input-reframe.md): PASS if any factor arm pooled z-lift ≥ +0.30 ; FAIL if best factor arm within ±0.05 of VIX-only. Result: factor-only arm is **outright negative** (−0.143 z, worse than no-gate), composite arms (AND, OR) both materially below VIX-only. Mechanism: 6-window factor walk-forward emits signal-quality at ~3y resolution; at pivot val_start the most-recent OOS factor read is 0–3y stale and reflects the *previous* regime's dispersion rather than the current one. Two clean inversions: (1) **2008 GFC pivots** (gate w1 +0.321, pairs w1 +0.870) — factor sq +1.128 (read from 2005-11 → 2007-09 calm) says SUSPEND while VIX correctly says DEPLOY; (2) **2011 post-GFC pivots** (pairs w2 +0.593) — factor sq +3.468 (GFC dispersion read) says DEPLOY while VIX says SUSPEND. The factor reading is anti-correlated with the deployment regime at the temporal grain we have. The v0 finding's Spearman ρ +0.486 between *factor-window* sq_mean and *factor-window* val Sharpe is real *within the factor walkforward* but doesn't transfer to a cross-app gate because of the lag. v2 plan: per-bar emission using existing `signal_quality_per_val_bar` arrays (2-hour fix, lag bounded by 20d instead of 780d) before any expanding-window retrain. v0 head (rank_ic) is still the right upstream — the issue is windowing, not training. Driver: `apps/gate/scripts/macro_meta_gate_eval.py` (factor extension). Artifacts: `Output/macro-meta-gate-eval.json` (now includes factor_sq, factor_state, alpha_factor_gated, alpha_and_gated, alpha_or_gated columns). Finding: [`factor-sizing-input-v1`](findings/factor-sizing-input-v1.md). |
| 2026-05-12 | cfr       | **CFR Phase 1 — tabular CFR meta-allocator over 16-action universe-agnostic menu** (EW / top-K mom / rev / lowv / highv × {0, 0.5, 1.0, 2.0} gross), 9-cell infoset on (trailing 21d EW vol × cross-sectional dispersion); train-only quantile cutoffs; 1 chronological pass per window | stooq_us_long (312 tickers) | 6-window cfr (1260-train / 780-val / 780-step daily bars over 2000-2025) | mean val CFR Sharpe ; mean trailing-best-greedy Sharpe ; mean naive uniform Sharpe ; mean passive EW Sharpe ; CFR vs trailing-best frac ; mean alpha vs passive EW | n/a (online tabular CFR; per-window training, single chronological pass) | **CFR Sh +0.593** ; trailing-best **−0.016** ; naive uniform **+0.591** ; passive EW **+0.685** ; CFR>trailing **6/6** | **CFR vs trailing-best lift: +0.609 mean (6/6 wins)** ; CFR vs naive uniform Δ +0.002 (~zero) ; **CFR alpha vs EW −0.093** (1/6 pos windows) | [`partial-OOS`](#verdict-labels) — clears the pre-registered Phase 1 PASS cut against trailing-best-greedy by a wide margin (+0.609 vs +0.10 threshold) but ties naive uniform mix and undershoots passive EW | First Phase-1 verdict from `apps/cfr` (Phase 0 scaffold shipped same day in commit `a552694`). Algorithm validation: counterfactual regret + regret matching converge to non-trivial policies that systematically outperform "switch to whatever just won" greedy baseline by **+0.609 mean Sharpe across 6/6 windows** (windows 0-1: +1.0/+0.9; window 5: +0.25 — lift narrows in calm markets but never inverts). Honest caveat — CFR Sharpe **ties naive uniform** mix (+0.593 vs +0.591), suggesting at this menu × universe the action mix matters less than just deploying everything 1/16: either modes are too close to alpha-zero to reward concentrated bets, OR the 9-cell infoset isn't separating regimes that would benefit from regime-specific concentration. Both interpretations are consistent with **Cover universal-portfolio**: in low-edge regimes, no-regret converges to uniform strategy; mixing is the right play when no infoset has clear edge. Per-window detail: w0 (2005-08, dot-com hangover) +0.279 vs passive 0.529 — α −0.25; w1 (2008-11 GFC) +0.596 vs passive 0.331 — α **+0.265** (the only positive alpha window, mirrors gate-drawdown-v0's GFC outlier); w2-5 (2011-23) consistently lose to passive by 0.11-0.27. Same regime-conditional pattern that landed pivot-arc apps as `partial-OOS`. **Architectural read on CFR ≈ uniform mix:** the algorithm is doing its job (no-regret) but the action menu is the binding constraint — Phase 2's natural move is to add alpha-positive modes (13F-imitation-pretrained scorers, sector-restricted variants) so there's something for regret matching to concentrate on. Wall: image build (cached) + 6.2s uv sync + **7.7s walkforward** + transit = 23s end-to-end on Modal CPU 8c. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase1.py`. Local equivalent: `uv run python apps/cfr/scripts/run_walkforward.py`. Artifacts: `Output/cfr-phase1.json`. Finding: [`cfr-phase1`](findings/cfr-phase1.md). |
| 2026-05-12 | cfr       | **CFR Phase 2a — enriched deterministic menu** (Phase 1 + 4 documented-alpha modes: mom121 = 12-1 momentum, lowv252 = 12-month low-vol, shtop = trailing 252d Sharpe top-K, trend = 252d return/MDD top-K; × {0, 0.5, 1.0, 2.0} gross → 28 actions) | stooq_us_long (312 tickers) | 6-window cfr (1260-train / 780-val / 780-step) | mean val CFR Sharpe ; mean trailing-best Sharpe ; mean naive uniform Sharpe ; mean passive EW Sharpe ; CFR vs trailing-best frac ; mean CFR vs naive uniform ; mean alpha vs passive EW | n/a | **CFR Sh +0.573** ; trailing-best **+0.044** ; naive uniform **+0.632** ; passive EW **+0.685** ; CFR>trailing **6/6** | **CFR vs Phase 1 CFR: −0.020** ; CFR vs naive uniform: **−0.059** (worse than Phase 1's +0.002) ; CFR vs trailing-best: +0.529 (still PASS on this cut) ; CFR alpha vs EW: −0.112 (worse than Phase 1's −0.093) | [`confirmed-null`](#verdict-labels) on the pre-registered Phase 2a cut (≥ +0.10 lift over Phase 1 CFR AND ≥ +0.10 over naive uniform on enriched menu — both failed; CFR was *worse* than Phase 1 on all comparisons) | Pre-registered cuts in updated [`TODO/apps-cfr`](TODO/apps-cfr.md): PASS if CFR ≥ Phase 1 CFR + 0.10 mean Sharpe AND CFR > naive uniform on Phase 2a menu by ≥ +0.10 → menu-enrichment direction confirmed; FAIL if not. Failed both. **Mechanism:** with 28 actions × 9 infosets = 252 regret-table entries and ~6,000 train rebals, adding more actions divides per-cell regret estimator's samples → cumulative-regret S/N per (infoset, action) worsens. Naive uniform mix benefits monotonically from each new diversifying action; regret matching suffers from added noise dimensions. Cesa-Bianchi & Lugosi's O(√(log n)/√T) regret bound gives 0.024 → 0.026 (Phase 1 → 2a), exactly the worsening direction observed. Per-window detail: only w1 GFC posts CFR alpha > 0 (+0.275, similar to Phase 1's +0.265); w0 alpha drops from −0.251 → −0.370 (mom121 / lowv252 / trend modes overpicked tech-bubble names whose 12mo-skip-1mo ranks were stale). **The Cover universal-portfolio diagnosis from Phase 1 is now confirmed at the menu-enrichment axis:** the binding constraint isn't menu content — it's the tabular regret-table sample density. Architectural correction: Phase 3 deep CFR with `regret_net(state, action_emb) → R` shares statistical strength across actions. Wall: 6.2s uv sync + 20.7s walkforward + transit = 34s end-to-end on Modal CPU 8c. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase2a.py`. Artifacts: `Output/cfr-phase2a.json`. Finding: [`cfr-phase2`](findings/cfr-phase2.md). |
| 2026-05-12 | cfr       | **CFR Phase 2b — Phase 2a + real SEC 13F-HR consensus mode** (`Top13FConsensusMode`: top-K most-broadly-held names by fund-count across 14 curated managers since 2013, 45-day filing lag; × {0, 0.5, 1.0, 2.0} gross → 31 actions). New `packages/edgar` (~600 LoC) — EdgarClient with rate limiting + .edgar-cache/, 13F-HR XML parser, manual issuer-name → ticker map covering ~80 mega-cap names | stooq_us_long (312 tickers, 49 in 13F-consensus subset) | 6-window cfr (1260-train / 780-val / 780-step) | mean val CFR Sharpe ; mean trailing-best Sharpe ; mean naive uniform Sharpe ; mean passive EW Sharpe ; CFR vs trailing-best frac ; mean CFR vs naive uniform ; mean alpha vs passive EW | n/a | **CFR Sh +0.583** ; trailing-best **+0.064** ; naive uniform **+0.652** ; passive EW **+0.685** ; CFR>trailing **6/6** | **CFR vs Phase 1 CFR: −0.010** ; CFR vs naive uniform: **−0.069** (worst of Phase 1/2a/2b) ; CFR alpha vs EW: −0.103 ; **per-window: w5 (val 2020-2023, full 13F coverage) alpha lift +0.277 vs Phase 1** | [`confirmed-null`](#verdict-labels) on the headline mean cut, but **per-window pattern is informative** — 13F mode is helpful where it has data (w5: alpha −0.271 → +0.006 vs Phase 1) and harmful where it doesn't (w0 pre-2013: −0.251 → −0.415 because cash-equivalent 13F actions absorb policy mass without contributing return) | Pre-registered cut: PASS if CFR ≥ Phase 1 CFR + 0.10 mean AND CFR > naive uniform on Phase 2b menu by ≥ +0.10. Failed both. **Mechanism:** same Cesa-Bianchi-Lugosi diagnosis as Phase 2a — 31 actions × 9 infosets, regret-table noise per cell continues to grow. **Plus a structural artifact:** `Top13FConsensusMode` returns all-zero weights when no 13F panel data exists for a bar, so its 3 gross variants (0.5/1.0/2.0) become cash-equivalent in pre-2013 windows but are *not* deduped to canonical cash by ActionMenu (gross > 0). CFR routes some policy mass to these phantom-cash actions in early windows, reducing effective deployment. v3 fix: era-dependent menu (drop top13f from menu pre-2013) OR explicit menu-time mask in ActionMenu for actions with no data. Despite the mean wash, **the 13F mode adds +0.277 alpha in the only window with full train+val coverage** (w5: post-COVID + 2022 hike cycle, the same regime cluster gate-drawdown / pairs / vol all post their wins in). The 13F signal is real but the tabular table can't extract it cleanly because (a) only 1.5 windows have full coverage so the 13F-specific cells in the regret table are under-visited, and (b) the cash-equivalent contamination in early windows compounds the noise. **Phase 3 (deep CFR with shared statistical strength + learned encoder over multi-modal state including 13F-overlap signal) is the architectural correction.** Wall: 6.2s uv sync + 20.3s walkforward + transit = 42s end-to-end on Modal CPU 8c. EDGAR prep wall: 254s (cold cache, SEC 10 req/s rate-limited; instant on warm). Driver: `uvx modal run apps/cfr/scripts/modal/run_phase2b.py` (after `prep_phase{1,2b}_data.py`). Artifacts: `Output/cfr-phase2b.json`, `.edgar-cache/`. Finding: [`cfr-phase2`](findings/cfr-phase2.md). |
| 2026-05-12 | cfr       | **CFR Phase 2b-fixed — availability-mask bugfix** (`BaseMode.availability` + `ActionMenu.precompute` returns `(weights, availability)`; `Top13FConsensusMode.availability` masks pre-2013 bars; walkforward skips unavailable actions in sampling/regret/eval) | stooq_us_long (312 tickers) | 6-window cfr (same as Phase 2b) | same metrics as Phase 2b | n/a | **CFR Sh +0.600** (vs 2b's +0.583, +0.017 lift); naive uniform +0.652 (unchanged); CFR vs naive **−0.052** (vs 2b's −0.069); alpha vs EW **−0.085** (vs −0.103) | per-window: w4 alpha lift **+0.164** vs 2b (−0.182 → −0.018), confirms the bugfix removes pre-coverage phantom-cash drag in late-train-coverage windows; w0 still −0.370 alpha (mostly pre-coverage) | [`confirmed-null`](#verdict-labels) (per Phase 2b's pre-reg cut: +0.017 ≪ +0.10 floor) but a clean reduction of the structural artifact identified in the original Phase 2b row | Bugfix-only run on the same Phase 2b menu. Adds `BaseMode.availability(prices) -> bool array`, threads through `ActionMenu.precompute` as `(weights, availability)` tuple, and modifies `CFRWalkForward._train_table` + `_eval_cfr` to mask unavailable actions out of policy sampling, regret update, and average-policy mixing. Bug previously: Top13FConsensusMode in pre-2013 windows returned cash-equivalent (gross > 0) weights that weren't deduped by `ActionMenu`, absorbing CFR policy mass that should have gone to active actions. Fix removes ~0.02 mean Sharpe drag without any other change. The deeper Cesa-Bianchi-Lugosi sample-density problem remains. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase2b.py --output-name cfr-phase2b-fixed.json`. Artifacts: `Output/cfr-phase2b-fixed.json`. Finding: [`cfr-phase3`](findings/cfr-phase3.md) (Phase 3 progression table). |
| 2026-05-12 | cfr       | **CFR Phase 3 — Deep CFR with tinygrad regret_net + 10-feature continuous state vector** (replaces 9-cell tabular infoset; state = 6 universe-internal features + 4 macro from FRED). Architecture: `Linear(10→64)→ReLU→Linear(64→64)→ReLU→Linear(64→31)` MLP, ~7K params, AdamW lr=5e-4 wd=1e-3, expected-baseline regret variance reduction, batch=64 every 5 rebals × 5 SGD/batch | stooq_us_long (312 tickers, full Phase 2b 31-action menu) | 6-window cfr (1260-train / 780-val / 780-step) | mean val CFR Sharpe ; mean trailing-best ; mean naive uniform ; mean passive EW ; CFR vs trailing ; CFR vs naive ; mean alpha vs EW | ~1500 SGD steps/window across ~310 train rebals, expected-baseline regret target | **CFR Sh +0.614** (best across all 5 phase variants); trailing-best +0.064; naive uniform +0.652; passive EW +0.685; CFR>trailing **6/6**; pos α windows **2/6** | **CFR vs Phase 1: +0.021** (real but ≪ +0.15 PASS floor); CFR vs naive: **−0.038** (smallest gap of any enriched-menu phase, but still negative); CFR alpha vs EW: **−0.071** (32% reduction vs Phase 1's −0.093); per-window: **w2 (2011-2014) flips −0.111 → +0.127** (cleanest deep-CFR win, post-GFC recovery regime previously missed by tabular's coarse cells); w5 regresses to −0.105 vs 2b's +0.006 (encoder over-extrapolation in the post-COVID regime) | [`MARGINAL`](#verdict-labels) per pre-registered Phase 3 cut (PASS = ≥ Phase 1 + 0.15 AND > naive by +0.10; FAIL = < Phase 1 − 0.10 OR < naive by −0.10; this is between) | Pre-registered cut: PASS if deep CFR ≥ Phase 1 + 0.15 mean Sharpe (i.e. ≥ +0.74) AND CFR > naive uniform by ≥ +0.10. Hit neither: mean +0.614 (Phase 1 + 0.021) and CFR−naive −0.038. **Architectural validation:** the deep representation IS more sample-efficient than tabular (parameter sharing across the 31×9 = 279 cells solves the Cesa-Bianchi-Lugosi diagnosis from Phase 2). Window 2's clean flip from negative to positive alpha (−0.111 → +0.127) is direct evidence: the continuous regime encoder finds something the discrete grid missed. **But the cumulative architectural progression Phase 1 → 3 is only +0.021 mean Sharpe.** Across 5 phase variants the architecture-side variance is bounded at ~±0.02 — same magnitude as the no-regret O(√(log n)/√T) bound at our T=6,000 / n=31. The binding constraint is no longer architecture or menu — it's signal-availability in the menu × universe × horizon combination. Cover universal-portfolio guarantees no-regret vs the best fixed-mix-in-hindsight, which on 25-year US equity *is* passive EW; we cannot beat it without genuinely regime-switching alpha that exists in some regime. Phase 4 should change the prediction problem (different universe / horizon / menu of composite-regime actions), NOT the meta-allocator's representation. **Pre-registered Phase 4 candidates** (in the [`apps/cfr` TODO](TODO/apps-cfr.md)): hybrid with macro v1b VIX-percentile gate (1-line on top of Phase 3); sector-ETF universe; daily-rebal horizon. Wall: image build (with clang for tinygrad CPU JIT) + 5.5s uv sync + tinygrad install + 48.5s walkforward = 80s end-to-end on Modal CPU 8c. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase3.py` after `prep_phase{1,2b,3}_data.py`. Artifacts: `Output/cfr-phase3.json`. Finding: [`cfr-phase3`](findings/cfr-phase3.md). |
| 2026-05-12 | cfr       | **CFR Phase 4a — Phase 3 deep CFR + per-bar VIX-above-1y-rolling-median gate** (rebal-time mask: when VIX < 1y trailing median, target portfolio = cash; else use deep CFR's policy). Tests whether the macro v1b finding's window-level gate composes with deep CFR at bar-level granularity | stooq_us_long (312 tickers), Phase 2b 31-action menu | 6-window cfr (1260-train / 780-val / 780-step) | mean val CFR Sharpe ; mean naive uniform Sharpe ; mean passive EW Sharpe ; mean alpha vs EW ; pos-α-windows | n/a | **CFR Sh +0.383** (vs Phase 3's +0.614; **−0.231 lift**); naive uniform +0.652; passive EW +0.685; pos α windows **1/6** | **CFR alpha vs EW: −0.302** (vs Phase 3's −0.071) ; CFR vs naive: −0.269 ; gate open **43%** of bars | [`confirmed-null`](#verdict-labels) — verdict per implemented cut (mean < Phase 1 − 0.10 ⇒ FAIL). Bar-level gating is too aggressive | Pre-reg cut: gated mean Sharpe ≥ Phase 3 + 0.15 (≥ +0.76) AND alpha vs gated-EW ≥ +0.10. Failed both spectacularly. **Mechanism:** macro v1b's lift was at the *window* level (suspend whole 3-year periods when val_start VIX < median); 4a applies it per-bar, suspending 57% of bars regardless of CFR's regime call. CFR loses compounding in many bars where it would have produced positive return. Per-window: w0 alpha −0.207 → −0.606, w1 +0.172 → −0.350, w2 +0.127 → −0.531, w5 −0.105 → −0.413; only w3 (−0.308 → −0.103) and w4 (−0.107 → +0.189) improved (where the gate happened to suspend genuinely-bad regimes). **Window-level meta-gating remains a viable v2 idea**, but bar-level is the wrong granularity. Wall: 30s on Modal CPU. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase4a.py`. Artifacts: `Output/cfr-phase4a.json`. Finding: [`cfr-phase4`](findings/cfr-phase4.md). |
| 2026-05-12 | cfr       | **CFR Phase 4b — Deep CFR on sector ETF universe** (10 SPDR sector ETFs: XLB/XLE/XLF/XLI/XLK/XLP/XLU/XLV/XLY + SPY; top_k=3; 13F mode dropped → 28-action menu). Tests universe-shift hypothesis: per-action variance ~10× larger at sector level than per-stock | sector ETFs (10 names, 2005-2025) | 5-window cfr (1260-train / 780-val / 780-step; only 5 windows fit since SPDRs start 2005) | mean val CFR Sharpe ; mean trailing-best ; mean naive uniform ; mean passive EW ; CFR vs naive ; mean alpha vs EW ; pos-α-windows | ~310 train rebals/window | **CFR Sh +0.780** (vs Phase 3's +0.614, +0.166 lift); passive EW +0.765; **mean alpha vs EW +0.015** (first positive across all 9 phase variants!); naive uniform +0.747; CFR vs naive **+0.034** ; pos α windows **3/5** (60%) | per-window alpha: **w0 +0.250, w4 +0.182** (strong positive); w2 −0.357 (worst); w1 +0.015 / w3 −0.012 (tied) | [`partial-OOS`](#verdict-labels) per pre-registered cut (PASS = +0.20 lift over Phase 3 AND ≥ +0.15 alpha vs EW; +0.166 lift and +0.015 alpha both come up short) | First CFR variant to post positive mean alpha vs passive EW. **Universe matters more than algorithm:** at sector-ETF granularity, the deep regret signal is clean enough to find a small alpha that on equity-only it couldn't. Different modes (mom, lowv, mom121, etc.) produce genuinely orthogonal portfolios on 10 sectors; on 312 stocks they had ~85% pairwise correlation. Wall: 11s walkforward + 4s uv sync = 27s end-to-end. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase4b.py` (after `prep_phase4b_data.py`). Artifacts: `Output/cfr-phase4b.json`. Finding: [`cfr-phase4`](findings/cfr-phase4.md). |
| 2026-05-12 | cfr       | **CFR Phase 4c — Phase 3 with rebal_days=5** (4× more SGD samples, 4× more friction at 10 bps round-trip = ~5%/yr) | stooq_us_long (312 tickers), Phase 2b 31-action menu | 6-window cfr (1260-train / 780-val / 780-step at **5-day** rebal) | mean val CFR Sharpe ; mean naive uniform ; mean passive EW ; CFR vs naive ; mean alpha vs EW ; pos-α-windows ; final train loss | ~1240 train rebals/window (4× Phase 3) ; **finite final loss for first time** (1e-3 to 4e-3 across all 6 windows; vs nan in Phase 3) | **CFR Sh +0.574** (slightly worse than Phase 3's +0.614); naive uniform +0.560; passive EW +0.688; CFR vs naive **+0.013** (tied) ; alpha vs EW **−0.114** (worse than Phase 3's −0.071) ; pos α windows **3/6** (better than Phase 3's 2/6) | per-window: w1 alpha +0.379 (GFC, strong), w5 +0.213 (post-COVID); w3 −0.806 (worst across all phase variants) | [`partial-OOS`](#verdict-labels) — mean alpha worse than Phase 3, CFR vs naive marginally positive | Pre-reg cut: ≥ Phase 3 + 0.10 AND alpha ≥ +0.05; failed both. **Notable side observation:** training stability dramatically improved at 5-day cadence. Final loss values were finite for the first time in any deep CFR run — 4× more SGD samples per window let AdamW converge stably without the late-training divergence we saw in Phases 3 / 4a / 4b / 4d. So the algorithm benefits from finer cadence; the friction tax (~5%/yr at 10 bps) eats the benefit at this universe. **Cleaner test:** combine 4c training with 4b/4d universe (sector-ETF or multi-asset where modes are more orthogonal and friction is amortized over larger per-action variance). Wall: 58.5s walkforward (vs Phase 3's 48.5s — only 1.2× slower despite 4× more rebals because tinygrad CPU JIT amortizes after first window). Driver: `uvx modal run apps/cfr/scripts/modal/run_phase4c.py`. Artifacts: `Output/cfr-phase4c.json`. Finding: [`cfr-phase4`](findings/cfr-phase4.md). |
| 2026-05-12 | cfr       | **CFR Phase 4d — Deep CFR on 13-asset multi-asset universe** (9 SPDR sector ETFs + 2 bond ETFs TLT/IEF + 2 commodity ETFs GLD/DBC; top_k=4; 13F dropped → 28-action menu). Tests cross-asset regime-switching hypothesis: bonds/commodities/equities are uncorrelated so per-action variance is large AND there's documented cross-asset alpha (60/40 → barbell during high inflation, etc.) | multi-asset ETFs (13 names, 2005-2025; DBC starts 2006) | 5-window cfr (1260-train / 780-val / 780-step) | mean val CFR Sharpe ; mean naive uniform ; mean passive EW ; CFR vs naive ; mean alpha vs EW ; pos-α-windows | ~310 train rebals/window | **CFR Sh +0.861** (vs Phase 3's +0.614; **+0.247 lift**, the largest in any CFR variant); passive EW +0.805; **mean alpha vs EW +0.056** (positive!); naive uniform +0.760; **CFR vs naive +0.101** (clears +0.10 cut!); pos α windows **3/5** | per-window alpha: **w0 +0.422** (post-GFC), w3 +0.090 (COVID), **w4 +0.301** (post-COVID inflation cycle); w2 −0.508 (the only big loss, 2016-2019 "everything works passive" era); w1 −0.026 (tied) | **[`PASS`](#verdict-labels)** — first variant in 9 CFR walkforward runs to clear the pre-registered cut. mean ≥ Phase 1 + 0.15 = +0.74 ✓; CFR vs naive ≥ +0.10 ✓. **Note:** superseded by 2026-05-13 realistic-friction re-evaluation (next row) — verdict downgraded to `confirmed-null` on realistic-alpha basis. | **The first deployable CFR result on raw-Sharpe basis.** Why multi-asset works where equity-only doesn't: (a) per-action variance — gold ↔ stocks correlation ~0, bonds ↔ stocks ~0; mode portfolios on 13 cross-asset are genuinely orthogonal (vs ~85% correlated on 312 equities), so regret signal per (state, action) is structurally larger; (b) real regime-conditional alpha — cross-asset literature has 60+ years of evidence that stocks/bonds/commodities have a regime-switching optimum (Bridgewater All Weather thesis). Honest caveats: alpha vs EW is +0.056 (positive but below the +0.15 paper-trade threshold from passive-EW-benchmark); only 5 windows (sector ETFs start 2005); w2 −0.508 is a catastrophic single-window loss; no `ss-cfr live` integration yet (need the four risk rails). Wall: 19.7s walkforward + 8.7s uv sync + tinygrad install = ~30s end-to-end. Driver: `uvx modal run apps/cfr/scripts/modal/run_phase4d.py` (after `prep_phase4d_data.py`). Artifacts: `Output/cfr-phase4d.json`. Finding: [`cfr-phase4`](findings/cfr-phase4.md). |
| 2026-05-13 | cfr       | **CFR Phase 4d + window-level VIX meta-gate — final close-out swing** (info-causal: at each val_start, deploy CFR if VIX@val_start > 1y rolling median, else defer to multi-asset EW. Tests whether macro v1b's window-level gate composes with Phase 4d to recover enough alpha to clear paper-trade threshold) | multi-asset ETFs (13 names, same as Phase 4d) | 5-window cfr (same val periods as Phase 4d) | mean net Sharpe ; mean alpha vs DCA ; positive-α windows ; gate fire rate | n/a (re-evaluation, no new training) | gate fired OPEN in **1/5 windows** (w4 only); gate held CLOSED in 4/5 (w0/w1/w2/w3); mean net Sh **+0.853** (gated CFR-or-EW) ; DCA mean net Sh +0.800 ; mean alpha vs DCA **+0.053** ; positive α windows **1/5** (w4 only at +0.263) | per-window: w4 +0.263 (right call — post-Fed-pivot 2022); w2 +0.000 (right call — calm bull, gate avoided CFR's −0.508 raw alpha); **w0 +0.000 — WRONG call** (March 2010 VIX 19 vs 1y median 25 because median was inflated by GFC; gate suspended bot during w0 which was actually CFR's BEST window, threw away +0.422 raw alpha); w1/w3 neutral (CFR ≈ EW regardless) | **[`confirmed-null`](#verdict-labels)** — alpha +0.053 in MARGINAL band by alpha alone, but positive in only 1/5 trips FAIL gate on positive-windows criterion (≤ 2/5 → FAIL). Final row in the CFR arc. | **The bot is fully dead.** Three independent arcs converge: (1) Phase 4d raw alpha doesn't survive realistic friction, (2) Phase 4a bar-level VIX destroys Phase 3, (3) this experiment — window-level VIX gate composes badly. **Mechanism (memory-heavy gate fights regime transitions):** 1y rolling median stays elevated for ~12 months after a stress event, gating the bot off during exactly the early-recovery regimes where it would be most useful. A shorter-memory criterion (60d, 90d) would have classified w0 as OPEN, but free-tuning the lookback to fit 5 windows is overfitting. **Operational rule extracted: memory-heavy regime gates fight you across regime boundaries**; any future gate composition must test memory-window robustness. **DCA stays canonical live; CFR arc fully closed.** Driver: `apps/cfr/scripts/macro_gated_phase4d_eval.py` (local CPU, ~5s). Artifacts: `Output/cfr-phase4d-vix-gated.json`. Finding: [`cfr-macro-gate-final`](findings/cfr-macro-gate-final.md). |
| 2026-05-13 | cfr / dca | **CFR Phase 4d vs multi-asset EW DCA — realistic-friction re-evaluation** (apply 50 bps/yr deployment drag to bot side: ETF spread slippage on TLT/DBC, short-term tax inefficiency from 11× annual turnover, operational labor; 5 bps/yr to DCA side: quarterly rebal, ETF in-kind redemption tax-efficient). Re-uses Phase 4d's per-window Sharpes from `cfr-phase4d.json` and computes EW from the same 13-asset price panel. | multi-asset ETFs (13 names, same as Phase 4d) | 5-window cfr (same val periods as Phase 4d) | mean per-window val Sharpe (NET of friction) ; worst-window Sharpe ; realistic alpha vs EW ; full-panel EW Sharpe (incl GFC) | n/a (re-evaluation, no new training) | **CFR net +0.815** ; **EW net +0.800** ; realistic alpha **+0.015** ; CFR worst window **+0.434** ; EW worst window **+0.642** (+0.21 advantage to DCA on regime stress) ; full-panel EW Sharpe (2005-2025 incl GFC): +0.673 ; full-panel max DD: −40.7% (GFC) | mean alpha shrinks 3.7× from raw +0.056 → net +0.015 ; on $100k portfolio that's ~$200/yr — comfortably exceeded by operational labor of running the bot ; w2 (2016-19) is the binding constraint where DCA pulls clear ahead | **[`confirmed-null`](#verdict-labels)** on realistic-alpha basis (alpha < +0.15 paper-trade threshold from `passive-ew-benchmark`; worst-window Sharpe favors DCA by +0.21). Supersedes the 2026-05-12 PASS row above. | **Deployment decision: DCA wins, bot does not.** Three hard facts: (1) realistic alpha is +0.015 ≈ noise; (2) DCA has *better* worst-case Sharpe; (3) operational tax of ~30 min/month broker monitoring + tax tracking + annual model retrain at $50/hr ≈ >$200/yr already exceeds the alpha. Net expected value of running the bot is negative. **Canonical live strategy is now [`apps/dca`](apps/dca.md)** — 13-asset basket from Phase 4d's universe, EW (1/13 each), quarterly rebal with 5% drift threshold, 5 risk rails (kill-switch, freshness, cadence/drift gate, per-name cap, dry-run). CFR Phase 4d archived; regret-net checkpoint preserved at `Output/cfr-phase4d.json` for future regime-shift re-deployment. Closing finding: [`cfr-vs-dca-realistic`](findings/cfr-vs-dca-realistic.md). |
| 2026-05-14 | cfr / dca | **CFR Phase 4d sensitivity follow-up — friction × gate stress-test** (audit-driven; stress-tests the three load-bearing choices in the 2026-05-13 closure rows: single-point friction at 50/5 bps with no sensitivity, FAIL on `positive in 1/5` denominator artifact, 1y VIX lookback never compared to alternatives). Re-uses Phase 4d's per-window raw Sharpes from `cfr-phase4d.json`; recomputes net Sharpe analytically across friction grid {10,15,25,35,50} × {2,5,10} bps and across gates {60d/90d/126d/252d VIX, 30d/60d in-universe pairwise-correlation dispersion vs 252d median, no gate}. | multi-asset ETFs (13 names, same as Phase 4d) | 5-window cfr (same val periods as Phase 4d) | mean alpha vs DCA across friction × gate space ; positive-α windows ; alpha-on-fired-windows | n/a (re-evaluation, no new training) | **always-deploy CFR (no gate): mean alpha swings from +0.015 (50/5 bps worst case) to +0.056 (10/10 bps best case); positive in 3/5 windows at every grid point**, not 1/5. Best gate by mean alpha is 252d VIX baseline (+0.053 at 50/5); 60d/126d VIX never fire; 90d VIX fires in w3 only (+0.013 mean); **dispersion gate fires in EXACTLY the wrong windows (w1/w2/w3) — captures w2's −0.508 alpha catastrophe; mean alpha −0.115 vs no-gate +0.015**. | original 50/5 bps friction is the worst-case corner not a best estimate; 5× swing across grid never clears +0.10 PASS threshold (max +0.060 at any combination); the "1/5 positive windows" FAIL count in `cfr-macro-gate-final` was an artifact of mixing the gate's deployment policy into the count (4 windows where gate was closed contributed alpha=0 by construction); positive-window count under no-gate is 3/5 → MARGINAL not FAIL. The two audit-proposed gate alternatives are both falsified: shorter VIX lookbacks underperform 252d (60d/126d never fire); average pairwise correlation anti-correlates with CFR's actual alpha pattern at this universe. | **[`diagnostic`](#verdict-labels)** — refines the verdict on the 2026-05-13 rows above. Deployment call (use DCA) unchanged; underlying alpha description ("alpha +0.015 = noise, bot is fully dead") is updated to "alpha +0.015 to +0.056 depending on friction assumption, 3/5 positive windows, below +0.10 deployment threshold". | Audit-driven follow-up to the 2026-05-14 research-directions audit (`.audit-research-directions.md`). Three audit hypotheses tested: (1) friction sensitivity — confirms the +0.015 figure is an unbracketed worst case, but full grid still falls below deployment threshold; (2) shorter-lookback VIX gate — falsified, 60d/126d never fire and 90d underperforms 252d; (3) in-universe dispersion gate — falsified, fires anti-correlated to CFR's alpha pattern at this universe. The "1/5 positive windows" FAIL framing was indeed a misframed denominator (3/5 at always-deploy); the deployment conclusion (use DCA, not CFR) is unchanged because no parameterization clears +0.10. The operational rule from `cfr-macro-gate-final` (memory-heavy gates fight regime transitions) is true for the 252d gate but the proposed fix (shorter lookback) does NOT help. **DCA still wins; the underlying CFR alpha is closer to `partial-OOS` than `confirmed-null` (3/5 positive at always-deploy, below +0.10 threshold), but this row itself is a `diagnostic` audit of prior closures.** Driver: `apps/cfr/scripts/sensitivity_analysis.py` (local CPU, ~3s). Artifacts: `Output/cfr-sensitivity.json`. Finding: [`cfr-sensitivity-followup`](findings/cfr-sensitivity-followup.md). |
| 2026-05-14 | pairs | **Pairs v0.1 — EG-passing-rate regime gate (audit-followup)** (re-screens each v0 train window for the pre-truncation EG-passing-count, which v0 did not log; applies three pre-registered gate thresholds — T_abs=3000 from the audit's read of v0 logs, T_pct50=median of train EG counts, T_pct30=30th percentile — against v0's existing per-window val Sharpes from `pairs-walkforward-summary.json`. Pre-reg before seeing the val effect.) | stooq_us_long (297 tickers, `min_history_bars=6500`) | 6-window pairs (1260-train / 780-val / 780-step) | mean agg val Sharpe across full panel + fired-only; positive-window count under both denominators | re-screening only (~8 min wall, 44k pairs × 6 windows / 8 workers); no backtest re-run | **Audit hypothesis falsified**: train EG-pass per window = [3918, 3522, 3118, 4755, 2249, 2857]; w0 train EG-pass is 3918 (3rd highest) yet val Sharpe is −1.233 (worst window). T_abs=3000 fires in w0/w1/w2/w3, mean alpha **+0.104** full-panel (+0.156 fired-only, 3/4 positive) — 3-5× short of audit's predicted "+0.099 → ~+0.5"; T_pct50=3320 fires in w0/w1/w3, mean alpha **+0.005** (worse than no-gate); T_pct30=2988 same fires as T_abs. **All three thresholds FAIL** the v0 pre-reg +0.20 marginal floor. | the audit's read of v0 stdout logs (3522-4755 working, 2249-2857 failing) was a post-hoc fit to val Sharpe — w0 sits in the "working" EG band by count yet posts the worst val Sharpe in the panel; the actual train EG-pass ranges (2249-4755 vs 2857-3918) overlap heavily and do not separate working from failing OOS regimes. Mechanism: train EG-pass count is dominated by the cross-sectional density of cointegrating relationships *during* the training slice (2000-2005 had high cointegration due to dot-com crash forced co-movement); those relationships break OOS. | **[`confirmed-null`](#verdict-labels)** on the EG-gate hypothesis specifically; pairs v0 verdict label remains `confirmed-null` though operationally better described as "alpha in 4/6 windows, mean dragged below floor by one large negative window" — the +0.099 → +0.5 audit prediction is falsified by 3-5× at every pre-registered threshold. | Audit-driven `diagnostic` follow-up to `pairs-classical-v0` and the 2026-05-14 research-directions audit. Tests the audit's specifically-named hypothesis ("if alpha goes from +0.099 → ~+0.5 with a sensibly pre-registered threshold, pair trading flips to deployable") and falsifies it cleanly: the EG-passing-rate is *not* a regime indicator at the v0 universe + horizon. Two remaining cheapest follow-ups for pairs identified by v0 are still untested: (a) per-pair stop-loss at 4σ to cap w0's catastrophe without affecting working windows; (b) within-sector restriction to remove cross-sector spurious cointegration. **`apps/pairs` stays parked**; the audit-flagged "missed deployment lever" is now confirmed to not be one. Driver: `apps/pairs/scripts/eg_gate_eval.py` (local 8-core, ~8 min wall). Artifacts: `Output/pairs-eg-gate-summary.json`. Finding: [`pairs-eg-gate-falsified`](findings/pairs-eg-gate-falsified.md). |
| 2026-05-14 | vol | **Vol surface v1 — per-rebal portfolio aggregator + costs in the loop** (audit-followup; replaces v0's "weak metric" per-cell Sharpe with time-series annualized Sharpe of a 100-pick top-K basket rebal'd every 20 days, alpha-vs-universe-baseline construction so friction cancels by symmetry; shuffle control with 10 random-seed predictors per window quantifies basket-averaging-artifact baseline). Pre-reg cuts locked before running v1: PASS = alpha Sharpe ≥ +0.30 with ≥ 4/5 positive; MARGINAL = [+0.10, +0.30] with ≥ 3/5; FAIL = < +0.10 OR ≤ 2/5. | gauss314 SPX universe (~3,877 symbols × 918 dates, 2019-10-14 → 2023-06-29) | 5-window vol (train=300d / val=120d / step=120d, rebal_days=20, top_k=100, no overlap) | **alpha Sharpe (annualized) of `gated_per_rebal − universe_per_rebal` series**; positive-α-windows; real-to-shuffle alpha-PnL ratio | linear OLS (same as v0; MLP deferred to v2 since v1 already PASSES) | **pooled alpha Sharpe +5.86** ; 5/5 positive windows ; pooled alpha mean PnL **+0.182 vol pts/rebal** vs shuffle baseline **+0.007 ± 0.005** (real / shuffle = **25.0×**) ; absolute gated Sharpe at 100 bps = +6.77 ; at 500 bps = +5.47 (alpha is friction-invariant) ; per-window alpha Sh = [+3.56, +27.5, +3.85, +13.7, +12.6] | per-window: w3 (2022-06 → 2022-12) and w4 (2022-12 → 2023-06) post the cleanest signal (val r > +0.23, alpha mean PnL > +0.23 vol pts) — replicates the v0 finding's "post-COVID inflation regime has richer surface structure" pattern; even early windows (w0 val r = +0.005) post alpha Sharpe +3.56 because alpha *mean* is stable (+0.184) while alpha *variance* is small | **[`confirmed-OOS`](#verdict-labels)** on the audit's "v0 had real signal, v1 will confirm it" hypothesis. **Supersedes v0's `inconclusive` verdict on the deployment-metric basis** — v0's per-cell-Sharpe metric was the bottleneck, not the signal. The +0.089 per-cell alpha had the right sign but understated the temporal-consistent signal by an order of magnitude. | **Most deployable workstream in the repo as of 2026-05-14** (contingent on dollar-PnL conversion + OI restriction in v2). Vol-points Sharpe ≠ dollar Sharpe — a cross-position correlation haircut (ρ≈0.3 across short-vol picks) would drop the absolute Sharpe by ~sqrt(5) ≈ 2.2×, leaving alpha Sharpe ≈ 2.6 — still well above PASS threshold. Audit's call "strongest signal in the repo, benched without v1 work" empirically validated. DCA remains canonical *live* (simplest deployable, no options friction); vol v1 becomes the active *research* workstream. v2 priorities: (1) dollar-PnL conversion with correlation, (2) OI-top-200 restriction, (3) DoltHub OOS extension to 2026, (4) MLP head. Driver: `apps/vol/scripts/run_walkforward_v1.py` (local ~3 min wall). Aggregator: `apps/vol/src/vol/portfolio.py`. Artifacts: `Output/vol-walkforward-v1-summary.json`. Finding: [`vol-surface-v1`](findings/vol-surface-v1.md). |
| 2026-05-14 | vol | **Vol surface v2 #1 — dollar-PnL conversion under three sizing conventions** (audit-followup #1; joins Stooq prices to compute ATM vega per (date, symbol), re-evaluates v1's top-100 basket under equal-$-vega, equal-share-count, and equal-vol-points sizing; pre-reg cuts PASS if all sizings clear +0.30 with dispersion ≤ 2×). | gauss314 ∩ Stooq overlap (~2191 symbols, 62% of full panel) | 5-window vol (train=300d/val=120d/step=120d, rebal_days=20, top_k=100) | pooled annualized portfolio Sharpe per sizing convention; dispersion ratio | n/a (re-evaluation with vega weighting) | **pooled $-vega Sharpe +4.60** ; pooled share-count Sharpe +1.95 ; both 5/5 positive ; **dispersion 2.36×** (just trips pre-reg ≤2× criterion) ; observed val r came UP on Stooq-overlap subset (+0.165 vs v1 +0.120 — 37% higher) | even the conservative (share-count) Sharpe is +1.95, 6.5× above PASS bar; the audit's "vol-points Sharpe might be overstated in $ terms" concern is empirically false at this universe — standard equal-$-vega sizing gives same Sharpe as vol-points; share-count sizing over-weights mega-caps and loses ~half the Sharpe but stays positive | **[`confirmed-OOS`](#verdict-labels)** on the audit's "alpha exists in dollar terms" hypothesis (operational read overrides strict-pre-reg MARGINAL because share-count is not the standard sizing for vol strategies). | Audit-followup. The audit (`.audit-research-directions.md` at repo root) called dollar-PnL conversion the single biggest unknown. Result: under equal-$-vega (hedge-fund standard) the dollar Sharpe IS the vol-points Sharpe; under share-count-equal it's smaller but still PASS. Driver: `apps/vol/scripts/run_walkforward_v2_dollar.py` (~3 min local). Artifacts: `Output/vol-walkforward-v2-dollar-summary.json`. Finding: [`vol-surface-v2-dollar-pnl`](findings/vol-surface-v2-dollar-pnl.md). |
| 2026-05-14 | vol | **Vol surface v2 #2 — OI-restricted walk-forward** (audit-followup #2; filters predictor pool to top-N by `puts_open_interest + calls_open_interest` per date BEFORE top-K pick selection; tests whether v1 alpha survives restriction to liquid options; pre-reg cuts PASS if alpha Sharpe ≥ +0.30 AND ratio to v1 ≥ 0.5×). Sweep over OI thresholds {200, 500, 1000}. | gauss314 → OI-top-N per date | 5-window vol (same shape, top_k scaled down on tighter universes) | pooled alpha Sharpe; per-window alpha pattern; ratio to v1 unrestricted +5.86 | n/a (re-evaluation with OI filter) | **OI-top-200: alpha Sh −0.48, 3/5 pos, −0.08× v1** ; OI-top-500: alpha Sh −0.05, 3/5 pos, −0.01× ; OI-top-1000: alpha Sh +1.04, 3/5 pos, +0.18× ; **alpha decays monotonically with universe restrictiveness**. Per-window pattern even on liquid names: w0/w1 (2021 calm-bull) anti-correlate; w3 (2022-06 → 2022-12 post-Fed-pivot) and w4 stay strongly positive (alpha Sh +4.2 and +6.5 on top-200). | the audit-flagged "OI restriction is the most important v2 step before paper-trading" identified the binding constraint, not a refinement; the vol surface alpha is concentrated in low-OI names where market-makers are conservative on premium; mechanism is consistent with academic VRP literature (premium scales inversely with options liquidity) | **[`reversed-OOS`](#verdict-labels)** on deployability — the v1 PASS verdict applies to the unrestricted universe but not the deployable subset that real options markets require. | The arc-decisive experiment. Combined with v2 #1 (PASS in $ terms) and v2 #3 (PASS on OOS extension), this is the lever that prevents broad deployment. Regime-gated-liquid-universe v3 is the natural rescue (deploy only when stress-regime conditions met; per-window data shows liquid-name alpha exists in 2022-2023 windows). Driver: `apps/vol/scripts/run_walkforward_v2_oi.py`. Artifacts: `Output/vol-walkforward-v2-oi-{oi200,oi500,oi1000}-*.json`. Finding: [`vol-surface-v2-oi-restriction`](findings/vol-surface-v2-oi-restriction.md). |
| 2026-05-14 | vol | **Vol surface v2 #3 — DoltHub OOS extension 2023-08 → 2026-04** (audit-followup #3; builds 4-feature proxy from DoltHub `volatility_history` parquet: iv_over_hv, iv_z, iv_change_4w, hv_change_4w; trains on 2019-10 → 2023-07 gauss314-overlap period; tests on 2.7-year OOS slice with no gauss314 overlap; pre-reg cuts PASS if OOS Pearson r ≥ +0.05 AND ≥ 8/11 positive quarters). | DoltHub `volatility_history` (~2265 symbols × weekly Saturday snapshots) | single train (2019-10 → 2023-07) + single OOS val (2023-08 → 2026-04, split into 11 quarterly windows for verdict) | OOS Pearson r (pooled), cross-sectional r, alpha-PnL per quarter, positive-quarter count | OLS on 4-feature proxy + forward realized vol from Stooq prices (honest target, fixed a target-leakage bug where DoltHub's hv_current[t+4] has 0.85 autocorrelation with hv_current[t]) | **OOS Pearson r = +0.165** (37% higher than v1 in-sample +0.120, on weaker 4-feature stack) ; cross-sectional r +0.07 (post-de-mean) ; alpha mean PnL **+0.076 vol pts per rebal** (42% of v1's +0.182 — expected with 4 features vs 10) ; **positive in 11/11 OOS quarters** ; pooled alpha Sharpe +7.48 ; train r² +0.041 | initial-run target was `iv − hv_current[t+4w]` which leaked because hv_current has 0.85 autocorrelation at lag-4-weeks; fixed by computing forward RV from underlying log-return std × sqrt(252) over the next 20 trading days from Stooq prices ; signal-on-unrestricted continues OOS — but v2 #2 still blocks deployment on liquid subset | **[`confirmed-OOS`](#verdict-labels)** on signal persistence. The underlying name-level VRP inefficiency hasn't been arbitraged away over 2.7 years of OOS. | Confirms the audit's "natural OOS test" hypothesis. Important operational rule extracted: when constructing forward-realization targets, use price returns, not lagged HV snapshots (the autocorrelation leaks). Driver: `apps/vol/scripts/run_walkforward_v2_dolthub_oos.py`. Artifacts: `Output/vol-walkforward-v2-dolthub-oos-summary.json`. Finding: [`vol-surface-v2-dolthub-oos`](findings/vol-surface-v2-dolthub-oos.md). |
| 2026-05-14 | vol | **Vol surface v3 — regime-gated liquid-universe composition** (composes v2 #2's OI-top-200 filter with VIX-N-day-rolling-median per-rebal gate; tests whether stress-regime gating rescues deployability lost in v2 #2). Pre-reg cuts: PASS = fired-α Sh ≥ +0.30 AND fire-rate ∈ [20%, 80%] AND ≥ 4/5 fired-positive. Sweep over gate lookbacks {60d, 126d, 252d}; headline ex-ante is 60d. | gauss314 → OI-top-200 per date | 5-window vol (top_k=50 picks on liquid pool, rebal_days=20) | fired-alpha Sharpe (annualized); fire rate; fired-positive window count; full-panel alpha (closed-gate rebals defer to universe) | re-evaluation only (linear OLS predictor reused from v1) | **60d (headline ex-ante)**: fired-α Sh −0.66, fire-rate 43%, 2/5 pos → **FAIL** (short-memory gate fires in calm-bull). **126d (operational best)**: fired-α Sh **+2.01**, fire-rate 37%, 3/5 pos → **MARGINAL**. **252d**: fired-α Sh +1.84, fire-rate 33%, 2/5 pos → FAIL (≤2 windows with fires; w0/w4 zero fires). | 126d arm correctly held closed in w0 (2021 calm-bull, 0 fires) avoiding v2 #2's α=−2.57 disaster; fired in w3 (2022 post-Fed-pivot, 3 fires, captured α=+0.036). The 60d ex-ante FAIL + 126d operational MARGINAL demonstrates the memory-window-robustness operational rule from `cfr-sensitivity-followup`: gate lookback must match the regime-transition timescale (126d for vol surface; 252d for CFR cross-asset rotation). | **[`partial-OOS`](#verdict-labels)** on the arc (v3 architecture is the deployment recipe). 126d arm is MARGINAL by strict pre-reg (3/5 < 4/5 fired-positive bar) but materially rescues v2 #2's FAIL. | Closes the v2 arc decision: vol IS deployable on liquid options markets WITH the 126d-VIX-rolling-median gate constraint, expected operating point ~4.7 fired rebals/yr at +2.01 fired-α Sharpe (95% CI roughly +0.5 to +3.5 at one-year sample). DCA stays canonical live for simplicity; vol v3 unblocks the live-infrastructure build-out (ss-vol live + IBKR/Tradier options broker, ~12h wall). Driver: `apps/vol/scripts/run_walkforward_v3_regime_gated.py`. Artifacts: `Output/vol-walkforward-v3-regime-gated-summary.json`. Finding: [`vol-surface-v3-regime-gated`](findings/vol-surface-v3-regime-gated.md). |
| 2026-05-14 | vol | **Vol surface v2 arc-synthesis — partial-OOS closure** (synthesizes v0/v1/v2 #1/v2 #2/v2 #3 into a single arc verdict; documents the binding-liquidity-constraint, the regime-gated-liquid v3 rescue path, and the live-deployment work that remains). | n/a (synthesis) | n/a | n/a | n/a | signal real (v1 PASS, v2 #3 OOS-stable); dollar-Sharpe substantial (v2 #1 PASS); deployability blocked by liquidity (v2 #2 reversed-OOS); regime-conditional liquid-name alpha exists in 2022-2023 windows but absent in 2021 windows | the audit's "strongest signal in the repo, benched without v1 work" framing is validated for signal existence and OOS continuity, but the deployability claim hinges on a regime-gate-on-liquid-universe v3 test not yet run; DCA stays canonical live until v3 settles deployment-readiness | **[`partial-OOS`](#verdict-labels)** at the arc level. v1 PASS verdict on unrestricted alpha stands; deployment requires v3 (regime-gated liquid universe) before paper-trading; absent v3, arc would close `confirmed-null` on broad-deployment basis. | Recommended v3 architecture documented in synthesis page: top-200 OI filter + VIX-above-N-day-median regime gate + full 10-feature surface + equal-$-vega sizing + 100-500 bps cost-in-loop. Pre-reg cuts: net Sharpe ≥ +0.30 on top-200-OI universe WITH regime gate applied, positive in ≥ 4/6 deployed windows. ~6h to implement and run. If v3 PASSES, arc closes as `partial-OOS` shippable (with regime constraint). If v3 FAILS, closes `confirmed-null` on broad-deployment. Synthesis: [`vol-arc-synthesis`](findings/vol-arc-synthesis.md). |
| 2026-05-14 | relational / cross-app | **Delisting-aware passive EW benchmark (audit-followup)** (tests whether the `passive-ew-benchmark` ffill convention silently inflates passive Sharpe by holding delisted names flat at last close, per the audit's predicted +0.05 to +0.15 Sharpe bias monotone with universe breadth). Three-arm replication: existing `ffill().dropna(axis=1)` vs delisting-aware-cash (capital → cash on first NaN, redistributed at next rebal) vs strict-perm-death (only counts NaN this bar AND all subsequent bars as delisting, transient halts smoothed via `ffill(limit=5)` like the existing code). | phase-2 (21), stooq_us_long (312), ex-phase-2 (296), factor-wide (2162) | phase-2 split (val 2021-01-01 → 2025-12-11), rebal_days=20, commission_bps=10 | val Sharpe per arm ; Δ Sharpe between strict-perm-death and existing-ffill ; count of permanently-dead tickers in val window | re-evaluation only (~30s local Mac for all 4 universes) | **Permanent deaths in val per universe: 0/21, 0/312, 0/296, 0/2162.** Δ Sharpe (strict − existing): phase-2 +0.0000, stooq_us_long −0.0002, ex-phase-2 −0.0002, factor-wide +0.0001. **Audit's predicted +0.05 to +0.15 Sharpe bias does not materialize at any universe breadth**; the existing ffill convention is empirically a no-op because the underlying data contains no permanently-delisted names. | mechanism is two-pronged and unfixable in-script: (1) `stooq_us_long/manifest.json` is hand-curated to entries with `last_date ≈ archive_end` (survivorship filter at universe selection — manifest builder enforced "still alive in archive" criterion); (2) the Stooq archive itself reuses ticker symbols when companies delist and replacement entities adopt them (BBBY shows $20 prices in April 2023 when Bed Bath & Beyond was bankrupt at pennies, and ~$6 in late-2024 — clearly a successor entity using the ticker symbol). The trailing data after a delisting event is replacement-entity data, not held-flat phantom data. The audit's specific predictions are falsified: stooq_us_long Morlet alpha shifts from −0.133 to −0.135 (not flipped to within-noise); ex-Phase-2 analog alpha shifts from −0.334 to −0.336 (not narrowed to −0.20); monotone-with-breadth pattern does not appear (Phase-2 = 0 bias, all other universes ≈ identical 0.0002 bias regardless of size 296 vs 2162). | **[`confirmed-null`](#verdict-labels)** on the audit's hypothesis — the predicted ffill-delisting bias does not exist on this dataset at the predicted magnitude. **All alpha verdicts from `passive-ew-benchmark.md` stand as written**: Phase-2 analog +0.067 (noise), stooq_us_long Morlet −0.133 (alpha-negative), ex-Phase-2 analog −0.334 (alpha-catastrophic). The deeper issue (Stooq ticker reuse + manifest curation) is a real survivorship problem in absolute Sharpe terms but it is bidirectional in bias direction and cancels in the alpha column when model + passive run on the same universe. | Audit-driven `confirmed-null` test of the audit's `passive-ew-benchmark` ffill critique. The audit (`.audit-research-directions.md` at repo root) predicted "a cleanly-handled delisting accounting could flip the ex-Phase-2 −0.334 to within −0.20, and possibly flip stooq_us_long's −0.133 Morlet result entirely." Empirical Δ on strict-perm-death implementation is 0.0001-0.0002 Sharpe per universe — three orders of magnitude smaller than the audit's prediction. The audit's diagnosis (ffill convention masking delistings) was correct *as a code review observation* but irrelevant *as a numerical bias* because the universes in question contain no delisted names to begin with. The "monotonic passive Sharpe decay with universe breadth" pattern the audit pointed to has a different mechanism: mega-cap concentration dilution, not delisting bias. Driver: `apps/relational/scripts/delisting_aware_ew_benchmark.py` (local CPU, ~30s end-to-end). Artifacts: `Output/delisting-aware-ew-benchmark.json`. Finding: [`delisting-aware-ew-falsified`](findings/delisting-aware-ew-falsified.md). |
| 2026-05-14 | factor | **Endogenous-horizon mixture — state-conditional rebal cadence** (pre-registered test of the hypothesis that rebal cadence should be a model output; emits per-bar scores + softmax `π_t` over horizon bins `{5,10,20,40,60}`, deploys at `argmax(π_t)` holding flat between rebals; per-bar mixture loss `-mean_t Σ_k π_t[k]·IC_k_t` with no entropy regularization; pre-reg PASS = endog Sharpe ≥ best-fixed + 0.10 AND beats random-π AND no π collapse). | factor-narrow | 6-window factor at h_min=5 fine grid (train=252 fine bars ≈ 5y, val=156 ≈ 3y, step=156) | mean val net Sharpe under irregular cadence vs each fixed-h baseline vs random-π | n/a (mixture loss is per-bar IC × π) | endog **+0.448** ; fixed h=5 +0.327, h=10 +0.322, h=20 +0.288, h=40 +0.399, **h=60 +0.401 (best fixed)** ; random-π +0.329 ; delta vs best-fixed **+0.048** (under +0.10 threshold) ; delta vs random-π **+0.119** ; π argmax shares {5:0%, 10:2%, 20:0%, 40:18%, **60:80%**} ; per-window: w3 (2015) and w5 (2021) use a real mixture (entropy 0.32/0.19) and beat best-fixed by +0.02 each; w0 (2005-11, GFC run-up) confidently picks h=60 and **loses by −0.37** dragging the mean ; without w0 the remaining 5 windows tie best-fixed | the per-bar mixture loss without entropy regularization rewards collapse to whichever horizon has structurally larger Pearson IC magnitude (longer horizons → more signal-to-noise); once π_t is near δ on h=60, the gradient on `π_t[k≠60]` is small and the model gets stuck; lower turnover cost reinforces the attractor. The +0.119 vs random-π lift is largely **cost-reduction** (random has 60% mass on h≤20 vs endog's 80% on h=60) not state-conditional skill. Only the two windows where the model failed to collapse actually beat best-fixed — consistent with state-conditional selection having real content underneath the collapse. | **[`partial-OOS`](#verdict-labels)** — 3/4 nulls pass (N1: no global collapse worst-share 0.80 ≤ 0.90 ; N2: beats fixed h_max ; N4: beats random-π) ; N3 fails (delta +0.05 < +0.10). Endogenous selection works *when the model uses the mixture* but the naive loss finds the degenerate attractor in 4/6 windows. | Next experiment is an entropy-weight sweep over `α ∈ {0.05, 0.1, 0.2, 0.3}` to force mixture use (smallest α that keeps the w3/w5 pattern alive in all 6 windows; tracked in [`TODO/factor-horizon-entropy-reg`](TODO/factor-horizon-entropy-reg.md)). If no α clears the threshold, verdict closes `confirmed-null` on this architecture and the orthogonal lever is a different *kind* of horizon-emission (e.g. continuous horizon head + REINFORCE) rather than discrete mixture. Driver: `apps/factor/scripts/modal/horizon_mixture.py` (T4 cloud, ~4 min walk-forward wall + feature build ≈ 13 min total, ~$0.07). Artifacts: `Output/horizon-mixture-{windows.npz, comparison.png}`. Finding: [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md). |
| 2026-05-14 | vol | **Vol surface v3.1 — composite regime gate (next-experiment after v3 oracle)** (pre-registered after v3 oracle revealed +2.86 fired-Sharpe headroom; tests whether multi-feature gates capture the oracle ceiling. 6 arms on same 5 walk-forward windows: vix-126d baseline ; disp-126d only ; mean-vrp-126d only ; vix-OR-disp ; vix-AND-disp ; lr-composite (4-feature logistic regression on per-rebal alpha-sign) ; oracle). | gauss314 → OI-top-200 per date | 5-window vol (same as parent v3 row) | pooled fired-α Sharpe per arm vs vix-126d baseline + oracle ceiling ; per-window detail ; pre-reg cuts | n/a (uses same predictor as v3) | **`vix-or-disp` PASSES strict pre-reg** (fired-α Sh +0.39 ≥ +0.30, fire rate 67% in band, 4/5 fired-positive matching oracle). **No arm clears STRONG-PASS** (fired-α Sh ≥ +3.0). vix-126d single-feature still wins on per-rebal Sharpe (+2.01 vs OR-composite's +0.39); the OR-composite captures w4 (2022-12 recovery) which vix-126d misses but dilutes Sharpe by firing on lower-quality rebals. Every composite arm is *worse* than vix-126d on per-rebal Sharpe; best composite delta is `vix-and-disp` at −0.80. **lr-composite fails** (76% fire rate, only +0.15 fired-Sharpe — overfit on 15 train rebals/window). | the oracle's +2.86 headroom **does not live** in simple cross-sectional aggregates (VIX, iv-over-hv dispersion, mean-VRP) at rolling-median thresholds. Three plausible explanations for where it does live: time-varying/higher-order features (regime momentum, term-structure inversions); insufficient training data for learned approaches at this sample scale; non-Markovian signal that snapshots can't recover. | **[`confirmed-null`](#verdict-labels)** on "simple composite captures the oracle headroom" hypothesis. **[`partial-OOS`](#verdict-labels)** on operational deployment — `vix-or-disp` is a deployable second arm with better window diversification (4/5 vs 3/5 positive) at lower per-rebal Sharpe. | Two deployable arms now exist: single-VIX-126d (higher per-rebal Sharpe, fewer windows) and OR-composite (more window diversification, lower per-rebal). Choice depends on operational preference. **STRONG-PASS bar not yet achievable** with simple feature engineering — closing >30% of oracle headroom requires richer features (per-symbol dispersion patterns, term-structure, options-flow), more training data (expanding-window classifier), or non-binary gate (continuous sizing). None pre-registered. Workstream paused at partial-OOS with deployment unblocked. Driver: `apps/vol/scripts/run_walkforward_v3_1_composite.py` (local CPU, ~2 min wall, no Modal). Artifacts: `Output/vol-walkforward-v3-1-composite-summary.json`. Finding: extended in [`vol-surface-v3-regime-gated`](findings/vol-surface-v3-regime-gated.md) under "v3.1 — composite regime gate". |
| 2026-05-15 | pairs | **Pairs v1 — ML half-life-and-friends predictor (pre-registered #1 EV follow-up after the oracle finding)** (7 train-time features into L2-regularized logistic regression, expanding-window walk-forward training; predicts P(val Sharpe > 0) per screened pair; two selection arms: thr-0.5 and top-50; tests whether the +1.79 oracle headroom is reachable with simple aggregates). | stooq_us_long (297 tickers) | 6-window walk-forward at h=20 (same as v0 + EG-gate + oracle parent rows) | mean val Sharpe per arm vs v0 baseline + oracle ceiling capture rate | numpy-only L2-regularized LR via Newton-Raphson; expanding window (window 0 falls back to baseline; later windows train on prior windows' (features, label) tuples) | **predictor-thr-0.5 mean +0.104, 5/6 positive** (captures **5.4%** of oracle's +1.79 headroom); **predictor-top-50 mean +0.023, 4/6 positive** (captures 0.9%). Per-window: predictor-thr-0.5 lifts 3/6 windows (w2 −0.33→+0.30, w4 +0.14→+0.29, w5 −0.05→+0.19) but the lifts are <0.6 Sharpe each. predictor-top-50 actively WORSENS w0 (−1.23 vs −1.04), w3 (+0.31 vs +0.56), w4 (−0.09 vs +0.14) — uncalibrated confidence scores hurt under concentration. LR over-predicts positive (mean P 0.56-0.73, 75-99% flagged P≥0.5) — features don't discriminate. | the 7 features capture *train-window characteristics* of each pair (`train_sharpe`, `half_life`, `corr`, `eg_p`, `|β|`, `pct_in_trade`, `n_trades`) but NOT: cross-pair relative information, forward regime indicators, stability of the cointegrating relationship, or non-linear feature interactions. The oracle's +1.79 headroom lives in feature space the v1 stack doesn't span. | **[`confirmed-null`](#verdict-labels)** on the v1 predictor design at this feature set + LR classifier. The oracle headroom from the parent row stands as theoretical maximum; no real-time predictor we've tested closes more than 5% of it. | Workstream paused. Future directions (none pre-registered): random forest / GBM on same features (modest expected lift); richer features encoding train-vs-val regime similarity rather than just in-sample fit quality (larger expected lift); regression target on val Sharpe magnitude + top-K selection; cross-window features using each pair's historical OOS generalization. Driver: `apps/pairs/scripts/run_pair_predictor_walkforward.py` (local CPU, ~7 min wall, no Modal). Artifacts: `Output/pairs-predictor-walkforward-summary.json`. Finding: extended in [`pairs-eg-gate-falsified`](findings/pairs-eg-gate-falsified.md) under "v1 predictor follow-up". |
| 2026-05-14 | pairs | **Pairs v0 — hindsight oracles on pair selection + per-window gate (largest oracle headroom in the 4-app survey)** (cross-app oracle diagnostic; tests whether the EG-gate-falsification's "alpha is regime-bound, w0 unsalvageable" framing holds when per-pair selection is allowed. 5 arms: all-pairs baseline, oracle-pos-pairs (keep pairs with val Sharpe > 0), oracle-top-half, oracle-top-quartile, window-gate-oracle (gate whole windows on v0 sign)). | stooq_us_long (297 tickers, min_history=6500) | 6-window walk-forward at h=20 (same as v0 + EG-gate parent rows) | mean val Sharpe per arm ; positive-window count ; per-window decomposition vs v0 baseline | n/a (reuses v0 screen + backtest; no retraining) | **oracle-pos-pairs mean +1.80 Sharpe, 6/6 positive vs v0 baseline +0.006, 3/6 positive — clears PASS by 3.6×**. oracle-top-quartile +2.43 mean (4.9× PASS). window-gate-oracle only +0.24 (3/6). **The decisive finding: per-pair selection has +1.79 of unrealized headroom; per-window gating has +0.24. Signal is at the pair level by 7×.** Even w0 (v0 catastrophic −1.04) has 116/200 positive-Sharpe pairs and posts +2.17 under oracle selection. | the EG-gate falsification's framing ("w0 unsalvageable due to regime shift") was wrong. The regime shift is real, but ~58% of screened pairs in w0 STILL mean-revert; the screening admits ~84 bad ones that drag the aggregate negative. Train-side regime measures operate at the wrong granularity — the issue isn't WINDOW-level regime identification but PAIR-level half-life prediction. | **[`diagnostic`](#verdict-labels)** on the oracle itself (cheats; not deployable). **Upgrades the v0 and EG-gate parent rows' framing**: the `confirmed-null` verdict on pair-spread mean reversion is *predictor-bound*, not architecture-bound. | Re-prioritizes the v0.x candidates: ML predictor for half-life (was #3) moves to #1. Per-pair selection has +1.79 of headroom; window-gate has +0.24; stop-loss / sector restriction are lower-leverage. Pre-reg cuts for any per-pair selector v1: PASS if mean ≥ +0.50 AND ≥ 4/6 positive (≥ 28% of oracle ceiling); STRONG-PASS if mean ≥ +1.0 AND ≥ 5/6 positive (≥ 55% capture). Driver: `apps/pairs/scripts/run_oracle_walkforward.py` (local CPU, ~10 min wall, no Modal). Artifacts: `Output/pairs-oracle-walkforward-summary.json`. Finding: extended in [`pairs-eg-gate-falsified`](findings/pairs-eg-gate-falsified.md) under "Hindsight oracle". |
| 2026-05-14 | gate | **Drawdown gate v0 — hindsight oracles on the gate decision** (cross-app oracle diagnostic; two ceilings: perfect-DD-predictor (uses realized forward 20d DD with same gate threshold logic, isolates predictor-quality limit) and perfect-daily-direction (gate iff next-day EW > 0, theoretical max of any binary gate)). | stooq_us_long | 6-window walk-forward, train=1260 / val=780 / step=780 bars at h=20 (same as parent v0 row) | mean alpha Sharpe per arm ; positive-α windows ; avg exposure ; per-window decomposition vs heuristic | n/a (no retraining; reuses same train/val splits) | **DD-oracle mean alpha +0.387 vs heuristic +0.067** — clears PASS by +0.29 over the +0.10 cut ; 6/6 positive windows (vs heuristic's 4/6) ; avg exposure 0.879 (identical to heuristic). **Daily-direction-oracle: +9.066 alpha, 6/6 positive, avg exposure 0.548** (theoretical maximum of any binary gate). Per-window pattern reveals systematic OLS failures cluster at high-alpha events: w1 GFC heuristic captures 24% of DD-oracle alpha (+0.321 vs +1.365), w4 COVID captures only 2% (+0.015 vs +0.783) — OLS predictor failed to flag COVID at all. **Heuristic captures 17.2% of DD-oracle ceiling**. | the v0 partial-OOS verdict is **predictor-bound, not architecture-bound**. The gate logic + prediction target + threshold methodology are fine; OLS on 10 aggregate features is the choke point. Across the 6 windows, OLS systematically misses the drawdown events that carry the bulk of available alpha. Better predictor → +0.32 mean alpha sitting on the table. | **[`diagnostic`](#verdict-labels)** on the oracle itself (cheats; not deployable). **Upgrades the gate v0 parent row's framing**: the partial-OOS reading stands as a result, but the ceiling-vs-implementation gap is now documented and the v1 priorities re-ordered. | Re-prioritizes the v1 candidates: predictor-quality interventions (two-stage classifier, better features, MLP/RF) move ahead of the regime-conditional-gate direction (which targets the gate-action lever, not the predictor lever that the oracle reveals is binding). Pre-reg cuts for any v1: PASS if mean alpha ≥ +0.20 AND ≥ 5/6 positive (captures ≥ 50% of DD-oracle's +0.387); STRONG-PASS if mean alpha ≥ +0.30 AND 6/6 positive. Driver: `apps/gate/scripts/run_walkforward.py --threshold-quantile 0.95` (extended with `oracle_dd_*` and `oracle_day_*` arms). Artifacts: `Output/gate-walkforward-summary.json`. Finding: extended in [`gate-drawdown-v0`](findings/gate-drawdown-v0.md) under "Hindsight oracles". |
| 2026-05-14 | vol | **Vol surface v3 — hindsight oracle on the regime-gate decision** (cross-app oracle diagnostic; greedy oracle fires iff realized per-rebal alpha > 0, strict upper bound on any gate; reruns v3 walkforward with oracle arm alongside the 60d/126d/252d VIX-rolling-median gates). | gauss314 → OI-top-200 per date | 5-window vol (same as parent v3 row) | pooled fired-α Sharpe vs each VIX-lookback heuristic ; full-panel α Sharpe ; fire rate ; fired-positive windows | n/a (no retraining; reuses per-rebal (gated-pnl, universe-pnl) from v3 windows) | **Oracle pooled fired-α Sharpe +4.87 vs 126d heuristic +2.01** — oracle headroom is **+2.86** ; oracle full-panel α Sh **+3.25** vs 126d **+1.13** (2.9× lift) ; oracle fire rate **70%** vs 126d 37% (heuristic misses roughly half of positive-alpha rebals) ; oracle fired-positive windows **4/5** vs 126d 3/5 → oracle clears the strict pre-reg PASS bar that 126d missed by exactly one window. Per-window: oracle's g − u positive in 5/5 (predictor's signal IS real per-rebal; noise costs only on rebals where alpha is negative by chance). | the v3 MARGINAL verdict (3/5 fired-positive) is **gate-bound, not architecture-bound**. The vol-surface predictor produces positive alpha on roughly 70% of rebals on the top-200-OI universe; VIX-126d-rolling-median captures just over half of those because the alpha fires for reasons partly orthogonal to vol-of-vol (cross-sectional IV dispersion, term-structure, post-event drift). A composite gate richer than single-VIX-state has substantial real room to lift the verdict. | **[`diagnostic`](#verdict-labels)** on the oracle itself (it cheats; not deployable). **Upgrades the v3 parent row's verdict framing**: the partial-OOS MARGINAL stands as written, but the ceiling-vs-implementation gap is now documented and the v3.x research priority is re-ordered. | The composite regime gate (VIX + cross-sectional IV dispersion, item #4 in the original v3.x candidates) becomes the highest-value next experiment, not the lowest. Pre-reg cuts: (1) gate train + walkforward eval matched to v3 setup ; (2) PASS if composite-gate fired-α Sh ≥ +0.30 AND fire-rate ∈ [20%, 80%] AND ≥ 4/5 fired-positive ; (3) STRONG-PASS if fired-α Sh ≥ +3.0 (captures ≥ 50% of the +2.86 oracle headroom). Driver: `apps/vol/scripts/run_walkforward_v3_regime_gated.py` (extended with `simulate_oracle` arm; reuses cached per-window (g, u) panels, no retraining needed). Artifacts: `Output/vol-walkforward-v3-regime-gated-summary.json` (now contains `summary_by_lookback['oracle']`). Finding: extended in [`vol-surface-v3-regime-gated`](findings/vol-surface-v3-regime-gated.md) under "Hindsight oracle". |
| 2026-05-14 | factor | **Endogenous-horizon mixture — regime-gated heuristic + hindsight oracle (arc-closure with corrected ceiling)** (two related tests in one row. (a) Apply the workspace "regime filter > richer predictor" rule via VIX vs 126d rolling median, two direction priors + 3 baselines. (b) Greedy hindsight oracle picks the per-bar best horizon — strict upper bound on any real-time selector, isolates whether the heuristic failure is feature-limited or information-limited). | factor-narrow | 6-window factor at h_min=5 fine grid (same as parent rows) | mean val net Sharpe per arm ; oracle horizon-use distribution as a diagnostic on information content | n/a (deterministic deployment; score head is vanilla rank-IC linear) | **Heuristic gates fail**: inverted-vol −0.123, same-vol +0.196, fixed-h60 +0.221, fixed-h5 −0.061, random-h +0.116. All 4 pre-reg nulls FAIL for inverted-vol (N1−N4 all negative, direction prior reversed). **But oracle reveals a real ceiling**: hindsight greedy = +0.333, beats fixed-h60 by +0.112 → **N1 PASSES for the oracle**. Oracle argmax shares `{h=5:27%, h=10:22%, h=20:12%, h=40:11%, h=60:28%}` — distinctly not collapsed, U-shape across all 5 horizons. Decomposition: mixture's α=0 lift over vanilla-h5-head/fixed-h60 of +0.227 = ~+0.18 score-head specialization + ~+0.05 horizon-selection (mixture captures ~42% of the oracle's +0.112 horizon-selection ceiling). | the heuristic-VIX-binary regime gate captures 0% of the +0.112 oracle ceiling; the learned mixture-of-IC head captures ~42% (the +0.05 marginal). Information content for state-conditional horizon-selection exists at the per-bar level but is NOT well-extracted by single-feature regime classifiers. The score-head-specialization effect (~+0.18, dwarfs horizon-selection) is the dominant lever for raising deployment Sharpe; horizon-selection is a real but smaller secondary lever. | **[`confirmed-null`](#verdict-labels)** on the narrow heuristic-VIX-binary regime-gating form. **[`partial-OOS`](#verdict-labels)** on the broader endogenous-horizon thesis when corrected for the oracle ceiling (the prior leaderboard rows' partial-OOS verdict stands; the arc does NOT close confirmed-null end-to-end). | The arc closes partial-OOS. Operational rules landed: (1) train the score head at the deployment horizon — bigger lever than any horizon-selector. (2) the learned mixture captures ~42% of the horizon-selection oracle ceiling; heuristic VIX-binary captures 0%. Open follow-ups (not pre-registered): mixture-trained head + oracle (the true ceiling for this universe); `rebal_days=60` direct training (sanity-check specialization); per-bar trailing-IC-by-horizon as a real-time information-aware selector. Driver: `apps/factor/scripts/horizon_regime_gated.py` (local CPU, ~2 min wall, no Modal). Artifacts: `Output/horizon-regime-gated-windows.npz`. Finding: extended in [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md) under "Regime-gated horizon selection" + "Hindsight oracle" + "Arc closure (revised)". |
| 2026-05-14 | factor | **Endogenous-horizon mixture — entropy-weight sweep (TODO/factor-horizon-entropy-reg followup)** (5-arm sweep `α ∈ {0.0, 0.05, 0.1, 0.2, 0.3}` testing whether `−α·H(π_t)` rescues the partial-OOS verdict; hypothesis was that π collapse is the binding constraint). | factor-narrow | 6-window factor at h_min=5 fine grid (same as parent row) | mean val net Sharpe per α ; mean π entropy per α ; per-window endog vs best-fixed delta | n/a | **α=0.0**: endog +0.448, Δ-fix +0.048, H(π)=0.11 ; **α=0.05**: +0.408, +0.011, H=1.10 ; **α=0.1**: +0.422, +0.028, H=1.40 ; **α=0.2**: +0.420, +0.023, H=1.52 ; **α=0.3**: +0.401, +0.009, H=1.55 ; all five arms `partial-OOS` (N1/N2/N4 pass, N3 fails) ; Δ-rand stays roughly constant at +0.12 across the sweep | entropy reg works exactly as designed on π (mean H(π) climbs from 0.11 → 1.55, h=60 argmax share drops from 0.80 → 0.60), but the deployment Sharpe **does not improve** at any α. Two diagnostic windows tell the whole story: w0 (2005-11) stays catastrophic at −0.25 to −0.30 across the sweep regardless of α — none of the K discrete horizons can rescue that regime ; w3 (2015, where unregularized model already used a useful state-conditional mixture at entropy 0.32) DROPS from +0.84 at α=0 to +0.66–0.71 at α≥0.05 — forced higher entropy replaces good selections with worse ones. Best Δ-fix monotone-decreases as α rises, meaning the α=0 +0.05 lift was largely cost-concentration on h=60, not state-conditional skill. | **[`confirmed-null`](#verdict-labels)** on the entropy-regularization rescue hypothesis. π collapse was not the binding constraint; the architecture is bottlenecked on whether the score head's information at h=40+ varies meaningfully with state. The 2026-05-14 unregularized `partial-OOS` row above stands as the canonical result for the discrete mixture-of-horizons-IC architecture. | Closes `TODO/factor-horizon-entropy-reg`. Architecture ceiling on factor-narrow + 74-channel IndicatorGridConfig is at ~+0.05 Sharpe partial-OOS. Orthogonal levers remain: different feature stack (CWT bundle or polar-Morlet), different horizon-emission (continuous head + REINFORCE), or explicit regime-classifier → horizon mapping rather than end-to-end soft attention. None of those are pre-registered as next experiments yet — the workstream is on pause pending a separate hypothesis. Driver: `apps/factor/scripts/modal/horizon_mixture.py --entropy-weights '0.0,0.05,0.1,0.2,0.3'` (T4 cloud, ~50 min total wall, ~$0.30). Artifacts: `Output/horizon-mixture-{a0,a0p05,a0p1,a0p2,a0p3}-{windows.npz, comparison.png}` + `Output/horizon-mixture-sweep-summary.json`. Finding: extended in [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md) under "Entropy-weight sweep". |
| 2026-05-15 | critic | **Φ(state, action) → predicted-deployment-Sharpe v0 — cross-app window-level value function** (pre-registered Φ-quality test from `TODO/critic-phi-value-function.md`. Tiny MLP (2×16 hidden, 817 params) trained on 134 window-level triples pooled across factor/gate/pairs/vol. State features: VIX-level, VIX-6m/1m change, SPY 252d/63d log-return at val_start, app one-hot. Action features: 23-dim one-hot over the cross-app action vocab. LOO-by-(app, window): 23 folds. Baselines: global-mean, per-app-mean, per-action-mean.) | cross-app pool (factor 6w, gate 6w, pairs 6w, vol 5w; 134 total triples) | leave-one-(app, window)-out CV; 23 LOO folds total | per-app RMSE_Φ ; RMSE_Φ / RMSE_per-action-mean ratio ; Spearman r(Φ, realized) on held-out ; per-app verdict (PASS/MARGINAL/FAIL) ; overall verdict | in-sample RMSE 0.078 vs predict-mean baseline 2.31 (Φ memorizes train trivially — 817 params on 134 samples) | **First-pass (with oracle arms in vocab) deceptively positive**: gate r=+0.75, pairs r=+0.65, vol r=+0.36 — looks like rank quality. But RMSE always loses to per-action-mean (factor rel 4.47, gate 1.92, pairs 1.61, vol 2.43) → all four apps FAIL pre-reg App-PASS cut. **Diagnostic re-run dropping oracle arms (105 triples)** flips Spearman NEGATIVE on all four apps: factor r=−0.19, gate r=−0.85, pairs r=−0.51, vol r=−0.60. The apparent v0 rank quality was entirely the model learning the trivial "oracle = high" classifier via action one-hot — no real state-conditional ranking signal exists at this granularity. | window-level state features (macro VIX/SPY at val_start) + 23-dim action one-hot do NOT carry learnable state-conditional Sharpe-prediction signal for these apps at this dataset size. The 134-triple window-level dataset is below the threshold where a tiny MLP can extract anything beyond the per-action marginal mean. **Off-policy distribution shift is the binding constraint AND the data granularity is wrong**: oracles inflate apparent rank quality; non-oracle action vocab is too narrow + too few samples per (app, window) for the macro state features to discriminate. | **[`confirmed-null`](#verdict-labels)** on the value-function-from-aggregate-data hypothesis as pre-registered. Window-level Φ does NOT beat per-action-mean baseline on any app. The high-Spearman-with-oracles result is methodologically discarded as trivial oracle-detection. | Closes the day-1 pre-registration in `TODO/critic-phi-value-function.md`. **Natural rescue paths NOT closed by this row**: (a) pair-level Φ for pairs (300 per-pair training triples already exist; richer per-pair features) → being evaluated next as Φ v0.1; (b) per-rebal triple emission from existing apps (requires re-running walk-forwards with new emission code; ~3-6 hour workstream) — defer until v0.1 result. Driver: `apps/critic/scripts/run_phi_quality_walkforward.py`. Artifacts: `Output/critic-phi-quality-{summary, no-oracle-summary}.json`. Finding: documented in [`critic-phi-quality-v0`](findings/critic-phi-quality-v0.md). |
| 2026-05-15 | critic | **Φ v0.1 — pair-level Φ for pairs with rich 7-feature pair representation** (natural rescue of v0; tests whether finer granularity with the v1 LR's 7 pair features + 5 macro context features changes the verdict. Extended `apps/pairs/scripts/run_pair_predictor_walkforward.py` to emit `pairs-predictor-per-pair-records.npz` → 1200 per-pair records (6 windows × 200 backtested pairs). Tiny MLP (2×16 hidden) vs ridge-regression LR baseline. Two variants run: pair+macro features (12-d) and pair-only (7-d). LOO-by-window: 6 folds.) | stooq_us_long (297 tickers, min_history=6500) | 6-window walk-forward at h=20 (same as pairs v0/v1 parent rows), 1200 per-pair training records | RMSE_Φ vs RMSE_LR ridge regression vs RMSE_window-mean baseline ; Spearman r ; mean realized Sharpe of top-50-by-prediction within each held-out window | n/a (LR baseline trained per-fold from scratch; Φ retrained per-fold) | **Pair+macro (12 feat)**: RMSE_Φ 0.945, RMSE_LR 0.944, RMSE_window-mean 0.576 — rel(Φ/LR) 1.001, r(Φ) +0.054, r(LR) +0.103. **Pair-only (7 feat)**: RMSE_Φ 0.633, RMSE_LR 0.557, RMSE_window-mean 0.576 — rel(Φ/LR) 1.136, r(Φ) +0.105, r(LR) +0.202. Both variants: Φ ties or loses to LR on RMSE; LR has stronger rank quality. **Top-50 deployment proxy**: Φ-with-macro +0.156 vs LR +0.141 vs all-pairs baseline +0.102 vs within-window oracle +0.804 — Φ captures 7.7% of oracle headroom, LR 5.5% (Φ marginally beats LR on argmax precision but not on cuts). | **Information is in the pair features but is linear and small.** LR captures r=+0.20 Spearman in the pair-only variant — a real signal that beats predict-window-mean baseline by ~3% RMSE. Non-linearity hurts: Φ overfits the 1000-sample train set and adds noise. Macro features are pure noise at this granularity (LR RMSE balloons 0.557 → 0.944 when macro added); they amount to a per-window offset that the window-mean baseline already captures and that LOO-by-window sees as out-of-support. **The 7-feature pair representation is information-bounded — both linear and non-linear models max out at ~r=+0.20, ~5-8% oracle capture.** Lifting pair selection requires a different feature representation, not a different classifier. | **[`confirmed-null`](#verdict-labels)** on the pair-level Φ rescue hypothesis. Generalizes the existing pairs v1 LR closing diagnostic: the 5.4% oracle-headroom capture rate is approximately the ceiling at this feature space. **Arc closure**: both v0 (window-level cross-app) and v0.1 (pair-level for pairs) are confirmed-null; the cheap version of the user's value-function-from-existing-triples idea is adequately falsified at the available data granularity. | **Day-2 (policy training against -Φ) NOT attempted** — pre-reg exit condition was day-1 FAIL → close as `confirmed-null`. Open follow-ups that could change the verdict (not pre-registered): (1) per-rebal emission across apps (~3-6 hour workstream); (2) richer pair features — half-life trajectory, cross-pair rank within window, sector relations, term-structure of correlation; (3) end-to-end decision-structure training (the factor mixture pattern, the only approach that crossed meaningful oracle headroom in the survey). Driver: `apps/critic/scripts/run_pair_phi_quality.py [--no-macro]`. Artifacts: `Output/critic-pair-phi-quality-{summary, no-macro-summary}.json` + `Output/pairs-predictor-per-pair-records.npz`. Finding: extended in [`critic-phi-quality-v0`](findings/critic-phi-quality-v0.md) under "v0.1 rescue" + "Arc closure". |
| 2026-05-15 | factor | **Target-side REINFORCE — train π on Sharpe-residual, not rank-IC (TODO/factor-reinforce-target-side followup)** (4-arm sweep β ∈ {0, 0.5, 2.0, 8.0}. Loss = rank-IC for score head (detached in REINFORCE term) + β·score-function-gradient `-mean_t[log π_t[a_t]·advantage_t]` for π, where `a_t ~ Categorical(π_t)` sampled per bar and `advantage_t = (ret_at_sampled_t − traj_mean)/traj_std` is the per-bar Sharpe-residual. Attacks the rank-IC-vs-Sharpe training/deployment misalignment the horizon-aligned diagnosis pinned. Note: multi-arm Modal runs hit a stdout-buffering bug dropping all-but-last arm; β=0.5/2/8 run as separate single-arm Modal jobs, β=0 reuses cached 2026-05-14 baseline.) | factor-narrow (297 stooq_us_long, min_history=6500) | 6-window walk-forward at h_min=5 fine grid (same as parent rows) | mean val net Sharpe per β ; Δ-fix vs best-fixed-h ; π argmax shares ; per-window endog ; w3-canary check | n/a (deterministic train + eval; only β swept) | **β=0**: endog +0.448, Δ-fix +0.048 (baseline) ; **β=0.5**: +0.435, Δ-fix +0.039 ; **β=2.0**: +0.359, Δ-fix −0.003 (mid-β dip) ; **β=8.0**: endog **+0.453**, best-fix h=10 +0.357, **Δ-fix +0.095**, 5/6 positive, H(π) 0.80, argmax **h20:33% h40:43% h60:25%**. **Non-monotonic phase-transition in β**: degrades through β≤2 then jumps at β=8. **First rescue that does NOT damage the w3 canary**: w3 endog +0.855 (vs default +0.84) — prior three rescues dropped w3 by −0.18 to −0.29. At w3 mixture (+0.855) matches best achievable fixed-h40 (+0.86) → π correctly state-selecting. | the rank-IC-vs-Sharpe misalignment (identified by the three prior failed rescues) is **real and addressable**. The prior three kept π's gradient direction aligned with rank-IC (entropy smeared it, bilevel-return pointed the same way, horizon-aligned changed inputs not objective). Mean-centered + std-normalized Sharpe-residual is the first intervention with an expected gradient direction *orthogonal* to rank-IC's: mean-centering kills the "raise-all-returns" component rank-IC over-attributes to h=60; std-normalization is the Sharpe-shaping. Phase transition: needs β≈8 to fully escape the rank-IC attractor; β≤2 nudges are strictly worse. | **[`partial-OOS`](#verdict-labels)** (MARGINAL) on the best arm β=8. Δ-fix +0.095 clears the +0.07 MARGINAL cut (below the +0.10 PASS cut), 5/6 positive. **First positive lever in the four-attempt endogenous-horizon rescue arc** — entropy reg (+0.011), bilevel (−0.001), horizon-aligned grid (−0.036) all `confirmed-null`; this is the one that moves the needle and the one that preserves w3. | Per partial-OOS default-next (stratify windows): (1) higher-β sweep β ∈ {16, 32} — response is monotone-up past the β=2 dip, may clear +0.10 PASS, ~25 min Modal each (single-arm due to the buffering bug); (2) w0 regime gate — w0 (2005-11) is the only negative window and is negative under *every* config + *every* fixed horizon (structurally hard regime, not a π failure), gating it out lifts the mean; (3) output-side restructure (#2, per-horizon score heads + mixture over specialized heads) — pre-reg flagged this if REINFORCE PASSed; at MARGINAL it's a candidate not yet pre-registered. Driver: `apps/factor/scripts/modal/horizon_mixture.py --reinforce-weights '<β>'`. Artifacts: `Output/horizon-target-{beta8,...}-{windows.npz, comparison.png}` + `Output/horizon-target-sweep-summary.json`. Finding: extended in [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md) under "Target-side REINFORCE". |
| 2026-05-15 | factor | **Horizon-aligned IndicatorGridConfig — input-side rescue (TODO/factor-horizon-aligned-grid followup)** (2-arm sweep `(horizon-aligned, λ ∈ {0, 0.25})` testing whether expanding the 74-channel default grid to 104 channels with cells at periods matching the action-space horizons `{5, 10, 20, 40, 60}` (RSI n_grid +{20,40,60}; vol +{40}; MACD +{10,20,40,60}; coherence +{5,40,60}; CCI unchanged due to warmup) lifts the +0.048 Δ-fix from the 2026-05-14 baseline. Compared against cached `(default, λ ∈ {0, 0.25})` arms.) | factor-narrow (297 stooq_us_long, min_history=6500) | 6-window walk-forward at h_min=5 fine grid (same as parent rows) | mean val net Sharpe ; Δ-fix vs best-fixed-h ; per-horizon mean fixed-Sharpe profile (the load-bearing diagnostic) ; π argmax shares ; per-window endog | n/a (deterministic train + eval; same hyperparams as 2026-05-14 sweep) | **(default, λ=0)** baseline: endog +0.448, best-fix h=60 +0.401, **Δ-fix +0.048**, h=60 argmax 80%. **(horizon-aligned, λ=0)**: endog +0.401, **best-fix h=10 +0.437** (the IC profile genuinely flattens — score head IS using new short-horizon channels), **Δ-fix −0.036**, h=60 argmax **94%** (π collapses HARDER despite richer features). **(horizon-aligned, λ=0.25)**: endog +0.396, best-fix h=10 +0.431, Δ-fix −0.035. Per-window w3 (2015-06-30, the canary): default +0.84 → horizon-aligned **+0.55** (Δ −0.29) — third independent rescue attempt damages the same window, matching the entropy-reg drop (+0.84 → +0.66) and bilevel-reward drop (+0.84 → +0.57). | **the architecture has a fundamental misalignment**: π is trained to maximize per-bar rank-IC (which over-rewards h=60 due to highest per-bar SNR at long cumulative-return horizons) while the deployment metric is Sharpe (which under horizon-aligned grid is highest at h=10). Features genuinely make short-horizon Sharpe competitive (+0.437 at fixed-h10 vs +0.401 at default-h60), but π's training signal directs the policy to h=60 anyway. Three independent rescue attempts (entropy reg, bilevel return reward, horizon-aligned features) all damage the same fragile working window w3 in the same pattern — the architecture's state-conditional skill is information-bounded AND objective-bounded at this dataset scale. | **[`confirmed-null`](#verdict-labels)** on the input-side rescue hypothesis. The horizon-aligned grid does NOT lift mixture endog above default; the apparent score-head-IC improvement at short horizons doesn't translate to deployment because π's rank-IC training signal can't capitalize. | Closes `TODO/factor-horizon-aligned-grid`. **Side-finding worth documenting**: `(horizon-aligned, fixed-h10)` = +0.437 Sharpe is a comparable, simpler-to-operate deployment recipe (no mixture head, no π eval; just train score head and trade at h=10) vs `(default, mixture)` = +0.448 — gap is 0.011, within noise. Not pre-registered as a deployment but a real alternative if/when the mixture architecture is retired. Open follow-ups (not pre-registered): output-side restructure (per-horizon score heads, mixture *over* specialized heads); target-side intervention (REINFORCE-style π training with realized Sharpe as reward, bypassing rank-IC). These are genuinely new architectures. Driver: `apps/factor/scripts/modal/horizon_mixture.py --config-variant horizon-aligned --deployment-reward-weights '0.0,0.25'` (Modal T4, ~25 min wall, ~$0.15). Artifacts: `Output/horizon-align-{lam0,lam0p25}-{windows.npz, comparison.png}` + `Output/horizon-align-sweep-summary.json`. Finding: extended in [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md) under "Horizon-aligned feature grid". |
| 2026-05-15 | factor | **Bilevel horizon objective — rank-IC for score head, deployment-return for π (TODO/factor-bilevel-horizon-objective followup)** (5-arm sweep λ ∈ {0, 0.25, 0.5, 1.0, 2.0} testing whether direct deployment-return supervision lifts the +0.048 Δ-fix from the 2026-05-14 entropy-zero baseline toward the +0.112 oracle ceiling. Loss = `-mean_t Σ_k π_t[k] · IC_k_t / std_detached(IC) - λ · mean_t Σ_k π_t[k] · ret_k_t / std_detached(ret)`. The score head detaches inside the return term so it sees only rank-IC gradient; π head sees both. Per-batch std-normalization keeps λ dimensionless.) | factor-narrow (297 stooq_us_long, min_history=6500) | 6-window walk-forward at h_min=5 fine grid (same as parent rows) | mean val net Sharpe per λ ; Δ-fix vs best-fixed-h60 ; π entropy ; per-window Δ-fix decomposition ; per-window pos count | n/a (deterministic train + eval; no hyperparam tuning beyond λ) | **λ=0**: endog +0.448, Δ-fix **+0.048** ; **λ=0.25**: +0.453, **+0.047** (ties baseline within noise) ; **λ=0.5**: +0.426, +0.021 ; **λ=1.0**: +0.409, +0.009 ; **λ=2.0**: +0.401, +0.000 ; **Δ-fix monotone-decreases as λ rises**. Per-window: w3 (2015-06-30, the only window where mixture meaningfully won) Δ goes from +0.021 (λ=0) → -0.282 (λ=2) — return-noise destroys the single working window. pos_Δ_vs_best_fixed: 2/6 (λ=0,0.25) → 1/6 (λ≥0.5). H(π) climbs 0.11 → 0.27 but redistribution stays in {h=40, h=60}; short horizons (h=5/10/20 oracle wants 49% of bars on) stay near-zero across all arms. | the deployment-return signal is structurally noisier than rank-IC at this dataset scale: per-bar rank-IC aggregates over ~150-200 ticker correlations; per-day score-weighted return at horizon k is one scalar per (bar, k) cell. The SNR ratio is not tunable. **Both rescue attempts (entropy reg AND bilevel return-supervision) hurt the same single working window (w3) most** — the architecture's state-conditional horizon-mixing skill is fragile, emerging only at the cleanest noise level (rank-IC alone). Any signal injected into π's training that isn't aligned with rank-IC's direction degrades that fragile skill. | **[`confirmed-null`](#verdict-labels)** on the bilevel-rescue hypothesis. No arm clears the +0.07 MARGINAL cut or the +0.10 PASS cut. The 2026-05-14 unregularized `partial-OOS` row above stands as the canonical result for the discrete mixture-of-horizons-IC architecture on factor-narrow. | Closes `TODO/factor-bilevel-horizon-objective`. **Bilevel framing's CONCEPT is sound** — "train each head with the loss that matches the decision it makes" generalizes cleanly — but the empirical falsification is specifically about *per-day raw return as the π supervision signal* on *this dataset scale*. Open follow-ups (not pre-registered): (1) heavier deployment-aware loss with full softmax-top-N portfolio + commission accounting at training time (~5-10× more expensive); (2) different score-head features (CWT bundle, polar Morlet, SSL backbone); (3) per-bar trailing-IC-by-horizon as a real-time selector (closest cousin to bilevel without autograd). Driver: `apps/factor/scripts/modal/horizon_mixture.py --deployment-reward-weights '0.0,0.25,0.5,1.0,2.0'` (Modal T4, ~50 min wall, ~$0.30). Artifacts: `Output/horizon-bilevel-{lam0,lam0p25,lam0p5,lam1,lam2}-{windows.npz, comparison.png}` + `Output/horizon-bilevel-sweep-summary.json`. Finding: extended in [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md) under "Bilevel objective". |
| 2026-05-15 | critic | **Φ v0.2 — policy training against -Φ (user-requested exploratory; arc-closure validation of the day-2 step)** (after v0/v0.1 closed `confirmed-null` per pre-reg, user requested running the day-2 policy step anyway. Two policies trained against fixed Φ on the pair-level pairs data: π_vanilla maximizes E[σ(π)·Φ] with no regularization; π_CQL adds an anchor toward the empirical top-50/200=0.25 inclusion rate. Deploy via top-50 ranking per window; compare vs all-pairs baseline, LR, Φ-direct, oracle.) | stooq_us_long (same as v0.1) | 6-window LOO-by-window over the 1200 per-pair training records; per fold, Stage-1 trains Φ on the train fold and Stage-2 trains the policy network against fixed Φ | top-50 mean realized Sharpe per fold ; mean across folds ; % capture of oracle headroom (+0.702) ; per-fold Spearman of policy-vs-Φ rankings | n/a (Φ and policy both retrained per fold) | **All-pairs baseline +0.102 ; LR (v1 analog) +0.141 (+5.6% capture) ; Φ-direct +0.156 (+7.8%) ; π_vanilla +0.116 (+2.0%) ; π_CQL +0.142 (+5.8%) ; oracle +0.804 (100%)**. Best policy = π_CQL at +0.142 → Δ vs LR +0.001 (below MARGINAL cut +0.01), Δ vs Φ-direct **−0.014** (below PASS cut). **Methodological side-finding**: a misconfigured first run with Φ_lr=1e-2 (vs v0.1's tested 5e-3) showed π_CQL beating Φ-direct by +0.07 — exposing that **the CQL anchor acts as a regularizer on an under-trained Φ**, not a source of additional information. When Φ is hyperparameter-matched to v0.1, the apparent lift collapses. | **The policy step adds no degrees of freedom over Φ at this setup**: when π and Φ share the same input space and architecture, the optimization problem `max E[π · Φ]` trivially maps to "rank-by-Φ". π_CQL's only non-trivial effect is soft-regularizing Φ's noisy rankings, which is useful when Φ is under-tuned but neutral or harmful when Φ is well-tuned. Predictor-quality (Φ-quality) remains the binding constraint at all stages of the pipeline. | **[`confirmed-null`](#verdict-labels)** on the day-2 policy-training-rescue hypothesis. **Arc-final**: all three critic arms (v0 cross-app window-level, v0.1 pair-level for pairs, v0.2 policy training against -Φ) close confirmed-null. The user's full value-function-from-existing-triples idea is comprehensively falsified at the available data granularity. | **Working policy-training apparatus is in place** for when a future Φ-quality improvement (per-rebal emission, richer features, end-to-end training) lifts Φ materially above the LR baseline. Drop-in path: re-train Φ with new data/features, then `uv run python apps/critic/scripts/run_policy_walkforward.py` reuses the same LOO harness. Non-trivial policy gains would require (1) different action space (multi-class horizons, Bernoulli gate, budget-aware portfolio loss); (2) different policy state (π reads more than Φ); (3) full actor-critic with realized-return reward instead of value-fitting on per-sample Sharpe scalars. None pre-registered. Driver: `apps/critic/scripts/run_policy_walkforward.py`. Artifacts: `Output/critic-policy-walkforward-summary.json`. Finding: extended in [`critic-phi-quality-v0`](findings/critic-phi-quality-v0.md) under "v0.2 — policy training against -Φ" + "Arc closure". |
| 2026-05-15 | vol | **Small-capacity illiquid-VRP re-frame — Stage-1 falsified upstream of spread (`TODO/vol-borrow-illiquid-vrp`)** (re-frame of the discarded `confirmed-OOS` vol signal: can a small operator, for whom illiquidity is a moat not a kill, harvest the unrestricted-universe VRP that [v2 #2](findings/vol-surface-v2-oi-restriction.md) showed collapses to −0.48 on top-200 OI, sharpened by securities-lending stress? Two cheap offline gates ran before the borrow-conditioning/breakeven-spread stages. **Gate A**: decompose the unrestricted edge into halt-artifact vs real VRP. **Gate B-pre**: what fraction of clean predicted picks are quote-available in DoltHub `option_chain` — the only free per-contract source.) | gauss314 full schema (`.iv-cache/data_IV_USA.csv`, 3.08M `(date,symbol)`, 2019-10-14→2023-07-28, 793 unique clean-pick symbols); cost side DoltHub `option_chain` (broad in time 2019-06→2025+, **narrow in universe**) | 5-window walk-forward 300/120/120; rebal 20d; v1 linear predictor + `forward_iv_rv_gap` | `edge_frac=(IV−RV)/IV` (short-straddle return-on-premium); halt/degen/clean population decomposition; quote-available fraction of clean predicted picks (symbol- & pick-weighted); mean `edge_frac` member vs absent | n/a (deterministic OLS per window; no hyperparam tuning) | **Gate A**: halt (RV<0.05) only **1.8%** of rows; predictor top-K edge FULL +0.2495 → CLEAN **+0.1408** (retains 56%); val r ≈ unchanged +0.118→+0.114 → the edge is **real VRP, not survivorship**. **Gate B-pre**: clean picks 12.0% symbol- / **7.5% pick-quote-available**; mean `edge_frac` quote-available **−0.0919** vs absent **+0.1987**. Direct probe: PLCE 36 contracts on 2022-06-17 vs CTIC/MRNS/MNOV = 0. | Quote-vendor coverage tracks liquidity for the same reason MMs arbitrate the VRP in liquid names — the edge cohort is structurally co-extensive with the *un-quoted* cohort. The 7.5% quote-available subset **is** the liquid cohort [v2 #2](findings/vol-surface-v2-oi-restriction.md) falsified, here re-derived from an orthogonal axis (quote-DB membership) with the edge sign-flipping negative exactly as v2 #2 predicted. **Double kill**: 92.5% un-priceable + negative edge on the priceable remainder → breakeven-spread gate is moot. | **[`reversed-OOS`](#verdict-labels)** for the small-capacity re-frame on free data (real signal, un-priceable cohort, negative edge on the quote-available subset). Parent arc verdict **unchanged** (`partial-OOS`, v3 liquid-regime-gated recipe; DCA stays canonical live). | Closes [`TODO/vol-borrow-illiquid-vrp`](TODO/vol-borrow-illiquid-vrp.md). Durable concept (quote-availability is a deployability gate upstream of transaction cost) → [notes.md](notes.md#quote-availability-is-a-deployability-gate). Drivers: `apps/vol/scripts/run_v0_artifact_decomp.py`, `apps/vol/scripts/run_v0_tradable_fraction.py`. Artifacts: `Output/vol-v0-{artifact-decomp,tradable-fraction}.json`. Finding: [`vol-borrow-illiquid-vrp-falsified`](findings/vol-borrow-illiquid-vrp-falsified.md). |
| 2026-05-16 | notebook | **Recursive compression of the causal CWT — linear (Kalman) vs nonlinear (GRU), compression vs prediction** (user-prompted arc on fixed-dim summaries of an unbounded series; does recurrence *linearity* bind the no-low-rank result?). Two diagnostics, identical 13-scale causal-CWT panel + rel-Frobenius/R² metric: a subspace-fit linear-Gaussian Kalman/LDS, and a numpy GRU recurrent autoencoder (linear emission, so only the recurrence nonlinearity differs). State dim `k ∈ {1..13}` swept; scored as causal `recon`, one-step `predict`, and a non-causal PCA linear upper bound. | AAPL single-ticker (Stooq archive) | n/a (reconstruction; no train/val split) | reconstruction R² / relative-Frobenius error vs recursive state dim `k` ; Sharpe N/A | n/a (diagnostic) | **GRU recon ≈ linear PCA to ~3 decimals at every `k`** (k=6 +0.701 vs +0.703; k=10 +0.910 vs +0.912; k=13 anchor +0.998 vs +1.000); **Kalman filtered ≡ batch PCA bit-identical** (memoryless filter, tiny fitted `R`); no cheap knee — near-lossless needs `k ≈ p = 13`; **`predict` ≪ `recon` under both model classes** (RNN k=8 0.735 vs 0.814; k=13 0.883 vs 0.998), gap widening with `k` ; Sharpe N/A [non-portfolio: CWT reconstruction R²] | n/a | [`diagnostic`](#verdict-labels) | Nonlinearity buys nothing for CWT reconstruction → the no-low-rank result is a property of the panel, not the linear model class; per-scale-z-scored cross-scale redundancy is *linear* and PCA extracts it optimally. Reconstructible dimensionality ≫ predictable dimensionality (sufficient-statistic point, measured twice). First GRU pass underfit (k=13 R² 0.786) and was rejected by the built-in `k≥p`→1 anchor; overlapping-window retune reaches 0.998, validating small-`k` rows. Scripts: `apps/notebook/src/ss_notebook/{kalman_cwt.py (ss-kalman-cwt), rnn_cwt.py (ss-rnn-cwt)}` (RNN imports the Kalman panel+metric for bit-comparability). Artifact: `Output/AAPL-rnn-cwt.png`. Next: task-coupled predictive-state (rank-IC loss, universe-scale, Modal/tinygrad) — does predictable structure saturate at `k≈3–4`? Finding: [`cwt-recursive-compression`](findings/cwt-recursive-compression.md). |
| 2026-05-15 | factor | **Target-side REINFORCE — higher-β sweep β ∈ {16, 32} (TODO/factor-reinforce-target-side followup)** (the partial-OOS default-next from the β∈{0,0.5,2,8} row above: the β=2 dip → β=8 +0.095 phase-transition suggested β>8 might clear the +0.10 PASS cut. Same loss/detach contract as the parent REINFORCE row. Run single-arm per β. **Real root-cause of the "drops all but last arm" issue documented**: NOT stdout-buffering — it is (a) an indentation bug (the `train_scorer_horizon_walkforward` call sits in the `for lam` loop, outside `for beta`, so multi-β trains only the last β) + (b) single-arm filename collapse to `horizon-mixture-a0-*` (prefix falls back when `len(reinforce_weights)==1`), so sequential jobs overwrite locally. β=16/32 run single-arm with provenance-verified rename keyed on the embedded `reinforce_weight`; code path bit-identical to the β=8 row for comparability.) | factor-narrow (297 stooq_us_long, min_history=6500) | 6-window walk-forward at h_min=5 fine grid (same as parent rows) | mean val net Sharpe per β ; Δ-fix vs **single-global**-best-fixed-h ; **per-window Δ vs per-window-best-fixed (stratified skill test)** ; π argmax shares ; w0/w3 per-window endog | n/a (deterministic train + eval; only β swept) | **β=16**: endog +0.449, best-fix h=10 +0.358, Δ-fix **+0.091**, 5/6 endog>0, H(π) 0.87 ; **β=32**: endog +0.463, best-fix h=40 +0.355, **Δ-fix +0.108**, 5/6 endog>0, H(π) 0.94, argmax h20:32%/h40:41%/h60:24%. **Stratified — the load-bearing result**: against the *per-window* hindsight-best fixed horizon the mixture is **0/6 wins** (β=32 per-window Δ: w0 −0.189, w1 0.000, w2 −0.017, w3 0.000, w4 −0.050, w5 0.000 → 3 ties, 3 losses, **0 wins**); β=16 identical pattern (0/6). | The headline Δ-fix benchmarks the mixture against **one** horizon held fixed across *all* 6 windows (h=40, +0.355). Against the per-window best fixed horizon the learned π **never wins** — ties at best, loses at worst. +0.108 exists only because no single horizon is best in every window, so a switching policy beats a *non-switching* one on the cross-window mean — and that mean is dominated by w0's structural negativity (every config + every fixed horizon is negative in w0). This is "switching ≥ committing on average", **not** state-conditional skill. **Retrospective**: the entire endogenous-horizon arc's positive Δ-fix (+0.048 2026-05-14 baseline, β=8 +0.095, β=32 +0.108) is the same single-global-fixed-benchmark artifact; the discrete mixture likely never extracted per-window skill. | **[`partial-OOS`](#verdict-labels)** — β=32 Δ-fix +0.108 clears the *literal* +0.10 PASS cut with 5/6 positive endog, but the stratified per-window test (0/6 wins vs per-window-best-fixed) shows the headline metric overstates skill. **Deliberately NOT promoted to `confirmed-OOS`** (user-adjudicated): a hollow PASS would be the repo's 10th confirmed-OOS in 146 runs and corrupt the 9/146 base rate the strategic frame rests on. Consistent with the β=8 MARGINAL precedent in the same arc. Supersedes the "may clear +0.10 PASS" expectation in the β∈{0,0.5,2,8} row's notes. | The **w0 regime gate (parent row's next-step #2) is now moot**: stratification shows 0/6 per-window wins (not a w0-only problem); gating w0 mechanically lifts the mean without creating skill — a misleading rescue, not run. Arc's binding conclusion: the discrete mixture-of-horizons-IC architecture does not extract state-conditional skill on factor-narrow; its positive Δ-fix is a benchmark artifact. Next lever is NOT more β / a w0 gate — it is output-side restructure (per-horizon specialized score heads) OR retire the mixture. Strategic steer (user): the higher-EV move is the novel-data arc, not more horizon variants. Driver: `apps/factor/scripts/modal/horizon_mixture.py --reinforce-weights '<β>'`. Artifacts: `Output/horizon-target-{beta16,beta32}-{windows.npz,comparison.png,summary.json}`. Finding: extended in [`factor-endogenous-horizon-mixture`](findings/factor-endogenous-horizon-mixture.md) under "Target-side REINFORCE — higher-β". |
| 2026-05-16 | notebook | **CWT sequence-bottleneck — explicit length compression + numéraire-invariance (AAPL vs AAPL/GLD)** (follow-up to the per-bar arc above: encode an `L=32`-bar CWT window to ONLY the GRU's final hidden state `h_L ∈ ℝ^k` and reconstruct the whole `(32,13)` = 416-number window from it; matched linear baseline = PCA on flattened windows; re-run on AAPL priced in gold to test whether the high-dimensionality is price-series-specific or representation-intrinsic). | AAPL & AAPL/GLD single-ticker (Stooq archive; GLD gold proxy, inner-aligned on dates) | n/a (reconstruction; non-overlapping 32-bar eval blocks; no train/val split) | window-reconstruction R² / relative-Frobenius vs bottleneck dim `k` (one `h_L` → whole 416-number window) ; Sharpe N/A | n/a (diagnostic) | **Harsh, no cheap knee**: R² ≈ linear in `k`; at `k=13` (32× squeeze) R² **+0.823** AAPL / **+0.822** AAPL/GLD ≈ 18% of windowed variance unreconstructed (≈42% rel-Frobenius) — vs the per-bar arc reaching +0.998 at `k=p`. GRU ≤ PCA-flat at every `k` (constrained causal encoder ≤ unconstrained linear). **Numéraire-invariant**: matched 250-epoch settings, GRU columns differ ≤0.015 R² anywhere, **identical (≤0.004) for `k ≥ 8`** (k=13 0.787 vs 0.785); only a marginal low-`k` (k=2–6) ~0.01–0.02 edge for plain AAPL that washes out by `k≥8` ; Sharpe N/A [non-portfolio: CWT reconstruction R²] | n/a | [`diagnostic`](#verdict-labels) | Length compression (one fixed `k`-vector for a whole `L`-bar window) is far lossier than per-bar width compression and has no knee; the recursive state *can* collapse the time axis but only lossily. Denominating in gold leaves the curve unchanged → high-dimensionality is intrinsic to the causal-CWT representation, not the price series. Mechanism: `log(AAPL/GLD)=logret(AAPL)−logret(GLD)`, a small weakly-correlated additive perturbation that nudges the low-`k` tail but doesn't restructure the panel. Supersedes the earlier smoke-vs-full mismatch (plain-AAPL re-run at matched 250 epochs/seed). New reusable `--vs TICKER` denominator + `--seq-bottleneck` arm in `apps/notebook/src/ss_notebook/rnn_cwt.py` (`ss-rnn-cwt`). Artifacts: `Output/AAPL{,-over-GLD}-rnn-cwt-seqbottleneck.png`. Finding: [`cwt-recursive-compression`](findings/cwt-recursive-compression.md#length-compression-sequence-bottleneck-numeraire-invariance). |
| 2026-05-17 | notebook | **CWT predict-arm persistence control — does the one-step latent learn anything?** (follow-up gating the recurring "use the predict latent as a word2vec-style embedding for relational selection" idea: benchmark the `predict` arm against the trivial lag-1 forecast `ŷ_{t+1}=y_t`. The CWT is a slowly-varying wavelet transform, so persistence is a strong naive baseline; the latent's *learned* contribution is `predict − persistence`.) | AAPL single-ticker (Stooq archive) | n/a (one-step-ahead self-prediction; no train/val split) | next-CWT-vector R² vs lag-1 persistence floor, per recurrent dim `k` | n/a (diagnostic) | **persistence (lag-1) R²=+0.843**; learned predict latent **−0.108 vs persistence at `k=8`** (+0.735) and only **+0.040 at `k=13`** (+0.883); recon for context +0.814 / +0.998 | n/a | [`diagnostic`](#verdict-labels) — **falsifies learned CWT-self-predictability** | The predict arm's apparent ~0.74–0.88 R² is overwhelmingly trivial autocorrelation: at a usable compressed `k=8` the latent is *worse* than copying yesterday's CWT, and at full rank adds only +0.04. Sharpens "reconstructible ≫ predictable" to its strong form — *marginal learned predictability of the CWT-self-continuation target ≈ 0*. Gates the word2vec/relational-embedding idea **by construction**: the predict latent ≈ a lag-1 copy of the CWT, so centroid/kNN/farthest geometry on it = geometry on the current CWT = the [`lie_test1`](findings/factor-indicator-baseline.md) (kNN-on-CWT IC≈0) + [`relational-dwt-failure`](findings/relational-dwt-failure.md) (4/4 scorers) null already on the board. Per the pre-registered staged rule the universe-scale geometry→return-IC build was **not run** (premise falsified upstream); the only live path is a *return-coupled* (not CWT-self-prediction) embedding. Baseline `_persistence` added to `apps/notebook/src/ss_notebook/rnn_cwt.py`. Finding: [`cwt-recursive-compression`](findings/cwt-recursive-compression.md). |
| 2026-05-17 | factor | **Return-coupled recurrent CWT embedding — rank-IC-trained GRU k-sweep (TODO/factor-cwt-return-coupled; the one live path from the cwt-recursive-compression arc)** (the pre-registered close-out: a GRU state over the 13-scale causal CWT trained **end-to-end** against `pearson_rank_ic` at H=20 — encoder *in* the autograd graph, not a frozen reservoir, so genuinely orthogonal to the prior frozen-geometry CWT nulls [`lie_test1`](findings/factor-indicator-baseline.md)/`lie_test4`/[`relational-dwt-failure`](findings/relational-dwt-failure.md). New joint per-window trainer `factor.cwt_gru_walkforward` — `train_scorer_walkforward` freezes the backbone so it cannot test a *trained* recurrence. Per window: fresh GRU(k)+linear head, trained on the train rebal slice only, frozen for val; per-scale standardisation fit train-only; L=32 causal CWT window ending at the rebal bar. Hyperparams fixed (no val tuning). The un-jitted 32-step BPTT timed out at 3h on Modal; `TinyJit`-per-window fixed it (k=2 4994s→1175s, bit-identical) — infra fix, no pre-reg knob moved.) | factor-narrow (297 stooq_us_long, min_history=6500) | 6-window walk-forward, train=63 / val=39 / step=39 blocks, rebal_days=20 (deterministic-indicator-baseline windowing) | mean val rank-IC per `k ∈ {2,4,8,13,16,32}` ; pos-window frac ; eval-only val Sharpe / IR-vs-EW ; pre-registered saturation test (k≤4 vs k=13) | n/a (deterministic; fixed hyperparams, only `k` swept) | **Every k below the +0.0120 indicator baseline**: k=2 **−0.0098** (pos 0.50), k=4 **+0.0063** (0.67), k=8 **+0.0063** (0.67), k=13 **−0.0038** (0.50), k=16 **+0.0003** (0.33), k=32 **+0.0057** (0.67). Max +0.0063 ≈ half the baseline, far below the +0.0140 positive cut; flat ≈0 band, no monotone-in-`k` trend, no low-`k` plateau. Eval-only val Sharpe ~+0.46–0.48 all k (≈ the universe's passive level), IR-vs-EW ≤ 0 every arm. | A *trained* recurrence over the causal CWT extracts no compact — or any — cross-sectional return statistic on factor-narrow: it cannot clear the deterministic-indicator baseline at **any** `k`, and the max-capacity k=32 (where the diagnostic's *reconstruction* was near-lossless) is no better than k=4. This closes the [`cwt-recursive-compression`](findings/cwt-recursive-compression.md) arc's last live sub-question (reconstruction ≫ self-prediction ≫ **return-prediction ≈ 0**) and re-derives, from the supervised-end-to-end direction, the same null the frozen-geometry CWT tests (`lie_test1`, `lie_test4` cwt t≈−0.98, `relational-dwt-failure` 4/4) reached: the binding constraint is the CWT feature class, not the representation move applied to it. | **[`confirmed-null`](#verdict-labels)** — pre-registered kill criterion fires mechanically (best k≤4 +0.0063 < +0.0140 cut; every arm ≤ +0.0120 baseline; no low-`k` plateau). Clean, no band-edge adjudication. | Closes [`TODO/factor-cwt-return-coupled`](TODO/factor-cwt-return-coupled.md) and the CWT-as-predictor question arc-wide. **Strategic implication (matches the standing frame + the [`factor-reinforce-target-side`](TODO/factor-reinforce-target-side.md) steer): stop applying cleverer models to standard CWT data — the next lever is a different feature class or novel data, not another CWT representation.** Provenance: 6 arms over 2 Modal invocations, same commit (`72802c5`), same pinned config, seed=0, arms independent by construction (k only sets GRU hidden dim, per-window fresh init): timing-probe run k∈{2,32}, completion run k∈{4,8,13,16}; merged. Driver: `apps/factor/scripts/modal/cwt_return_coupled.py`. Trainer: `factor.cwt_gru_walkforward.train_cwt_gru_walkforward`. Artifacts: `Output/cwt-return-coupled-{summary.json,windows.npz,ic-vs-k.png}`. Finding: [`cwt-recursive-compression`](findings/cwt-recursive-compression.md#return-coupled-embedding-the-arc-closure) ("Return-coupled embedding — the arc closure"). |
| 2026-05-18 | gate | **Dynamic-rebal-per-regime cadence oracle (Stage-0 kill gate)** — hindsight per-window best of EW rebal cadence over {daily, 5d, 20d, 60d, buy-hold} on the gate v0 EW substrate; pre-reg kill gate = oracle headroom over fixed daily-EW < +0.15 (a deployable "model chooses its wake time" selector cannot exceed its own hindsight oracle) | stooq_us_long | 6-window 1260/780/780 daily 2000-2025 | mean val Sharpe (daily-EW vs per-regime cadence-oracle) | n/a (hindsight oracle; deterministic re-slice) | daily-EW **+0.766** ; cadence-oracle **+0.769** ; **headroom +0.004** (kill gate +0.15, fails 37×) ; per-window Δ [0, 0, 0, +0.009, +0.005, +0.008] ; recon-vs-artifact unc_sharpe max-err 0.029 PASS | n/a | **[`confirmed-null`](#verdict-labels)** | Closes the dynamic-rebal lever (5th reframing of state-conditional selection: CFR / macro-gate / critic-Φ / factor-endogenous-horizon / this). Cadence is a non-binding knob on a diversified ~312-name basket and does not vary by regime; the +0.39 regime-table signal is strategy-selection, not execution cadence. Gross daily marginally best but net-negative vs slower under turnover cost (DCA quarterly is canonical). Orthogonal redirect: vol deployability (only large real signal; execution/data-bound). Driver `apps/gate/scripts/dynamic_rebal_cadence_oracle.py`; artifact `Output/dynamic-rebal-cadence-oracle.json`. Finding [`dynamic-rebal-cadence-null`](findings/dynamic-rebal-cadence-null.md). |
| 2026-05-18 | factor | **Short-horizon × (C,L) rep — indicator grid @ rebal_days=20 (within-sweep control + bind-check)** (pre-reg [`TODO/factor-shorthorizon-representation`](TODO/factor-shorthorizon-representation.md)) | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | +0.119 | **+0.0043 ; +0.253** | val−train −0.114 ; **4/6 pos** | [`diagnostic`](#verdict-labels) — within-sweep horizon-matched control; **anchor drift** | 20d indicator cell = +0.0043 vs the 2026-04-30 leaderboard anchor +0.0120 (5/6). 297/297 tickers built (not a universe bug); most plausible cause is the documented `IndicatorGridConfig` CCI-grid default change (in-code `(n=80,w=63)`→`(n=40,w=21)`, post-dating that row). Append-only anchor **not re-run**; the pre-registered rule is explicitly relative to *this within-sweep* cell so all 9 cells share one commit/harness/universe and the ΔIC comparison stays valid. Per-win val IC [0.002,−0.010,0.020,0.001,−0.009,0.022]. Decision metric is IC (commission-free) — cf. this page's closing commission-geometry note. Driver `apps/factor/scripts/modal/train_shorthorizon_repr.py` (`23b4b2c`); encoders `cl_encoders.py` (`202cb3d`). Artifacts `Output/sh-indicator-r20-windows.npz`, `Output/shorthorizon-summary.json`. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — indicator grid @ rebal_days=10 | factor-narrow | 6-window factor (10d) | mean val IC ; mean val Sharpe | +0.088 | **+0.0118 ; +0.620** | val−train −0.076 ; **6/6 pos** | [`confirmed-OOS`](#verdict-labels) (weaker arm; one window razor-thin) | The *existing* deterministic grid self-improves vs its own 20d control (+0.0043→+0.0118, ≈ the stale +0.0120 anchor), all 6 val windows positive — but w3 is +0.0003 (6/6 only marginally). Net Sh +0.620 already nets 10bps at 2× the 20d turnover. Per-win val IC [0.025,0.004,0.007,0.000,0.010,0.025]. Half the horizon-only effect; r5 is the strong arm. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | **Short-horizon × (C,L) rep — indicator grid @ rebal_days=5 (headline: the +0.012 ceiling was a horizon artifact)** | factor-narrow | 6-window factor (5d) | mean val IC ; mean val Sharpe | +0.071 | **+0.0212 ; +0.728** | val−train −0.050 ; **6/6 pos** (min +0.0095) | [`confirmed-OOS`](#verdict-labels) — robust; known-anomaly / cost caveat | The *existing* `IndicatorGridConfig` at a 5-day horizon = ≈5× the 20d control IC with **all 6 walk-forward windows positive** (per-win [0.043,0.029,0.017,0.010,0.013,0.016], monotone-decaying but never near 0). Net Sh +0.728 survives 10bps at 4× the 20d turnover — the commission-confound *headwind* (heavier drag at short cadence, per the 2026-05-04 quarterly `reversed-OOS` + this page's closing note) works *against* this number, so it is conservative. Caveats (→ adjacent test): short-horizon cross-sectional reversal is a well-studied/arbitraged anomaly; 5d turnover cost-sensitivity not stress-tested (commission fixed 10bps); per-window IC front-loaded. Refines the row-160 `factor-cwt-return-coupled` steer: a *different feature class* still isn't the lever — the unexamined binding free parameter was the **prediction horizon**, not the representation. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — spectral (truncated causal rFFT, C=4 signals × L=16 low-freq bins) @ rebal_days=20 | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | +0.139 | **−0.0035 ; +0.020** | ΔIC vs indicator@20 **−0.0078** ; 4/6 pos | [`confirmed-null`](#verdict-labels) | A non-CWT fixed (C,L) spectral encoder loses to the within-sweep control; per-win val IC sign-unstable [0.020,−0.032,0.043,0.006,−0.069,0.011]. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — spectral @ rebal_days=10 | factor-narrow | 6-window factor (10d) | mean val IC ; mean val Sharpe | +0.103 | **+0.0010 ; +0.435** | ΔIC vs indicator@10 **−0.0108** ; 4/6 pos | [`confirmed-null`](#verdict-labels) | Loses to the control at the same horizon; representation move adds nothing at 10d. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — spectral @ rebal_days=5 | factor-narrow | 6-window factor (5d) | mean val IC ; mean val Sharpe | +0.073 | **−0.0008 ; +0.280** | ΔIC vs indicator@5 **−0.0220** ; 2/6 pos | [`confirmed-null`](#verdict-labels) | Worst gap: where the indicator grid is strongest (+0.0212), the spectral encoder is ≈0/negative (median −0.0043). Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — MiniRocket (84 canonical kernels × L=4 dilations, PPV, bias=0) @ rebal_days=20 | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | +0.155 | **+0.0071 ; +0.565** | ΔIC vs indicator@20 **+0.0028** (< +0.005 bar) ; 5/6 pos | [`confirmed-null`](#verdict-labels) — best encoder cell, still fails the pre-reg bar | The single cell where a non-CWT encoder beats the (drifted) control, but ΔIC +0.0028 is below the pre-registered +0.005 outcome-1 bar and only at 20d (rule requires h=5 or 10). Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — MiniRocket @ rebal_days=10 | factor-narrow | 6-window factor (10d) | mean val IC ; mean val Sharpe | +0.109 | **+0.0039 ; +0.472** | ΔIC vs indicator@10 **−0.0079** ; 5/6 pos | [`confirmed-null`](#verdict-labels) | Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-18 | factor | Short-horizon × (C,L) rep — MiniRocket @ rebal_days=5 | factor-narrow | 6-window factor (5d) | mean val IC ; mean val Sharpe | +0.078 | **+0.0012 ; +0.336** | ΔIC vs indicator@5 **−0.0200** ; 4/6 pos | [`confirmed-null`](#verdict-labels) — closes the representation hypothesis | At the horizon where the cadence lever is strongest, the heaviest deterministic encoder adds nothing (+0.0012 vs grid +0.0212). With spectral, the **representation hypothesis is `confirmed-null` across 2 non-CWT (C,L) encoders × 3 horizons**; per pre-reg outcome-2 the gated learned conv-AE arm is **not built** (chasing another representation while cadence is the lever = the "stop testing variations of the same lever" anti-pattern). Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-19 | factor | **Short-horizon edge — skip-1 implementation-lag (microstructure control), indicator @ rebal_days=5** (pre-reg [`TODO/factor-shorthorizon-microstructure`](TODO/factor-shorthorizon-microstructure.md)) | factor-narrow | 6-window factor (5d) | mean val IC ; mean val Sharpe | skip-0 +0.0212 (reproduces 2026-05-18 to ±3e-5 — regression anchor) | **skip-1 +0.0114 ; +0.529** | ΔIC −0.0098 (retains 54%) ; **5/6 pos** | [`partial-OOS`](#verdict-labels) — part-real, part bid-ask-bounce | A 1-day execution lag (`forward_skip=1`: enter close(t+1), hold to t+1+H) ~halves the raw 5d IC (+0.0212→+0.0114) — ≈46% of the headline reversal IC was non-tradable same-bar mean reversion (bid-ask bounce). But it does **not** collapse: +0.0114 stays above the pre-registered reversed-kill (+0.0106) and the 20d skip-0 cell (+0.0043), 5/6 windows hold, net Sh +0.529 (after the lag *and* 10bps at 4× turnover) > 20d skip-0 +0.253. Lands in the pre-reg **partial** band (IC ∈ [+0.0106,+0.012), ≥4/6). Decay: skip-1 per-window val IC [0.026,0.013,0.013,0.006,−0.005,0.016] — front-loaded, w4 negative → the *recent* tradable edge is ~+0.005–0.016, marginal. Deployable only as a cost/turnover-controlled modest-IC signal, not the +0.0212 headline. skip-0 reproduced 2026-05-18 (+0.0043/+0.0118/+0.0212) to ±3e-5 → `forward_skip` change is bit-safe, prior rows stand. Driver `train_shorthorizon_repr.py --forward-skip-grid` (`c56e3fa`). Artifacts `Output/sh-indicator-r5-s{0,1}-windows.npz`. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md#microstructure--skip-1-follow-up). |
| 2026-05-19 | factor | Short-horizon edge — skip-1, indicator @ rebal_days=10 | factor-narrow | 6-window factor (10d) | mean val IC ; mean val Sharpe | skip-0 +0.0118 (=2026-05-18 ±3e-5) | **skip-1 +0.0076 ; +0.562** | ΔIC −0.0042 (retains 64%) ; 4/6 pos | [`partial-OOS`](#verdict-labels) | More skip-robust in *fraction* than 5d (64% vs 54%, as the pre-reg predicted — longer horizon, smaller microstructure share) but lower *absolute* skip-1 IC (+0.0076) and consistency degrades to 4/6. 5d stays the strongest tradable horizon in absolute IC post-lag; neither collapses, so the short-horizon signal is not *entirely* microstructure. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md#microstructure--skip-1-follow-up). |
| 2026-05-19 | factor | Short-horizon edge — skip-1, indicator @ rebal_days=20 (lag-control context) | factor-narrow | 6-window factor | mean val IC ; mean val Sharpe | skip-0 +0.0043 (=2026-05-18 ±3e-5) | **skip-1 +0.0014 ; +0.369** | ΔIC −0.0029 ; 3/6 pos | [`confirmed-null`](#verdict-labels) | The 20d cell was already weak/drifted (+0.0043); under a 1-day lag it is indistinguishable from noise (3/6). 20d was never the edge — included only as the lag-control context. Finding [`factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md#microstructure--skip-1-follow-up). |
| 2026-05-22 | lie | **Shape-kNN 1mo-reversal as a market-neutral long/short** (IC→portfolio translation test; per-ticker shape-feature kNN scores → dollar-neutral L/S via `factor.block_port_returns_long_short_np`, 10bps + 50bps/yr borrow) | phase-2 (21) | single split (train→2021-03, test 2021-2025; 57 non-overlapping 21d blocks) | cross-sectional IC t ; L/S ann Sharpe ; deflated-Sharpe t | n/a (kNN; IC on test) | **IC t=+3.75 (1178 dates, reproduces v3/v4)** but **L/S ann Sharpe −0.018 ; deflated t −1.55 (DSR 0.06, n_trials=9)** | the +3.75 IC does NOT translate to a deployable book | [`confirmed-null`](#verdict-labels) (on the portfolio; the IC itself is real) | The IC's t-stat borrows power from 1178 daily-overlapping dates; the harvestable book has 57 non-overlapping monthly realizations, and at **21-name breadth** the fundamental law (IR≈IC·√breadth·TC) gives too thin a top-minus-bottom spread to clear costs+borrow. Same IC-doesn't-fully-translate pattern as the factor short-horizon reversal (2026-05-19, partly bid-ask bounce). **Next experiment:** re-run on a wider liquid universe (factor-narrow 297 → √breadth ≈ 3.8× higher) — if the L/S deflated t clears 0 there, breadth was the binding constraint; if not, the signal is non-harvestable. Driver `apps/lie/scripts/shape_knn_longshort.py`; on the [Cross-arc DSR ladder](#cross-arc-deflated-sharpe-ranking). Finding [`deflated-sharpe-leaderboard`](findings/deflated-sharpe-leaderboard.md). |
| 2026-05-22 | lie | **Shape-kNN 1mo-reversal long/short — breadth expansion** (the pre-registered next-test from the row above: identical signal on a wider liquid universe to see if √breadth ≈ 3.8× rescues it) | factor-narrow (297) | single split (train→2021, test→2026; 94 non-overlapping 21d blocks) | cross-sectional IC t ; L/S ann Sharpe ; deflated-Sharpe t | n/a (kNN; IC on test) | **IC reverses to t=−0.81** (mean −0.0059, 94 rebal dates) ; **L/S ann Sharpe −0.703 ; deflated t −3.45 (DSR 0.000)** | breadth did NOT rescue it — signal vanishes/reverses | [`reversed-OOS`](#verdict-labels) | **Breadth hypothesis falsified.** The +3.75 IC was **Phase-2 mega-cap-specific** (cf. [relational universe-shift](findings/relational-universe-shift.md)) — on the heterogeneous 297-name universe the shape-kNN neighbor pool no longer carries the 1mo-reversal premium and the cross-sectional IC reverses to −0.81. As a deployable book it is worse than at 21 names (−3.45 vs −1.55). **Closes the shape-kNN spread-trade line:** the signal is small, universe-specific, and non-harvestable. (Parallelized 8-worker kNN: 1.35M train × 27.9k queries in 88s — `apps/lie/scripts/modal/shape_knn_longshort_modal.py`.) Finding [`deflated-sharpe-leaderboard`](findings/deflated-sharpe-leaderboard.md). |
| 2026-05-22 | factor | **5d skip-1 indicator signal as a deployable book — DSR test** (the "wire the +0.0212 5d signal into a market-neutral long/short" candidate; uses the microstructure-controlled skip-1 stream, pooled across windows, 10bps×2 + 50bps/yr borrow) | factor-narrow (297) | 6-window factor (5d), skip-1 | pooled-stream ann Sharpe ; deflated-Sharpe t (focused n=8 / conservative n=50) | reproduces 2026-05-19: mean val IC +0.0114 ; mean val Sh +0.529 (mean-of-windows) | **L/S pooled Sharpe −0.45, deflated t −3.64/−4.52** ; **LO pooled Sharpe +0.357 but skew +9.6/kurt 218, deflated t +0.10/−0.91** | neither construction is skill | [`confirmed-null`](#verdict-labels) | The market-neutral **long/short is firmly negative** (short leg doesn't pay — IC predicts top-outperformance not bottom-underperformance — and 5d doubles turnover so commission dominates; same pattern as factor-LS −3.07). The **long-only** book's +0.357 raw Sharpe is a **fat-tail mirage**: one 5d block returns +92.8% (a single small-name moonshot); ex-that-block Sharpe falls to +0.272, and the PSR moment correction pushes deflated t to +0.10 (focused, coin-flip) / −0.91 (conservative). The 5d signal's IC is real, but **no deployable construction clears a confident skill bar**. Closes the "wire the 5d signal into a spread/long-short" line; DCA stays the only arc with deflated t > 2. Driver `apps/factor/scripts/modal/train_shorthorizon_repr.py --rebal-days-grid 5 --forward-skip-grid 0,1`; on the [Cross-arc DSR ladder](#cross-arc-deflated-sharpe-ranking). Finding [`deflated-sharpe-leaderboard`](findings/deflated-sharpe-leaderboard.md). |
| 2026-05-22 | dca | **Regime-scaled DCA — monetize the aggregate regime signal on the live basket** (scale DCA exposure by predicted aggregate risk; 3 arms — passive / vol-target Moreira-Muir / walk-forward predicted-drawdown gate — on the 13-ETF Phase-4d basket, 10bps overlay turnover) | multi-asset ETFs (13) | full span + dd-gate val-span (matched), n_trials=6 | deflated-Sharpe t (arm vs passive) ; alpha (arm − passive Sharpe) | passive deflated t +1.83 full / +1.98 val (n_trials=6 baseline) | vol-target +1.91 (alpha **+0.019** full, **−0.063 val** — sign flips) ; dd-gate val +2.07 (alpha **+0.020**, avg exp 0.77) | no arm clears passive by more than noise | [`confirmed-null`](#verdict-labels) | **The aggregate regime signal is real but NOT monetizable on DCA.** Both overlays add alpha of only +0.02 — inside the gate arc's own ±0.10 noise band — and vol-target *reverses sign* between full (+0.019) and val (−0.063) spans, the signature of noise. dd-gate de-risks to 77% avg exposure for +0.02 alpha (pays turnover for nothing). Deflated-t clusters at +1.8–2.1 across all arms, statistically indistinguishable. Consistent with [gate v0](findings/gate-drawdown-v0.md) (partial-OOS, alpha +0.067 within noise) → shrinks to +0.02 on the real basket. **Closes the regime-overlay-on-DCA line: passive DCA is the final answer.** Driver `apps/dca/scripts/regime_scaled_dca.py`; artifact `Output/regime-scaled-dca-{returns.npz,summary.json}`. Finding [`deflated-sharpe-leaderboard`](findings/deflated-sharpe-leaderboard.md). |
| 2026-05-22 | factor | **12-1 cross-sectional momentum — the canonical durable anomaly the repo never tested at its horizon** (pre-registered single hypothesis, NOT an Optuna sweep; score = cum log-ret [t−252,t−21] skip-1 per Jegadeesh-Titman; dollar-neutral L/S, monthly non-overlapping, 10bps/leg + 50bps/yr borrow) | factor-narrow (297) | full point-in-time history, 302 monthly blocks (fixed rule, no train/val) | ann Sharpe ; PSR deflated-t (n=1 pre-reg) ; deflated-t (n=6 term-structure ladder) ; per-chunk consistency | n/a (unsupervised fixed rule) | **ann Sharpe +0.081 ; PSR t +0.40 (DSR 0.66) ; ladder t −0.88 (DSR 0.19) ; 3/6 chunks positive** | weak/non-significant net of costs + crash | [`confirmed-null`](#verdict-labels) | **The field's most durable cross-sectional anomaly is marginal-at-best here.** The academic ~0.5–1.0 gross momentum premium is eaten by costs + the 2008-09 momentum crash (skew −1.09, kurt 8.15, the −0.30 chunk). Even the most generous pre-registered framing (n=1, no selection penalty) gives deflated-t +0.40 — positive but far below significance (DSR 0.66 vs ~0.95 bar); the ladder framing goes negative. **Critically: this validates that the repo's "cross-section is bound" conclusion was not a bad-search artifact** — even the canonical survivor doesn't clear the bar on this liquid US universe at realistic friction. (Per the search-based literature — Sullivan-Timmermann-White / Harvey-Liu — this was run pre-registered, not as an Optuna horizon-sweep, to avoid manufacturing a winner.) Untested variants that could differ: value-weighted, crash-managed (Daniel-Moskowitz dynamic momentum), or a broader/longer universe. Driver `apps/factor/scripts/momentum_12_1.py`; on the [Cross-arc DSR ladder](#cross-arc-deflated-sharpe-ranking). Finding [`deflated-sharpe-leaderboard`](findings/deflated-sharpe-leaderboard.md). |
| 2026-05-22 | factor | **Low-volatility / BAB cross-sectional factor — the structural anti-edge ("short what naive money overpays for")** (pre-registered single hypothesis; score = −trailing-252d realized vol; long low-vol / short high-vol, dollar-neutral, monthly, 10bps/leg + 50bps/yr borrow) | factor-narrow (297) | full point-in-time history, 302 monthly blocks (fixed rule, no train/val) | ann Sharpe ; PSR deflated-t (n=1) ; deflated-t (n=6 factor-family) ; per-chunk consistency | n/a (unsupervised fixed rule) | **ann Sharpe +0.128 ; PSR t +0.64 (DSR 0.74) ; family t −0.66 (DSR 0.26) ; 3/6 chunks positive** | best cross-sectional arm, still sub-significant; recent decay | [`confirmed-null`](#verdict-labels) | The honest structural form of "invert the reliably-bad investments" — shorting the high-vol/lottery names naive demand overpays for (Baker-Bradley-Wurgler / Frazzini-Pedersen BAB), where the badness is genuine negative *gross* alpha (survives the inversion cost-algebra `r_inv = −r − 2c`). **Marginally the best cross-sectional arm tested** (deflated-t +0.64 at n=1 vs momentum +0.40) but still below significance (DSR 0.74 vs ~0.95), and goes negative under family deflation. **Last two 6-year chunks negative** — the documented post-2018 low-vol *crowding/decay* (smart-beta inflows). Skew ~0 (the dollar-neutral short-beta tilt helps in crashes, so no crash signature). **CAVEAT:** dollar-neutral carries a negative net-beta tilt; true beta-neutral BAB (lever legs to neutralize) is the untested follow-up, but a +0.13 Sharpe with recent decay is unlikely to clear significance even beta-neutralized. Reinforces the load-bearing null: **both durable cross-sectional survivors (momentum, low-vol) are marginal-at-best on this liquid US universe net of costs.** Driver `apps/factor/scripts/low_vol_bab.py`; on the [Cross-arc DSR ladder](#cross-arc-deflated-sharpe-ranking). Finding [`deflated-sharpe-leaderboard`](findings/deflated-sharpe-leaderboard.md). |
| 2026-05-22 | gate | **Gate v1 — nonlinear predictor + cross-sectional dispersion features** (attacks the predictor-bound v0 per the +0.387 oracle ceiling: swap OLS for MLP / HistGradientBoosting, add price-derived cross-sectional return-std + breadth-above-200dma features) | stooq_us_long (312) | 6-window gate (1260/780/780, h=20, q=0.95) | mean alpha (gated vs unc EW) ; positive-α windows | n/a (per-window fit) | **OLS+disp +0.040 (2/6) ; MLP −0.118 (0/6) ; HGB −0.063 (2/6)** — all **worse than v0** (+0.067, 4/6) | nonlinear heads overfit; dispersion features add noise | [`confirmed-null`](#verdict-labels) | The oracle headroom (+0.387, v0 captures 17%) is **real but not capturable** by these predictor upgrades: MLP/HGB **overfit** the rare-drawdown target on 1260-bar windows (the documented risk), and the 2 added dispersion features *hurt* even OLS (+0.040/2-6 vs v0 +0.067/4-6). The gate is predictor-bound, but a more-flexible predictor on this sample makes it worse, not better. v0 10-feature OLS @ q=0.95 stays the gate's best (still marginal). Overturns the gap-audit's optimistic ~35% prior. Driver `apps/gate/scripts/run_walkforward_v1.py`; artifact `Output/gate-v1-summary.json`. |
| 2026-05-22 | macro | **Continuous VIX-percentile sigmoid meta-gate** (replaces macro v1b's binary VIX-above-median window gate with exposure=sigmoid((VIX-pct−0.5)·slope) on the 17 pivot-app v0 windows) | gate 6 + pairs 6 + vol 5 = 17 windows | post-process of v0 per-window alphas (no retrain) | mean alpha (gated) vs ungated + binary v1b ; positive-α windows | n/a | **ungated +0.085 (13/17) ; binary v1b +0.075 (4/17) ; continuous slope=10 +0.095 (13/17)** | continuous beats binary but **not ungated** | [`confirmed-null`](#verdict-labels) | The continuous gate nominally beats the binary v1b by +0.021 — but only because the **binary cliff over-suspends** (drops below ungated). Against the meaningful baseline (ungated +0.085) the continuous gate adds **+0.010** — noise at n=17. So the VIX meta-gate, binary or continuous, **adds nothing over ungated deployment**; the continuous form merely repairs the binary's self-inflicted damage. Consistent with [regime-scaled DCA](findings/deflated-sharpe-leaderboard.md) and [`cfr-macro-gate-final`](findings/cfr-macro-gate-final.md) (memory-heavy gates fight regime transitions). Driver `apps/gate/scripts/macro_meta_gate_continuous.py`; artifact `Output/macro-meta-gate-continuous.json`. |
| 2026-05-22 | vol | **B1 Phase A — borrow-stress conditioning on the locked v3 recipe** (Stage-0 PASS → full FINRA short-vol + SEC FTD composite; v3 recipe unchanged + borrow-tercile split of the 50 picks per fired rebal) | gauss314 → OI-top-200 | 5-window vol (300/120/120, 20d), 11 fired rebals | H1 per-tercile alpha ; H2 per-tercile worst-rebal ; H3 conditioned net Sharpe vs unconditioned v3 | n/a | **H1 HOLDS strong** (lo +0.002, hi **+0.042**; hi-tercile in-sample Sharpe **+3.80** vs uncond +2.01) ; **H2 squeeze-tail FALSE** (hi worst −0.026 vs lo −0.108) ; **H3 pre-reg drop-hi FAILS −2.17** | mechanism real but direction flipped vs pre-reg | [`partial-OOS`](#verdict-labels) | **The borrow-stress mechanism is real and strong, but the scoping page's directional assumption is backwards.** High borrow-stress = **rich-AND-safe** (richest VRP +0.042, *best* tail −0.026), not the squeeze-tail-to-avoid the page assumed — so the pre-registered conditioned rule (drop high-stress) **discards the alpha** and fails (−2.17). The implied rule is the inverse (overweight high-stress: in-sample hi-tercile Sharpe +3.80). **CRITICAL DISCIPLINE:** that flip is read off 11 in-sample fired rebals and post-hoc — exactly the Sullivan-Timmermann-White data-snooping trap. It is a *hypothesis for OOS*, not a result. Phase B (pre-registered **overweight-hi** on never-seen DoltHub 2024-26) is the only legitimate confirmation; if it doesn't hold OOS, H1 was an 11-rebal mirage. Driver `apps/vol/scripts/{prep_borrow_data,run_walkforward_v3_borrow}.py`; artifacts `Output/vol-borrow-{stage0,phaseA}.json`. TODO [`vol-borrow-liquid-universe`](TODO/vol-borrow-liquid-universe.md). |
| 2026-05-22 | vol | **B1 Phase B — OOS confirmation of the borrow premium-amplifier (closes the arc)** (pre-registered test of Phase A's flipped/in-sample discovery: does high-borrow-stress carry richer realized VRP on never-seen DoltHub 2024-26?) | DoltHub `volatility_history` weekly, OOS 2023-08→2026-04, liquid cohort = gauss314 top-200-OI union (327 names) | 552 weekly snapshots / 11 quarters; iv_rv_gap = iv_current − forward-20d realized vol (from prices, not DoltHub hv) | hi-stress vs lo-stress mean realized VRP ; hi>lo quarter-fraction | n/a (OOS) | **hi-stress VRP +0.011 ≤ lo-stress +0.014 ; hi>lo in 36% of quarters** (pre-reg PASS needed hi>lo AND ≥60%) | the in-sample +3.80 does NOT hold OOS | [`reversed-OOS`](#verdict-labels) — **borrow leg closes** | The decisive falsification. Phase A's hi-tercile in-sample Sharpe +3.80 (11 fired rebals) **does not replicate** on 552 OOS weeks — high-borrow-stress carries *marginally lower*, not richer, VRP out-of-sample, and beats low-stress in only 36% of quarters. The in-sample signal was selection on 11 rebals (the STW trap, flagged at Phase A). **The disciplined process worked: the spectacular in-sample signal was OOS-tested before any deployment claim and killed.** Closes the B1 borrow arc and the free-borrow-proxy novel-data leg (the illiquid leg died on coverage; this liquid leg passes coverage but fails OOS confirmation). DCA stays the only deflated-t-confident edge. (Caught + fixed a units bug mid-run — DoltHub IV is decimal vs my percent forward-vol; the corrected magnitudes are realistic and the verdict is unchanged. Caveat: DoltHub weekly/decimal substrate ≠ gauss314 daily, but the OOS sample is 50× larger than Phase A and the mechanism should be substrate-agnostic.) Driver `apps/vol/scripts/run_borrow_phaseB_oos.py`; artifact `Output/vol-borrow-phaseB.json`. Finding [`vol-borrow-liquid-universe`](findings/vol-borrow-liquid-universe.md). |

| 2026-05-23 | vol | **v3 architecture re-run on DoltHub 2024-26 OOS — does the +1.15 signal and ρ≈0 with DCA both replicate?** (predictor = v2-dolthub-oos 4-feature OLS trained 2019-10→2023-07; VIX 126d-median regime gate identical to v3; top-K=100 per 20-trading-day non-overlapping rebal; honest forward-RV from Stooq prices; pre-reg gates: fired Sh ≥ +0.30, fire-rate ∈ [20%,80%], ≥60% pos quarters, \|ρ(full_panel, DCA-block)\| ≤ 0.15) | DoltHub `volatility_history` 2023-08→2026-04, ~3K names/day | **33 non-overlapping 20-trading-day rebals**, single-split train→OOS | pooled fired-α Sharpe ; full-panel α Sharpe ; deflated-t (n_trials=12) ; ρ with DCA-block ; pos-qtr count | n/a (single split; predictor frozen on train) | **fired-α Sharpe +6.18 ; full-panel +2.82 ; deflated-t +5.55 ; 10/11 quarters positive (91%) ; ρ(full, DCA) = +0.276** | signal replicates AND grows 2-4× (vs gauss314 +1.15 Sh / t +1.32) but pre-reg ρ gate FAILS, regime-tailwind caveat | [`partial-OOS`](#verdict-labels) — **MARGINAL** (3/4 pre-reg gates pass) | **Read this row with the regime caveat front-and-center.** The OOS sample is 2023-08 → 2026-04: post-COVID calm-bull, VIX averaged ~15, no vol crisis. iv_current > forward 20d RV was a structural windfall. The original gauss314 sample (2019-23, +1.15 Sharpe) covered 2020 COVID + 2022 Fed-pivot — the +6.18 fired Sharpe on DoltHub is partly the calm regime, not a 20-year-applicable read. ρ with DCA is **+0.276** (date-aligned 29 blocks), not the ≈0 the gauss314-tail proxy suggested — diversification still meaningful (much better than relational +0.79) but weaker than claimed. DCA + vol×3 ensemble on the date-aligned overlap: deflated-t **+5.35** (vs DCA-overlap solo +1.39, vs DCA-full-daily +1.93). **The strongest empirical case yet for building `ss-vol live` (options-broker integration)** — *if* you believe the gate still fires in crises (it did in gauss314's 2020/2022 windows; need to confirm post-COVID). The next vol crisis is the binding falsification test; this sample doesn't contain one. Cost: `commission_bps=0` in the dump — options-broker frictions will reduce deployable Sharpe materially. **First-publication correction (2026-05-23)**: the initial dump used `val_dates[::4]` which collapsed to ~5-trading-day cadence in the daily-2024+ portion of the OOS span and overlapped the 20-day forward-RV window 4×, inflating deflated-t to +11.21 by double-counting independence. Corrected to step every 20 trading days (n=138 → n=33); per-period Sharpe is essentially unchanged, the lift is in obs-independence not signal magnitude. Driver `apps/vol/scripts/run_walkforward_v3_dolthub_oos.py`; artifacts `Output/vol-v3-dolthub-oos-{returns.npz,summary.json}`; on the [Cross-arc DSR ladder](#cross-arc-deflated-sharpe-ranking) at rank #1. Finding [`vol-v3-dolthub-oos`](findings/vol-v3-dolthub-oos.md). |
| 2026-05-23 | dca | **DCA basket Optuna search — does any 13-ETF-alternative materially beat canonical?** (pre-registered bucket search over 3,600 combinations of {equity_core × intl × bonds × commodities × REITs × rebal_cadence × drift_threshold}; N_TRIALS=200 TPE; train 2005-2018 / val 2019-2025 includes 2020+2022 crises; objective = train-period deflated-t under workspace sharpe_std_ann=0.25; pre-reg bar locked in commit d49c672 BEFORE eval: Δ val deflated-t > +1.0 = confirmed-OOS, > 0 = partial-OOS, ≤ 0 = confirmed-null, AND val max-DD ≤ canonical + 5pp) | 22 ETF candidates (19 with 2005-on coverage after dropping DBC/USO/VEU) | walk-forward single split; same-method canonical comparison | val deflated-t (winner vs canonical) ; val max-DD diff ; n holdings | n/a (held-out OOS slice never seen during Optuna search) | **canonical-12 val: annSh +0.854, defl-t +0.419, maxDD −0.239 ; winner val (VTI+TLT+IEF+GLD): annSh +0.858, defl-t +0.436, maxDD −0.222** ; **Δ deflated-t = +0.016** | far below the +1.0 confirmed-OOS bar | [`partial-OOS`](#verdict-labels) — technically positive lift, semantically `confirmed-null` (within noise) | **The pre-reg called this exactly**: confirmed-null or weak partial-OOS expected; got +0.016 lift. Canonical basket is defensible — Optuna couldn't find a defensible alpha improvement over 200 trials × 3,600-combo space. **One operational note**: the winner is a *simpler* basket (4 names: VTI+TLT+IEF+GLD vs canonical's 12) achieving near-identical val Sharpe (+0.858 vs +0.854) with marginally better val max-DD (−22.2% vs −23.9%). This is "same alpha, fewer moving parts" — operational simplification, not alpha lift. The 9 SPDR sectors EW ≈ VTI in exposure; replacing them collapses friction without losing diversification. Deploying either canonical-13 or winner-4 is equally defensible; the search proved that. Driver `apps/dca/scripts/optuna_basket_search.py`; artifact `Output/dca-basket-optuna.json`. Finding [`dca-basket-optuna`](findings/dca-basket-optuna.md). Pre-reg [`TODO/dca-basket-optuna`](TODO/dca-basket-optuna.md). |
| 2026-05-23 | dca | **DCA × vol overlay joint Optuna search — does any (basket × vega_scale) combination materially beat the canonical 13-ETF + vol×3 ensemble?** (pre-registered joint search over 16,800 (basket buckets × vega_scale ∈ {0, 0.25, 0.5, 1, 2, 3, 5}) combinations; N_TRIALS=200 TPE; train rebal 0-19 / val 20-32 of the 33-obs vol-v3-DoltHub OOS sample, periods_per_year=12.6, sharpe_std_ann=0.25; pre-reg bar locked in commit 1a06966 BEFORE eval: Δ val deflated-t > +1.0 AND val maxDD ≤ canon +5pp = confirmed-OOS; capital-free-overlay semantics `r_ens = r_dca + vega × r_vol_alpha`) | 22 ETF candidates (DCA cash leg) × frozen vol-v3 stream (`Output/vol-v3-dolthub-oos-returns.npz`, 33 non-overlapping 20d rebals 2023-08 → 2026-03) | walk-forward single split on the 33-obs vol stream; same-method canonical comparison | val deflated-t (winner vs canonical-13 + vol×3) ; val max-DD diff ; n holdings | n/a (held-out OOS slice never seen during Optuna search) | **canonical-13 + vol×3 val (n=13): annSh +2.282, defl-t +4.083, maxDD −0.010 ; winner val (SPY+GLD+vol×3, 80d rebal): annSh +2.769, defl-t +4.695, maxDD −0.008** ; **Δ deflated-t = +0.612** ; **8 of top-10 trials = exact same config (SPY+GLD+vega=3.0+80d); all top-10 carry vega_scale=3.0** | positive but below the +1.0 confirmed-OOS bar | [`partial-OOS`](#verdict-labels) — Δ deflated-t positive, basket axis below noise floor at n_val=13; canonical recipe defensible | **The pre-reg called this**: partial-OOS or confirmed-null expected; got +0.612 lift, below the +1.0 bar. **The load-bearing finding is the vega_scale=3.0 robustness** — every top-10 trial picks it; TPE collapsed onto that scale across all basket variations. The basket axis is *structurally below the noise floor* at n_train=20 / n_val=13: the vol overlay × 3.0 dominates the ensemble variance budget, the DCA leg contributes ~+0.20-0.40 Sharpe of the +2.77 total. **This is the joint-search version of the basket-only arc's finding**: a simpler basket (SPY+GLD, 2 ETFs) ties canonical-13 on deflated-t — complexity in the cash leg is replaceable without alpha loss. Caveats: 13 val obs is thin and the sample is the vol-v3 regime-tailwind calm-bull window (no vol crisis); both winner and canonical numbers are inflated relative to crisis-inclusive expectations. **Vol-side hyperparams (predictor refit window, gate lookback=126d, top-K=100, gate threshold = VIX rolling median) were NOT swept — frozen at v3 recipe upstream**; only the cash-leg basket and the overlay-sizing knob (vega_scale) varied. **Operational read**: canonical 13-ETF + vol×3 stays defensible; do NOT deploy the joint-search winner over canonical (search proved equivalence under the bar, not superiority). Driver `apps/dca/scripts/optuna_dca_vol_ensemble.py`; artifact `Output/dca-vol-ensemble-optuna.json`. Finding [`dca-vol-ensemble-optuna`](findings/dca-vol-ensemble-optuna.md). Pre-reg [`TODO/dca-vol-ensemble-optuna`](TODO/dca-vol-ensemble-optuna.md). |
| 2026-05-23 | factor | **Factor head trained against studentized Sharpe-diff loss — pre-reg `confirmed-null`** (first user of the differentiable Lo/Bailey-LdP studentized t-stat loss landed in `cb8f84e`; head-to-head against `ir_vs_ew` baseline + `rank_ic` reference on factor-narrow 5d windows; 200/100/100 train/val/step blocks, n_steps=200, seed=42; pre-reg locked at `fdab384` BEFORE eval) | factor-narrow (310 names @ min_history_bars=6500) | 10 walk-forward windows, 5d cadence, forward_skip=1 | mean val IC ; per-period t-stat ; pooled OOS bootstrap CI ann ΔSR | n/a (held-out OOS) | **candidate t=+0.69 (CI [−0.26, +0.73], includes 0) ; baseline `ir_vs_ew` t=+1.29 (CI [+0.02, +0.91], EXCLUDES 0) ; reference rank_ic t=+0.87** ; **Δt = −0.60** (candidate WORSE than baseline) | candidate fails to beat the literal-canonical Sharpe-aligned loss; surprise side-result is that `ir_vs_ew` is the only arm with a 95%-CI-excluding-zero result | [`confirmed-null`](#verdict-labels) (per locked pre-reg bar) | Pre-reg's "honest expectation = confirmed-null" came true with margin. The candidate's loss surface has a stability quirk (window-0 NaN — Lo-Mertens delta-method s.e. collapses at near-uniform softmax init; future use should add a stability floor or use `with_moments=True`). The methodology-rewrite finding that "no arc beats DCA at 95%" is reinforced one layer deeper: even direct gradient training against the literature-canonical analogue of the test can't extract alpha the cross-section doesn't carry. **The surprise positive — `ir_vs_ew` at +0.46 ann ΔSR vs zero CI [+0.02, +0.91]** — is worth following up; Ledoit-Wolf CI vs DCA on this stream specifically would clarify whether it's a real beat-DCA candidate or just beat-zero. Driver: `apps/factor/scripts/modal/train_studentized_sharpe_diff.py` (Modal T4, ~8 min); verdict: `apps/factor/scripts/verdict_studentized_sharpe_diff.py`. Artifact: `Output/factor-stud-sh-diff-{rank_ic,ir_vs_ew,studentized_sharpe_diff_vs_ew}-windows.npz`, `factor-studentized-sharpe-diff-verdict.json`. Finding [`factor-studentized-sharpe-diff-loss`](findings/factor-studentized-sharpe-diff-loss.md). |
| 2026-05-23 | meta | **DSR Steps 2-5 — full methodology rewrite landed** (Step 2: vol-v3 realistic 200bps options friction → ann Sharpe +2.82 → +2.41, deflated-t +4.08 → +3.43; Step 3: per-arc-class sharpe_std re-measurement DATA-BLOCKED — Optuna studies only persist top-10 trials; kept workspace 0.072 default and documented; Step 4: Ledoit-Wolf studentized stationary-bootstrap CI on Sharpe-difference vs DCA-canonical added as new primary cross-arc column; Step 5: stratified ladder by (arc class × sample structure × universe) — 7 strata, cross-stratum DSR-rank explicitly NOT meaningful) | full cross-arc ladder | n/a (methodology rewrite, no new run) | Ledoit-Wolf ΔSR vs DCA (95% CI); DSR within-stratum | n/a | **ZERO arcs beat DCA under Ledoit-Wolf CI**: vol-v3 c=200bps ΔSR=+1.17 [−0.38, +3.06] includes 0 ; vol-v3 c=0bps ΔSR=+1.50 [−0.16, +3.45] includes 0 ; relational +0.24 [−0.32, +0.80] includes 0 ; basket-winner-4etf +0.17 [−0.39, +0.73] includes 0 ; gate-v0 +0.21 [−0.38, +0.82] includes 0. **FOUR arcs significantly worse than DCA**: momentum-12-1 ΔSR=−0.74 [−1.28, −0.12]; factor-LS-baseline −0.84 [−1.45, −0.11]; factor-5d-LS −1.09 [−1.77, −0.44]; lie-wide −1.62 [−2.59, −0.57] | the entire "above DCA" cluster on the prior ladder was statistically noise; the apparent top-of-ladder was an artifact of using DSR magnitude as a cross-arc rank key | [`diagnostic`](#verdict-labels) — methodology rewrite | The most consequential finding: under the literature-canonical apples-to-apples test (Ledoit-Wolf 2008 studentized stationary-bootstrap CI on date-aligned Sharpe difference), **DCA is the only deployable strategy with statistical backing**. The ensemble (DCA + vol-v3 overlay) is "plausibly positive but not significant at 95%" — its incremental ΔSR sits inside the bootstrap CI for vol-v3 alone. The four arcs flagged as significantly *worse* than DCA confirm cross-sectional null findings (factor LS, momentum, lie-wide). Drivers: `packages/portfolio/src/ss_portfolio/sharpe_diff.py` (new), `apps/docs/scripts/compute_sharpe_diff_vs_dca.py` (new), `apps/vol/scripts/dump_vol_v3_realistic_cost.py` (new). Artifacts: `Output/sharpe-diff-vs-dca.json`, `Output/vol-v3-dolthub-oos-c{50,100,200,400}-returns.npz`. See `TODO/ladder-methodology-rewrite.md` (now done) and the closing finding `findings/ladder-methodology-rewrite.md`. |
| 2026-05-23 | meta | **DSR Step 1 — null-noise floor in quadrature (ladder-methodology-rewrite step 1 of 5)** (audit synthetic test showed workspace `sharpe_std_ann=0.25` double-counts the null component of cross-trial std at short n_obs; decomposed empirically to structural-only 0.072 ann + per-arc null floor `1/√(n_obs−1)` combined in quadrature inside `standardize_oos`) | full cross-arc DSR ladder (17 deployable arcs) | n/a (methodology recompute, no new run) | deflated-t shifts per arc; verdict re-anchored at t=+2 | n/a | **DCA-canonical +1.93 → +2.02 (clears t=+2 for the first time)** ; vol-v3-DoltHub +5.55 → +4.08 (still #1 but shrunk) ; relational +1.53 → +0.71 (Step 1 null-floor cut) ; vol-v3-gauss314 +1.32 → +0.09 (Step 1 wiped) ; lie-Phase-2 −0.86 → −1.58 (more decisive null) | three short-stream arcs (vol-v3-DoltHub, relational, vol-v3-gauss314) had their deflated-t inflated under prior calibration | [`diagnostic`](#verdict-labels) | Three-agent synthesis triggered this: methodology brief flagged DSR-as-cross-arc-rank misuse; assumptions audit flagged the 0.25 empirical's null-noise contamination; ensemble Optuna confirmed n_val=13 is below the noise floor. The variance decomposition: workspace observed 0.245 ann = √(0.234²_null + 0.072²_structural). New formula treats `sharpe_std` arg as structural-only, combines null floor in quadrature. Effect: short-stream arcs get punished more (correct), long-stream arcs barely move. **DCA-canonical's lift over the t=+2 bar is the load-bearing finding** — it's the only arc with a 20-year sample length AND a positive deflated-t under proper calibration. Driver `packages/portfolio/src/ss_portfolio/deflated.py` + `apps/docs/scripts/compute_dsr.py` (default `sharpe_std_ann=0.072`). Steps 2-5 remain: realistic options friction, per-arc-class calibration, Ledoit-Wolf CI column, stratified ladder. See `TODO/ladder-methodology-rewrite.md`. |
| 2026-05-25 | factor | **Crypto venue port — 74-channel indicator grid on CryptoCompare top-50 daily** (pre-registered Modal walkforward; 42 effective tickers from top-50 universe after 2000-bar floor; H=5d cross-sectional rebal; tinygrad linear & MLP heads, n_steps=200, lr=0.01, wd=1e-3; 5 walk-forward windows train=110/val=55/step=55; commission_bps=10 equity baseline; periods_per_year=73; pre-reg bar locked in `TODO/factor-crypto-venue-test.md` BEFORE Modal eval kicked off) | cryptocompare_top50_v1 (42 tickers, 2018-01 → 2026-05) | rebal_days=5, 5-fold walk-forward (train=110/val=55/step=55 blocks; 275 OOS blocks per scorer) | mean val IC ; positive-val-IC fraction ; DSR deflated-t (n_trials=2, sharpe_std_ann=0.40) | linear mean train IC +0.155 ; MLP +0.658 | **linear: mean val IC +0.0217 ; 4/5 positive (0.80) ; val Sharpe L/O −0.392 / L/S +0.110 ; DSR-t L/O −1.47 / L/S +0.055 ; MLP: mean val IC +0.0134 ; 4/5 positive (0.80) ; val Sharpe L/O −0.408 / L/S −0.118 ; DSR-t L/O −1.55 / L/S −0.87** | val IC bar (+0.025) FAILS both scorers ; DSR-t (+1.5) FAILS all 4 cells ; only pos-window-fraction clears | [`confirmed-null`](#verdict-labels) — 2 of 3 load-bearing gates fail; the 4/5 positive-windows gate is at binomial p≈0.19 under fair-coin null, well above the 5% threshold | The 74-channel deterministic indicator grid (RSI/CCI/MACD/vol/coherence) that produced equity arc's `confirmed-OOS` result at val IC +0.0212 6/6 windows (`findings/factor-shorthorizon-representation.md`) **does NOT transfer to crypto top-50 daily at H=5d**. Per the TODO's `confirmed-null` decision branch, the next venue-fit arc is **perp funding-rate cash-and-carry** (He-Manela-Ross 2023) — a structural premium with capacity constraints, free data via Binance/Bybit/OKX funding-rate endpoints, not a refinement of cross-sectional indicator features on crypto. **Mechanism guess (ordered by plausibility):** (a) indicator-grid parameter ranges are equity-microstructure-flavored, miss crypto's continuous-time bar structure; (b) crypto cross-section is conditioned on features the grid doesn't carry — perp funding term structure, on-chain flow, dominance regime, listing tier; (c) MLP train IC leaped +0.155 → +0.658 with no OOS lift (val fell +0.0217 → +0.0134), evidence the features carry training noise, not signal. **Surprises:** (a) the smoke-test n=50 val IC +0.0504 did NOT replicate at n_steps=200 (collapsed to +0.0217 for linear) — short-train artifact; (b) L/S Sharpe was the strongest cell (+0.110 linear) but is spot-only-conceptual, not deployable without perp funding accounting; (c) DSR-t with `sharpe_std_ann=0.40` (crypto cross-trial dispersion midpoint of TODO's recommended 0.35–0.50 band) puts all four cells deep below the +1.5 hurdle. **Caveats:** single horizon (5d) tested, single feature stack; longer-horizon TS-momentum on crypto (Moskowitz-Ooi-Pedersen 2012) is NOT falsified by this null. **Modal cost** ≈ $0.10, wall=36s linear + 48s MLP. Driver `apps/factor/scripts/modal/train_indicator_crypto.py`. Data prep `apps/factor/scripts/prep_crypto_universe.py`. Artifacts `Output/walkforward-crypto-summary.json` + `Output/walkforward-crypto-{linear,mlp}-s200-wd0.001-windows.npz`. Run `https://modal.com/apps/sid-ghodke/main/ap-eXYDZzA0envTyAIpQui8Ww`. Pre-reg [`TODO/factor-crypto-venue-test`](TODO/factor-crypto-venue-test.md). Finding [`findings/factor-crypto-venue`](findings/factor-crypto-venue.md). Parent (equity result this ported) [`findings/factor-shorthorizon-representation`](findings/factor-shorthorizon-representation.md). |
| 2026-05-23 | meta | **Meta-allocator regime-forecasting walk-forward — B3 inverse-arc-vol is the deployable winner** (pre-registered triple-benchmark + 5-candidate eval; pooled OOS 2015-01 → 2025-11 n=2740 days; 6-arc panel dca/gate/pairs/relational/dca_winner_4etf/vol_v3; rebal 20 TD; 10 bps switching cost per `|Δw|/2`; macro 6-stack for C2/C3; DSR n_trials=8) | meta-alloc-arcs-6 | meta-alloc-3fold-2015-25 | pooled OOS annualized Sharpe ; DSR-t (n_trials=8) ; Ledoit-Wolf ΔSR_ann CI vs each benchmark | n/a (held-out OOS) | **B3 inverse-vol Sharpe +1.722 / DSR-t +4.17 ; B1 persist L=252 +1.402 / +3.08 ; B2 1/N +1.355 / +2.98 ; C2 turb-overlay +1.318 / +2.84 ; C1 Markov+Laplace +1.294 / +2.78 ; C5 combo +1.213 / +2.51 ; B1 L=126 +1.143 / +2.20 ; C3 meta-label +1.068 / +2.04 ; C4 CUSUM +0.885 / +1.44 ; B1 L=504 +0.759 / +1.04** | only **B3 vs B2** clears all bars: ΔSR_ann +0.367 [+0.028, +0.682] (CI excludes 0, DSR-t +4.17, Δ ≥ +0.3) | [`confirmed-OOS`](#verdict-labels) for **B3 inverse-arc-vol vs B2 1/N** | The deployable meta-allocator across the 6-arc panel is **inverse-arc-volatility weighting** (B3): pooled OOS Sharpe +1.722 vs 1/N +1.355 (ΔSR_ann +0.367 [+0.028, +0.682], CI excludes 0). Every modeling candidate C1–C5 is `reversed-OOS` against B3 at 95% CI; none clears persistence (B1 L=252 +1.402) or 1/N. **Operational rule:** do not forecast which arc will win — weight inversely by each arc's trailing-252d realized vol, renormalize on availability. This is the Bridgewater All-Weather framing, reproduced on this specific 6-arc panel. **Surprises:** (a) persistence L=252 ties 1/N at ΔSR_ann +0.047 [−0.567, +0.689] — the literature's "trailing-Sharpe winner is the bar" turns out to be statistically indistinguishable from naive equal-weight on this panel; (b) C5 Rapach-Strauss-Zhou simple-mean ensemble does *worse* than each of its sub-baseline components — averaging three losers does not rescue them; (c) C4 CUSUM fired only 8 times across 2740 days at h=4.0 (literature's "regime detection is intrinsically lagged" lower bound, confirmed); (d) C3 meta-labeling (López de Prado framing) overfit 9 macro+arc-Sharpe features to ~50–150 labels per rebal — `reversed-OOS` vs both B2 and B3. **Caveats:** vol_v3 only enters fold 3 (2024-04-12 onward); the +0.367 CI excluding 0 is driven primarily by the 2015–2023 multi-arc base, not the vol_v3 tail. **Highest-EV follow-up:** re-run B3 vs B2 with vol_v3 excluded, to test whether the result is panel-structural or vol_v3-specific. Pre-reg: [`TODO/meta-allocator-regime-forecasting`](TODO/meta-allocator-regime-forecasting.md). Finding: [`findings/meta-allocator-regime-forecasting`](findings/meta-allocator-regime-forecasting.md). Driver: `apps/docs/scripts/meta_allocator_run.py`. Artifacts: `Output/meta-allocator-results.json` + `Output/meta-allocator-daily-streams.npz`. |
| 2026-05-22 | meta | **DSR ladder recompute — sharpe_std calibration fix (audit UF-2/UF-3)** (replaced the `1/sqrt(n_obs)` fallback in `standardize_oos` with an empirically-measured cross-trial dispersion of 0.25 annualized — observed std across 39 factor walk-forward arms in this workspace — and made `periods_per_year` a required NPZ key) | full cross-arc DSR ladder (15 deployable arcs) | n/a (methodology recompute, no new run) | deflated-t (DCA) ; pos-Δ count ; ranking-flip count | n/a | **DCA t +2.07 → +1.93 (still #1, no arc clears t=+2)** ; vol v3 +0.13 → +1.32 (#5 → #3) ; relational +0.74 → +1.53 (lifted at #2) ; 6 ranking flips, all within the sub-t=+2 cluster | the old fallback was bidirectionally wrong — over-deflated short-block arcs (vol n=30), under-deflated long-daily arcs (DCA n=5232) | [`diagnostic`](#verdict-labels) | The fallback used the null s.e. of a *single* Sharpe estimator (1/√n), not the cross-trial dispersion the DSR formula requires (Bailey-López de Prado 2014). With the empirical 0.25 (workspace n=39, observed 0.245), short-stream arcs *lift* and long-stream arcs *drop slightly*; the qualitative claim updates from "DCA is the only t-confident edge" to "**no arc clears t=+2; DCA is closest at +1.93, vol v3 and relational the next two at +1.32–1.53**". The frontier of "what survives deflation" widens modestly but the load-bearing conclusion — *no clean win on free public-equity data; next lever is paid/novel data or a different venue* — stands. Driver `apps/docs/scripts/compute_dsr.py` (rev 2026-05-22, adds `sharpe_std_ann` ArcSpec field + required `periods_per_year` key, raises on missing). See `findings/deflated-sharpe-leaderboard.md`. |

## Pending walk-forward checks called out elsewhere

These have hypotheses to test but no results yet; they're tracked in
the [TODO](TODO/index.md) backlog and should land here as new rows when
run:

- **DWT wider-universe validation** (factor-wide or stooq_us_long) —
  superseded by the 2026-05-09 ex-Phase-2 *uncompressed* run: the
  cross_ticker baseline already collapses to val Sharpe 0.48 on the
  wider universe, so layering DWT on top of an already-broken arm
  isn't informative. Skip unless we have a separate reason to test
  compression specifically.
- ~~**Equal-weight benchmark on Phase-2 + ex-Phase-2**~~ — **RAN
  2026-05-10**, both pre-test thresholds confirmed: Phase-2 EW val
  1.066 (model adds +0.067 alpha, within noise), ex-Phase-2 EW val
  0.832 (model is **−0.334 alpha**, catastrophic). Also extended to
  stooq_us_long where Morlet shows **−0.133 alpha**. See the three
  2026-05-10 leaderboard rows above and
  [`passive-ew-benchmark`](findings/passive-ew-benchmark.md). No
  current relational checkpoint clears its passive baseline.
- **Rebal-days sweep** (factor-narrow or phase-2) — sweep
  `rebal_days ∈ {5, 10, 20, 40}` to determine whether DWT-L1 supports
  a faster rebalance cadence than 20d. Gates the daily-cron-with-trigger
  decision in [TODO](TODO/index.md).
- **Non-Haar wavelet sweep** (phase-2) — db2/sym4/coif1 vs Haar at
  L=1. Cheap (~7 min) but only worth it after wider-universe and
  rebal-days resolve.
- **DCT zigzag-keep-top-k** (phase-2 head-to-head) — the deferred
  follow-up to the DWT/DCT compression breakthrough; needs a flat-input
  decoder branch in replay before the replay-side comparison is
  possible. Relational side can run as-is.
- **Modal-cron live deployment dry-run** (Alpaca paper) — first
  ~2-week paper-trade window comparing live decisions vs backtest
  expected positions. Counts as a "verdict-by-paper-trade" row, not
  a backtest row, but lands in the same table for visibility.

## Reading the table

A few interpretation notes:

1. **`confirmed-null` is information.** A row marked `confirmed-null`
   has a *false* hypothesis tested against and confirmed false; the
   data is useful — it tells future-you the lever has been tried and
   doesn't move. Don't re-run.
2. **`reversed-OOS` is the load-bearing column.** When it appears,
   the matching prose entry in [Findings](findings/index.md) should
   already be qualified — if it isn't, that's a doc bug.
3. **Single-split eval (e.g., `phase-2 split`) is weaker than rolling
   walk-forward.** A single train/val window can flatter or
   denigrate a finding by accident of regime alignment. Rolling
   windows are stronger evidence. The current relational result is
   single-split — a rolling-window protocol on Phase-2 would be a
   stronger follow-up, not a separate experiment.
4. **The `partial-OOS` label hides a lot.** When you see it, click
   through to the artifact path and read the per-window stats — the
   "partial" usually means "1 outlier window inflates the mean";
   medians are typically more honest than means in those cases.
5. **Sharpe ≠ skill at low IC.** Several factor rows show val Sharpe
   well above val IC's "alpha-positive" threshold (e.g., quarterly
   horizon at val IC ≈ 0 still posts +0.65 Sharpe). That's commission
   geometry, not signal — a near-zero-IC strategy with low turnover
   beats a higher-IC strategy with high turnover on Sharpe. Always
   read the IC column alongside the Sharpe column.
