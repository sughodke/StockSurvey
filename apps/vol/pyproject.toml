[project]
name = "vol"
version = "0.1.0"
requires-python = ">=3.10"
description = "Implied vol surface predictor — skew/smile/HV-ratio/OI-imbalance/VIX-spread features for the IV/RV gap forecast that NO_OPTIONS.md's 9 scorers did not test."
dependencies = [
    "ss-loaders",
    "ss-features",
    "ss-portfolio",
    "ss-iv",
    "ss-macro",
    "numpy",
    "pandas",
]

[project.optional-dependencies]
# `live` path needs Alpaca options support + the DCA leg (for the
# ensemble subcommand). `dca` is a workspace member; alpaca-py via
# the same extra ss-portfolio uses.
live = [
    "dca",
    "ss-portfolio[alpaca]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vol"]

[project.scripts]
ss-vol = "vol.cli:main"

[tool.uv.sources]
ss-loaders = { workspace = true }
ss-features = { workspace = true }
ss-portfolio = { workspace = true }
ss-iv = { workspace = true }
ss-macro = { workspace = true }
dca = { workspace = true }
