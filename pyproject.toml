[project]
name = "stocksurvey"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "joblib",
    "matplotlib",
    "numpy",
    "pandas",
    "requests",
    "scikit-learn",
    "scipy",
    "yfinance",
    "tqdm",
]
