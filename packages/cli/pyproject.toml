[project]
name = "ss-cli"
version = "0.1.0"
requires-python = ">=3.10"
description = "Shared argparse helpers for StockSurvey CLI scripts (data-loader + save args)."
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ss_cli"]
